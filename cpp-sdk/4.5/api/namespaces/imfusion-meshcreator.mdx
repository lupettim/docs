---
title: "ImFusion::MeshCreator"
sidebarTitle: "MeshCreator"
description: "Creates common mesh primitives with normals."
---

## Overview

Creates common mesh primitives with normals.

## Functions

- [Mesh](/cpp-sdk/4.5/api/classes/imfusion-mesh) \* [createTubularMesh](#createtubularmesh) (const [std::vector](http://en.cppreference.com/w/cpp/container/vector.html)&lt; vec3 &gt; &centerLine, const [std::vector](http://en.cppreference.com/w/cpp/container/vector.html)&lt; double &gt; &radius, int samples, [std::vector](http://en.cppreference.com/w/cpp/container/vector.html)&lt; vec3 &gt; \*samplePoints=0, [std::vector](http://en.cppreference.com/w/cpp/container/vector.html)&lt; vec3 &gt; \*sampleNormals=0, const [std::vector](http://en.cppreference.com/w/cpp/container/vector.html)&lt; vec3 &gt; &colors=&#123;&#125;, bool closeEnds=false) — Create tubular mesh given a center line and a radius.
- [Mesh](/cpp-sdk/4.5/api/classes/imfusion-mesh) \* **createTubularMesh** (const [std::vector](http://en.cppreference.com/w/cpp/container/vector.html)&lt; vec3 &gt; &centerLine, const [std::vector](http://en.cppreference.com/w/cpp/container/vector.html)&lt; vec3 &gt; &normal, const [std::vector](http://en.cppreference.com/w/cpp/container/vector.html)&lt; double &gt; &radius, int samples) — Create tubular mesh given a center line, normals and a radius. Samples specifies the number of segments for one tube revolution.
- [Mesh](/cpp-sdk/4.5/api/classes/imfusion-mesh) \* **createTubularMesh** (const [std::vector](http://en.cppreference.com/w/cpp/container/vector.html)&lt; [std::vector](http://en.cppreference.com/w/cpp/container/vector.html)&lt; vec3 &gt; &gt; &loops, bool close=false) — Create tubular mesh given individual loop data.
- [Mesh](/cpp-sdk/4.5/api/classes/imfusion-mesh) \* **createPyramid** (const vec3 &tip, const vec3 &baseP1, const vec3 &baseP2, const vec3 &baseP3, const vec3 &baseP4) — Create a pyramid.
- [Mesh](/cpp-sdk/4.5/api/classes/imfusion-mesh) \* **createPyramid** (double width, double height, bool createTextureCoordinates=false) — Create a pyramid This is a shortcut for [createCone()](#createcone) with four sides.
- [Mesh](/cpp-sdk/4.5/api/classes/imfusion-mesh) \* [createCone](#createcone) (double majorRadius, double minorRadius, double height, int samples=32, bool createTextureCoordinates=false) — Create a cone with given major radius, minor radius and height.
- [Mesh](/cpp-sdk/4.5/api/classes/imfusion-mesh) \* [createBox](#createbox) (const vec3 &extent, bool createTextureCoordinates=false) — Create a centered box with given extent.
- [Mesh](/cpp-sdk/4.5/api/classes/imfusion-mesh) \* [createBoundingBox](#createboundingbox) (const vec3 &center, const vec3 &extent, const mat3 &orientation=mat3::Identity(), bool createTextureCoordinates=false) — Create mesh depicting a bounding box with chosen center and extent.
- [Mesh](/cpp-sdk/4.5/api/classes/imfusion-mesh) \* [createCylinder](#createcylinder) (const vec3 &center, const vec3 &axis, double radius, double height, int samples, vec3 \*color=nullptr, bool createTextureCoordinates=false) — Create a closed cylinder mesh Optionally texture coordinates can be created.
- [Mesh](/cpp-sdk/4.5/api/classes/imfusion-mesh) \* [createIcosahedron](#createicosahedron) (double radius, bool smooth=false) — Create an icosahedron, which is a 12 vertices, 20 faces mesh.
- [Mesh](/cpp-sdk/4.5/api/classes/imfusion-mesh) \* [createIcosphere](#createicosphere) (double radius, int numSubdivisions=1, bool smooth=false) — Create an icosphere, generated by recursive-downsampling of an icosahedron; has a more even distribution of vertices around the surface.
- [Mesh](/cpp-sdk/4.5/api/classes/imfusion-mesh) \* **createSphere** (double radius, int samples=32, bool createTextureCoordinates=false) — Create a sphere mesh with the chosen radius and number of samples along Z (it uses twice as many in radial X/Y direction) Optionally the sphere can be created with (halfedge) texture coordinates following a equirectangular projection.
- [Mesh](/cpp-sdk/4.5/api/classes/imfusion-mesh) \* **createEllipsoid** (const vec3 &radius, int samples=32, bool createTextureCoordinates=false) — Create an ellipsoid; if only one radius is different, it is advised to use Z for that and rotate afterwards Optionally the ellipsoid can be created with (halfedge) texture coordinates following a equirectangular projection.
- [Mesh](/cpp-sdk/4.5/api/classes/imfusion-mesh) \* **createLineProjectedMesh** (const [GlLine](/cpp-sdk/4.5/api/classes/imfusion-glline) \*line, [GlImplicitlyPlanarPointBasedAnnotation](/cpp-sdk/4.5/api/classes/imfusion-glimplicitlyplanarpointbasedannotation) \*base) — Create a prism, using a planar annotation as base, and a line to determine projection axis and height.
- [Mesh](/cpp-sdk/4.5/api/classes/imfusion-mesh) \* [createRectangularGrid](#createrectangulargrid) (const vec2 dimension, const vec2i gridDivisions=vec2i::Zero()) — Creates a grid in the XY plane and normal of +Z, with the given dimensions.
- [std::unique_ptr](http://en.cppreference.com/w/cpp/memory/unique_ptr.html)&lt; [Mesh](/cpp-sdk/4.5/api/classes/imfusion-mesh) &gt; [createClosedSplineSurface](#createclosedsplinesurface) (const [GlSpline](/cpp-sdk/4.5/api/classes/imfusion-glspline) &spline) — Creates a surface mesh filling the inside of a closed spline, and texturizes it with a checkerboard pattern using the spline's color and transparency.

## Function Documentation

### createTubularMesh

```cpp
Mesh * ImFusion::MeshCreator::createTubularMesh (const std::vector < vec3 > & centerLine, const std::vector < double > & radius, int samples, std::vector < vec3 > * samplePoints = 0, std::vector < vec3 > * sampleNormals = 0, const std::vector < vec3 > & colors = {}, bool closeEnds = false)
```

Create tubular mesh given a center line and a radius.

Samples specifies the number of segments for one tube revolution. The normals are computed internally.

### createCone

```cpp
Mesh * ImFusion::MeshCreator::createCone (double majorRadius, double minorRadius, double height, int samples = 32, bool createTextureCoordinates = false)
```

Create a cone with given major radius, minor radius and height.

Axis are X, Y and Z respectively. Optionally texture coordinates can be created. If this is the case a distortion-free parametrization is computed that unwraps the cone shell and base onto a texture map. The shell is unwrapped on the left side of the texture with the base next to it. The whole parametrization is rescaled into a 1x1 square.

### createBox

```cpp
Mesh * ImFusion::MeshCreator::createBox (const vec3 & extent, bool createTextureCoordinates = false)
```

Create a centered box with given extent.

This is a shortcut for [createBoundingBox()](#createboundingbox) without specifying center and orientation

### createBoundingBox

```cpp
Mesh * ImFusion::MeshCreator::createBoundingBox (const vec3 & center, const vec3 & extent, const mat3 & orientation = mat3::Identity(), bool createTextureCoordinates = false)
```

Create mesh depicting a bounding box with chosen center and extent.

The column vectors of the optional orientation matrix define the basis of the bounding box. The basis must be right handed and normalized, i.e. the determinate of mat3 must be 1 or otherwise nullptr is returned. By default the box is axis-aligned. Optionally texture coordinates can be created. If this is the case a distortion-free parametrization is computed that unwraps the cube to the common "sideways t" texture map, where the aspect ratio of the leftmost face is extent[0]/extent[2], the second is extent[1]/extent[2] and so on, scaled appropriately to fit in a 1x1 square.

### createCylinder

```cpp
Mesh * ImFusion::MeshCreator::createCylinder (const vec3 & center, const vec3 & axis, double radius, double height, int samples, vec3 * color = nullptr, bool createTextureCoordinates = false)
```

Create a closed cylinder mesh Optionally texture coordinates can be created.

If this is the case a distortion-free parametrization is computed that unwraps the cylinder into disjoint shell and cap surfaces placed in a common texture map. If the height is greate than the cylinder circumference the discs are placed next to the shell in the texture map, otherwise they are placed above.

### createIcosahedron

```cpp
Mesh * ImFusion::MeshCreator::createIcosahedron (double radius, bool smooth = false)
```

Create an icosahedron, which is a 12 vertices, 20 faces mesh.

**Parameters:**

- `radius` — radius of the sphere the object fits into

### createIcosphere

```cpp
Mesh * ImFusion::MeshCreator::createIcosphere (double radius, int numSubdivisions = 1, bool smooth = false)
```

Create an icosphere, generated by recursive-downsampling of an icosahedron; has a more even distribution of vertices around the surface.

**Parameters:**

- `radius` — radius of the sphere
- `numSubdivisions` — number of downsampling passes on the icosahedron

### createRectangularGrid

```cpp
Mesh * ImFusion::MeshCreator::createRectangularGrid (const vec2 dimension, const vec2i gridDivisions = vec2i::Zero())
```

Creates a grid in the XY plane and normal of +Z, with the given dimensions.

Positive division splits the rectangular mesh into 2^k x 2^l rectangle patches to create a graph topology for meshing. (vec2i gridDivisions = (k,l))

### createClosedSplineSurface

```cpp
std::unique_ptr < Mesh > ImFusion::MeshCreator::createClosedSplineSurface (const GlSpline & spline)
```

Creates a surface mesh filling the inside of a closed spline, and texturizes it with a checkerboard pattern using the spline's color and transparency.

Calls Polygon2D::triangulate() internally.
