---
title: "ImFusion::GUI::SliceViewDefaultEventHandler::ActionSpeed"
sidebarTitle: "ActionSpeed"
description: "Record to define the speed at which a mouse/touch movement will modify the underlying view."
---

`#include <ImFusion/GUI/SliceViewDefaultEventHandler.h>`

## Overview

Record to define the speed at which a mouse/touch movement will modify the underlying view.

## Public Attributes

- double **mouseSpeed** = 1.0 — unitless factor by which the action's base speed is multiplied when using mouse movements
- double **touchSpeed** = 1.0 — unitless factor by which the action's base speed is multiplied when using touch movements
