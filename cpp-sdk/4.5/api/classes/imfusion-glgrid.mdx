---
title: "ImFusion::GlGrid"
sidebarTitle: "GlGrid"
---

## Public Member Functions

- **GlGrid** (const vec2 &size, const vec2 &spacing=&#123;10, 10&#125;)
- void **setSize** (const vec2 &size)
- void **setSpacing** (const vec2 &spacing)
- void **setLineWidth** (double width)
- void **setLineColor** (const vec4 &color)
- virtual void [draw](#draw) (const [GlView](/cpp-sdk/4.5/api/classes/imfusion-glview) &view) override — Draw the object in 3D space.
- virtual [Geometry::AlignedBox](/cpp-sdk/4.5/api/classes/imfusion-geometry-alignedbox) [bounds](#bounds) () const override — Get the axis-aligned bounding box of this [GlObject](/cpp-sdk/4.5/api/classes/imfusion-globject) in world space.
- virtual [std::string](http://en.cppreference.com/w/cpp/string/basic_string.html) [typeName](#typename) () const override — Return unique identifier for this object class used for serialization.
- virtual void [configure](#configure) (const [Properties](/cpp-sdk/4.5/api/classes/imfusion-properties) \*p) override — Configure this object instance by de-serializing the given [Properties](/cpp-sdk/4.5/api/classes/imfusion-properties).
- virtual void [configuration](#configuration) ([Properties](/cpp-sdk/4.5/api/classes/imfusion-properties) \*p) const override — Serialize the current object configuration into the given [Properties](/cpp-sdk/4.5/api/classes/imfusion-properties) object.

## Member Function Documentation

### draw

```cpp
virtual void ImFusion::GlGrid::draw (const GlView & view)
```

*override, virtual*

Draw the object in 3D space.

### bounds

```cpp
virtual Geometry::AlignedBox ImFusion::GlGrid::bounds () const
```

*override, virtual*

Get the axis-aligned bounding box of this [GlObject](/cpp-sdk/4.5/api/classes/imfusion-globject) in world space.

Return an empty (default-constructed) AlignedBox object if the [GlObject](/cpp-sdk/4.5/api/classes/imfusion-globject) does not reside in world space.

### typeName

```cpp
virtual std::string ImFusion::GlGrid::typeName () const
```

*override, virtual*

Return unique identifier for this object class used for serialization.

### configure

```cpp
virtual void ImFusion::GlGrid::configure (const Properties * p)
```

*override, virtual*

Configure this object instance by de-serializing the given [Properties](/cpp-sdk/4.5/api/classes/imfusion-properties).

The default implementation will do so automatically for all registered [Parameter](/cpp-sdk/4.5/api/classes/imfusion-parameter) and [SubProperty](/cpp-sdk/4.5/api/classes/imfusion-subproperty) instances.

**See also:** [configuration()](#configuration) for the inverse functionality

### configuration

```cpp
virtual void ImFusion::GlGrid::configuration (Properties * p) const
```

*override, virtual*

Serialize the current object configuration into the given [Properties](/cpp-sdk/4.5/api/classes/imfusion-properties) object.

The default implementation will do so automatically for all registered [Parameter](/cpp-sdk/4.5/api/classes/imfusion-parameter) and [SubProperty](/cpp-sdk/4.5/api/classes/imfusion-subproperty) instances.

**See also:** [configure()](#configure) for the inverse functionality
