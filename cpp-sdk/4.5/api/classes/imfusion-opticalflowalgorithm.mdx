---
title: "ImFusion::OpticalFlowAlgorithm"
sidebarTitle: "OpticalFlowAlgorithm"
description: "Algorithm to compute the optical flow along a sequence of images."
---

`#include <ImFusion/Base/OpticalFlowAlgorithm.h>`

## Overview

[Algorithm](/cpp-sdk/4.5/api/classes/imfusion-algorithm) to compute the optical flow along a sequence of images.

## Public Types

- enum OpticalFlowMode &#123; **OPENCV_FARNEBACK** = 0 , **BLOCK_MATCHING** , **LINEAR_REGISTRATION** , **DEMONS_REGISTRATION** &#125; — Different computation modes.

## Public Member Functions

- **OpticalFlowAlgorithm** ([SharedImageSet](/cpp-sdk/4.5/api/classes/imfusion-sharedimageset) \*img) — Creates the algorithm instance with a series of images.
- **~OpticalFlowAlgorithm** () override — Destructor.

## Methods implementing the algorithm interface

- void [compute](#compute) () override — Applies the processing.
- [OwningDataList](/cpp-sdk/4.5/api/classes/imfusion-owningdatalist) [takeOutput](#takeoutput) () override — If new data was created, make it available here.
- static bool [createCompatible](#createcompatible) (const [DataList](/cpp-sdk/4.5/api/classes/imfusion-datalist) &data, [Algorithm](/cpp-sdk/4.5/api/classes/imfusion-algorithm) \*\*a=0)

## Methods implementing the Configurable interface

- void [configure](#configure) (const [Properties](/cpp-sdk/4.5/api/classes/imfusion-properties) \*p) override — Configure this object instance by de-serializing the given [Properties](/cpp-sdk/4.5/api/classes/imfusion-properties).
- void [configuration](#configuration) ([Properties](/cpp-sdk/4.5/api/classes/imfusion-properties) \*p) const override — Serialize the current object configuration into the given [Properties](/cpp-sdk/4.5/api/classes/imfusion-properties) object.

## Member Function Documentation

### createCompatible

```cpp
bool ImFusion::OpticalFlowAlgorithm::createCompatible (const DataList & data, Algorithm ** a = 0)
```

*static*

Factory method to check for applicability or to create the algorithm

### compute

```cpp
void ImFusion::OpticalFlowAlgorithm::compute ()
```

*override, virtual*

Applies the processing.

### takeOutput

```cpp
OwningDataList ImFusion::OpticalFlowAlgorithm::takeOutput ()
```

*override, virtual*

If new data was created, make it available here.

### configure

```cpp
void ImFusion::OpticalFlowAlgorithm::configure (const Properties * p)
```

*override, virtual*

Configure this object instance by de-serializing the given [Properties](/cpp-sdk/4.5/api/classes/imfusion-properties).

The default implementation will do so automatically for all registered [Parameter](/cpp-sdk/4.5/api/classes/imfusion-parameter) and [SubProperty](/cpp-sdk/4.5/api/classes/imfusion-subproperty) instances.

**See also:** [configuration()](#configuration) for the inverse functionality

### configuration

```cpp
void ImFusion::OpticalFlowAlgorithm::configuration (Properties * p) const
```

*override, virtual*

Serialize the current object configuration into the given [Properties](/cpp-sdk/4.5/api/classes/imfusion-properties) object.

The default implementation will do so automatically for all registered [Parameter](/cpp-sdk/4.5/api/classes/imfusion-parameter) and [SubProperty](/cpp-sdk/4.5/api/classes/imfusion-subproperty) instances.

**See also:** [configure()](#configure) for the inverse functionality
