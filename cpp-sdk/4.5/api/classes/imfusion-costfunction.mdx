---
title: "ImFusion::CostFunction"
sidebarTitle: "CostFunction"
description: "Interface for a cost function that can be optimized."
---

`#include <ImFusion/Base/CostFunction.h>`

## Overview

Interface for a cost function that can be optimized.

## Public Member Functions

- virtual double [evaluate](#evaluate) (int n, const double \*x, double \*dx=0)=0 — Evaluate the cost function.

## Member Function Documentation

### evaluate

```cpp
virtual double ImFusion::CostFunction::evaluate (int n, const double * x, double * dx = 0)
```

*pure virtual*

Evaluate the cost function.

n is the number of dimensions, x the parameters of size n. If the pointer dx is provided, the cost function shall compute partial derivatives and write it into dx.
