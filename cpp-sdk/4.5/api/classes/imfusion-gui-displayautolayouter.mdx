---
title: "ImFusion::GUI::DisplayAutoLayouter"
sidebarTitle: "DisplayAutoLayouter"
description: "This class facilitates the layouting of Views inside a Display by encapsulating logic to generate layouts for dynamic view configurations based on a customizabl"
---

`#include <ImFusion/GUI/DisplayAutoLayouter.h>`

## Overview

This class facilitates the layouting of Views inside a [Display](/cpp-sdk/4.5/api/classes/imfusion-gui-display) by encapsulating logic to generate layouts for dynamic view configurations based on a customizable set of layouting algorithms.

The [DisplayAutoLayouter](/cpp-sdk/4.5/api/classes/imfusion-gui-displayautolayouter) is an optional building block that operates on the [Display](/cpp-sdk/4.5/api/classes/imfusion-gui-display) instance passed during construction to automatically create an appropriate [DisplayLayout](/cpp-sdk/4.5/api/classes/imfusion-gui-displaylayout) for it. You do not need to register Views with [DisplayAutoLayouter](/cpp-sdk/4.5/api/classes/imfusion-gui-displayautolayouter) since it will query [Display::views()](/cpp-sdk/4.5/api/classes/imfusion-gui-display) when needed. However, you can configure the visibility of individual Views, an optional ordering, and select the used layouting algorithm. Furthermore, you can specify additional settings for the layouting algorithm such as an optional maximizedView(), focusedView(), and lastViewInTopRow().

[DisplayAutoLayouter](/cpp-sdk/4.5/api/classes/imfusion-gui-displayautolayouter) will automatically apply the resulting [DisplayLayoutConfig](/cpp-sdk/4.5/api/classes/imfusion-gui-displaylayoutconfig) to the target [Display](/cpp-sdk/4.5/api/classes/imfusion-gui-display) passed during construction and call [DisplayLayout::layout()](/cpp-sdk/4.5/api/classes/imfusion-gui-displaylayout). You can subscribe to the signalLayoutingPerformed to be notified whenever this happens.

This class comes with a set of predefined layouting algorithms that you can access through the [Mode](#mode) enumeration. Furthermore, you can create custom algorithms by implementing the [LayoutAlgorithm](/cpp-sdk/4.5/api/classes/imfusion-gui-displayautolayouter-layoutalgorithm) interface and registering it through addLayoutAlgorithm().

The [Feature](#feature) enumeration enables a [LayoutAlgorithm](/cpp-sdk/4.5/api/classes/imfusion-gui-displayautolayouter-layoutalgorithm) to indicate whether or not it supports a feature of the [DisplayAutoLayouter](/cpp-sdk/4.5/api/classes/imfusion-gui-displayautolayouter). They do not have a direct impact on the functionality but allow consumers of this class (such as the [ViewLayoutWidget](/cpp-sdk/4.5/api/classes/imfusion-viewlayoutwidget)) to implement custom logic based on the selected/available layouting algorithms.

**Note:** [DisplayAutoLayouter](/cpp-sdk/4.5/api/classes/imfusion-gui-displayautolayouter) has no way of automatically getting notified if the target display's set of views has changed. You must call [resetLayout()](#resetlayout) manually to trigger the layouting process in which it will query the updated list of views.

**See also:** [Display](/cpp-sdk/4.5/api/classes/imfusion-gui-display), [DisplayLayout](/cpp-sdk/4.5/api/classes/imfusion-gui-displaylayout), [DisplayLayoutConfig](/cpp-sdk/4.5/api/classes/imfusion-gui-displaylayoutconfig)

## Classes

- struct [LayoutInfo](/cpp-sdk/4.5/api/structs/imfusion-gui-displayautolayouter-layoutinfo) — Record of information passed to LayoutFunction::createLayout().
- class [LayoutAlgorithm](/cpp-sdk/4.5/api/classes/imfusion-gui-displayautolayouter-layoutalgorithm) — Interface to derive from to define custom display layout algorithms.

## Public Types

- enum class [Mode](#mode) &#123; Auto = 0 , Rows = 1 , FocusPlusStack = 2 , FocusPlusRows = 3 , Custom = 100 &#125; — Enumeration of predefined layouting algorithms.
- enum class [Feature](#feature) &#123; **None** = 0 , Supports2DView = 1 , SupportsMPRViews = 2 , Supports3DView = 4 , SupportsAlgorithmViews = 8 , SupportsFocus = 16 , SupportsTopBottomRow = 32 , **SupportsAllViews** = Supports2DView | SupportsMPRViews | Supports3DView | SupportsAlgorithmViews &#125; — Enumeration of layouting features that a [LayoutAlgorithm](/cpp-sdk/4.5/api/classes/imfusion-gui-displayautolayouter-layoutalgorithm) may support.

## Public Member Functions

- **DisplayAutoLayouter** ([DisplayBase](/cpp-sdk/4.5/api/classes/imfusion-gui-displaybase) &targetDisplay)
- const [DisplayLayoutConfig](/cpp-sdk/4.5/api/classes/imfusion-gui-displaylayoutconfig) & [resetLayout](#resetlayout) () — Recompute the view layout with the currently configured settings and apply it to targetDisplay()-&gt;layout().
- const [DisplayLayoutConfig](/cpp-sdk/4.5/api/classes/imfusion-gui-displaylayoutconfig) & **lastLayoutConfig** () const — Returns the [DisplayLayoutConfig](/cpp-sdk/4.5/api/classes/imfusion-gui-displaylayoutconfig) generated by the last call to [resetLayout()](#resetlayout).
- void [configure](#configure) (const [Properties](/cpp-sdk/4.5/api/classes/imfusion-properties) \*p) override — Configure this object instance by de-serializing the given [Properties](/cpp-sdk/4.5/api/classes/imfusion-properties).
- void [configuration](#configuration) ([Properties](/cpp-sdk/4.5/api/classes/imfusion-properties) \*p) const override — Serialize the current object configuration into the given [Properties](/cpp-sdk/4.5/api/classes/imfusion-properties) object.

## Public Attributes

- Signal&lt; [Mode](#mode) &gt; **signalModeChanged** — Emitted whenever the currentMode() has changed.
- Signal **signalLayoutingPerformed** — Emitted whenever the views have been re-layouted and targetDisplay()-&gt;layout() was changed.
- Signal&lt; const [View](/cpp-sdk/4.5/api/classes/imfusion-gui-view) \*, bool &gt; **signalViewHiddenChanged** — Emitted whenever the hidden-state for a registered view was changed.

## Specify the list of views to be layouted, their visibility, and their order

- bool [isViewHidden](#isviewhidden) (const [View](/cpp-sdk/4.5/api/classes/imfusion-gui-view) \*view) const — Returns whether *view* should be hidden in the generated [DisplayLayout](/cpp-sdk/4.5/api/classes/imfusion-gui-displaylayout).
- void [setViewHidden](#setviewhidden) (const [View](/cpp-sdk/4.5/api/classes/imfusion-gui-view) \*view, bool value) — Sets whether *view* should be hidden in the generated [DisplayLayout](/cpp-sdk/4.5/api/classes/imfusion-gui-displaylayout).
- void [setViewsHidden](#setviewshidden) (const [std::vector](http://en.cppreference.com/w/cpp/container/vector.html)&lt; const [View](/cpp-sdk/4.5/api/classes/imfusion-gui-view) \* &gt; &views, bool value) — Sets whether the given set of *views* should be hidden in the generated [DisplayLayout](/cpp-sdk/4.5/api/classes/imfusion-gui-displaylayout).
- [std::vector](http://en.cppreference.com/w/cpp/container/vector.html)&lt; const [View](/cpp-sdk/4.5/api/classes/imfusion-gui-view) \* &gt; **visibleViews** () const — Returns all views of the targetDisplay() that are configured as *not* hidden considering the optional viewOrder() if it was set.
- void [setViewOrder](#setvieworder) ([std::optional](http://en.cppreference.com/w/cpp/utility/optional.html)&lt; [std::vector](http://en.cppreference.com/w/cpp/container/vector.html)&lt; const [View](/cpp-sdk/4.5/api/classes/imfusion-gui-view) \* &gt; &gt; viewOrder) — Set an optional custom view order to be used by the display layout.
- const [std::optional](http://en.cppreference.com/w/cpp/utility/optional.html)&lt; [std::vector](http://en.cppreference.com/w/cpp/container/vector.html)&lt; const [View](/cpp-sdk/4.5/api/classes/imfusion-gui-view) \* &gt; &gt; & **viewOrder** () const — Returns the optional custom order of the views shown in the display layout.
- void [swapViewOrder](#swapvieworder) (const [View](/cpp-sdk/4.5/api/classes/imfusion-gui-view) \*view1, const [View](/cpp-sdk/4.5/api/classes/imfusion-gui-view) \*view2) — Swaps the position of the two given views in the display layout.

## Configure optional settings passed to the layouting algorithm if applicable.

- const [View](/cpp-sdk/4.5/api/classes/imfusion-gui-view) \* **maximizedView** () const — Returns the optional view that should be shown alone in a maximized fashion.
- void [setMaximizedView](#setmaximizedview) (const [View](/cpp-sdk/4.5/api/classes/imfusion-gui-view) \*value) — Sets an optional view that should be shown alone in a maximized fashion.
- const [View](/cpp-sdk/4.5/api/classes/imfusion-gui-view) \* **focusedView** () const — Returns the view that is in focus if supported by the current layout mode.
- void **setFocusedView** (const [View](/cpp-sdk/4.5/api/classes/imfusion-gui-view) \*value) — Sets the view that is in focus if supported by the current layout mode.
- const [GUI::View](/cpp-sdk/4.5/api/classes/imfusion-gui-view) \* **lastViewInTopRow** () const — Returns a pointer to last view to be shown in top row in cases where views shall not be distributed evenly.
- void [moveViewToTopRow](#moveviewtotoprow) (const [View](/cpp-sdk/4.5/api/classes/imfusion-gui-view) \*view) — Moves the given *view* to the top row for display layouts that support LayoutFeatures::FeaturesTopBottomRow.
- void [moveViewToBottomRow](#moveviewtobottomrow) (const [View](/cpp-sdk/4.5/api/classes/imfusion-gui-view) \*view) — Moves the given *view* to the bottom row for display layouts that support LayoutFeatures::FeaturesTopBottomRow.

## Select the layouting algorithm/mode and add/remove custom algorithms.

- [Mode](#mode) **currentMode** () const — Returns the current view layout mode.
- void **setCurrentMode** ([Mode](#mode) value) — Sets the current view layout mode.
- const [LayoutAlgorithm](/cpp-sdk/4.5/api/classes/imfusion-gui-displayautolayouter-layoutalgorithm) & **currentLayoutAlgorithm** () const — Returns the currently active layouting algorithm.
- [Mode](#mode) **addLayoutAlgorithm** ([std::unique_ptr](http://en.cppreference.com/w/cpp/memory/unique_ptr.html)&lt; [LayoutAlgorithm](/cpp-sdk/4.5/api/classes/imfusion-gui-displayautolayouter-layoutalgorithm) &gt; algorithm) — Registers a new custom layouting algorithm and returns the custom [Mode](#mode) (&gt;= Mode::Custom) under which it was registered.
- void [removeLayoutAlgorithm](#removelayoutalgorithm) ([Mode](#mode) mode) — Removes a layouting algorithm that was previously registered under the given *mode*.
- [LayoutAlgorithm](/cpp-sdk/4.5/api/classes/imfusion-gui-displayautolayouter-layoutalgorithm) \* **layoutAlgorithm** ([Mode](#mode) mode) const — Returns the layouting algorithm that was registered for the given *mode* or nullptr if no such algorithm is found.

## Member Enumeration Documentation

### Mode

```cpp
enum class ImFusion::GUI::DisplayAutoLayouter::Mode
```

*strong*

Enumeration of predefined layouting algorithms.

**Enumerator:**

- `Auto` — Automatically chooses one between Rows or FocusPlusStack mode based on the visible views.
- `Rows` — Arranges all views in a grid-like fashion with up to 2 rows.
- `FocusPlusStack` — Shows a large focus view and vertically stacks all other views to the right.
- `FocusPlusRows` — Shows a large focus view and uses the Rows layout on the right for the other views.
- `Custom` — First enum value to be used for custom algorithms registered through addLayoutAlgorithm().

### Feature

```cpp
enum class ImFusion::GUI::DisplayAutoLayouter::Feature
```

*strong*

Enumeration of layouting features that a [LayoutAlgorithm](/cpp-sdk/4.5/api/classes/imfusion-gui-displayautolayouter-layoutalgorithm) may support.

**Enumerator:**

- `Supports2DView` — supports show/hide 2D view
- `SupportsMPRViews` — supports show/hide MPR views
- `Supports3DView` — supports show/hide 3D view
- `SupportsAlgorithmViews` — supports show/hide algorithm views
- `SupportsFocus` — supports a view being focused e.g. larger or more prominent than the rest
- `SupportsTopBottomRow` — supports a view being move to top/bottom row

## Member Function Documentation

### isViewHidden

```cpp
bool ImFusion::GUI::DisplayAutoLayouter::isViewHidden (const View * view) const
```

Returns whether *view* should be hidden in the generated [DisplayLayout](/cpp-sdk/4.5/api/classes/imfusion-gui-displaylayout).

**Note:** The naming of this function was chosen carefully to express the visibility logic of [DisplayAutoLayouter](/cpp-sdk/4.5/api/classes/imfusion-gui-displayautolayouter). The "hidden" state of a view serves only as input to the layouting algorithm. Marking a view as hidden means that it will never be considered during layouting and thus never be visible. However, a view that is *not* marked hidden may still not appear in the resulting currentLayout():
- The LayoutFunction may choose to display a view even if it is supposed to be hidden.
- Setting a maximizedView() will hide all other views even they are set visible.
- Only views that are part of targetDisplay() will be considered.

### setViewHidden

```cpp
void ImFusion::GUI::DisplayAutoLayouter::setViewHidden (const View * view, bool value)
```

Sets whether *view* should be hidden in the generated [DisplayLayout](/cpp-sdk/4.5/api/classes/imfusion-gui-displaylayout).

**Note:** The naming of this function was chosen carefully to express the visibility logic of [DisplayAutoLayouter](/cpp-sdk/4.5/api/classes/imfusion-gui-displayautolayouter). The "hidden" state of a view serves only as input to the layouting algorithm. Marking a view as hidden means that it will never be considered during layouting and thus never be visible. However, a view that is *not* marked hidden may still not appear in the resulting currentLayout():
- The LayoutFunction may choose to display a view even if it is supposed to be hidden.
- Setting a maximizedView() will hide all other views even they are set visible.
- Only views that are part of targetDisplay() will be considered.

### setViewsHidden

```cpp
void ImFusion::GUI::DisplayAutoLayouter::setViewsHidden (const std::vector < const View * > & views, bool value)
```

Sets whether the given set of *views* should be hidden in the generated [DisplayLayout](/cpp-sdk/4.5/api/classes/imfusion-gui-displaylayout).

**Note:** The naming of this function was chosen carefully to express the visibility logic of [DisplayAutoLayouter](/cpp-sdk/4.5/api/classes/imfusion-gui-displayautolayouter). The "hidden" state of a view serves only as input to the layouting algorithm. Marking a view as hidden means that it will never be considered during layouting and thus never be visible. However, a view that is *not* marked hidden may still not appear in the resulting currentLayout():
- The LayoutFunction may choose to display a view even if it is supposed to be hidden.
- Setting a maximizedView() will hide all other views even they are set visible.
- Only views that are part of targetDisplay() will be considered.

### setViewOrder

```cpp
void ImFusion::GUI::DisplayAutoLayouter::setViewOrder (std::optional < std::vector < const View * > > viewOrder)
```

Set an optional custom view order to be used by the display layout.

If configured, visibleViews() will consider the order in addition to viewVisibility() when filtering the views of targetDisplay(). Missing views will be appended to the end of the list.

### swapViewOrder

```cpp
void ImFusion::GUI::DisplayAutoLayouter::swapViewOrder (const View * view1, const View * view2)
```

Swaps the position of the two given views in the display layout.

**Note:** This function will synchronize viewOrder() with the targetDisplay()-&gt;views() so that they contain the same set of views.

### setMaximizedView

```cpp
void ImFusion::GUI::DisplayAutoLayouter::setMaximizedView (const View * value)
```

Sets an optional view that should be shown alone in a maximized fashion.

If you pass nullptr it will restore the layout config from before setting the maximized view.

### moveViewToTopRow

```cpp
void ImFusion::GUI::DisplayAutoLayouter::moveViewToTopRow (const View * view)
```

Moves the given *view* to the top row for display layouts that support LayoutFeatures::FeaturesTopBottomRow.

**Note:** This function will synchronize viewOrder() with the targetDisplay()-&gt;views() so that they contain the same set of views.

### moveViewToBottomRow

```cpp
void ImFusion::GUI::DisplayAutoLayouter::moveViewToBottomRow (const View * view)
```

Moves the given *view* to the bottom row for display layouts that support LayoutFeatures::FeaturesTopBottomRow.

**Note:** This function will synchronize viewOrder() with the targetDisplay()-&gt;views() so that they contain the same set of views.

### removeLayoutAlgorithm

```cpp
void ImFusion::GUI::DisplayAutoLayouter::removeLayoutAlgorithm (Mode mode)
```

Removes a layouting algorithm that was previously registered under the given *mode*.

Function will have no effect if *mode* is currentMode().

### resetLayout

```cpp
const DisplayLayoutConfig & ImFusion::GUI::DisplayAutoLayouter::resetLayout ()
```

Recompute the view layout with the currently configured settings and apply it to targetDisplay()-&gt;layout().

Normally, you do not need to call this function from the outside explicitly because all other member functions ensure to call it when necessary. You can query the result through observing the return value, querying lastLayoutConfig() afterwards, or by subscribing to signalLayoutingPerformed.

### configure

```cpp
void ImFusion::GUI::DisplayAutoLayouter::configure (const Properties * p)
```

*override, virtual*

Configure this object instance by de-serializing the given [Properties](/cpp-sdk/4.5/api/classes/imfusion-properties).

The default implementation will do so automatically for all registered [Parameter](/cpp-sdk/4.5/api/classes/imfusion-parameter) and [SubProperty](/cpp-sdk/4.5/api/classes/imfusion-subproperty) instances.

**See also:** [configuration()](#configuration) for the inverse functionality

### configuration

```cpp
void ImFusion::GUI::DisplayAutoLayouter::configuration (Properties * p) const
```

*override, virtual*

Serialize the current object configuration into the given [Properties](/cpp-sdk/4.5/api/classes/imfusion-properties) object.

The default implementation will do so automatically for all registered [Parameter](/cpp-sdk/4.5/api/classes/imfusion-parameter) and [SubProperty](/cpp-sdk/4.5/api/classes/imfusion-subproperty) instances.

**See also:** [configure()](#configure) for the inverse functionality
