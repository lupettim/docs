---
title: "ImFusion::SimilarityMeasureFactory"
sidebarTitle: "SimilarityMeasureFactory"
description: "The SimilarityMeasureFactory class provides a generic interface to any kind of SimilarityMeasure ."
---

`#include <ImFusion/Reg/SimilarityMeasureFactory.h>`

## Overview

The [SimilarityMeasureFactory](/cpp-sdk/4.5/api/classes/imfusion-similaritymeasurefactory) class provides a generic interface to any kind of [SimilarityMeasure](/cpp-sdk/4.5/api/classes/imfusion-similaritymeasureimpl).

Different types of SimilarityMeasures, with one or multiple implementations, can be registered to the factory during runtime. A set of default SimilarityMeasures are always registered.

Because different SimilarityMeasure may have different parameters, [SimilarityMeasureFactory](/cpp-sdk/4.5/api/classes/imfusion-similaritymeasurefactory) offers a generic way to access this parameters with the help of the ImFusion::Properties interface. Each [SimilarityMeasureFactory](/cpp-sdk/4.5/api/classes/imfusion-similaritymeasurefactory) contains a persistent [Properties](/cpp-sdk/4.5/api/classes/imfusion-properties) instance that contains the parameters of the current mode. This way, the [Properties](/cpp-sdk/4.5/api/classes/imfusion-properties) of a SimilarityMeasure can be accessed and modified without creating an actual instance of the SimilarityMeasure. Together with the ImFusion::PropertiesWidget it is relatively simple to generate a generic user interface to configure any kind of SimilarityMeasure.

Because some SimilarityMeasures use large amounts of (V)RAM for opimizations, a SimilarityMeasure instance should only be created while needed and deleted afterwards.

Basic usage:

```cpp
SimilarityMeasureFactory factory;
SimilarityMeasureImpl* sm = factory.generate(img1, img2);
while(something) {
   ...
 double val = sm->compute(nullptr);
   ...
}
delete sm;
```
 fragment
Exposing SimilarityMeasure properties in UI:

```cpp
SimilarityMeasureFactory factory;
// show available modes in a combobox
for(auto mode: sm->availableModes())
    qcombobox->addItem(QString::fromStdString(sm->acronym(mode)), (int)mode);
// create another combobox for available devices, connect signals...
...
// create a properties widget and show the current properties
PropertiesWidget pw;
Properties p("");
factory.similarityMeasureConfiguration(&p);
pw->configure(&p);
...
// get the user input from the widget and apply it to the factory
Properties p("");
pw->configuration(&p);
factory.configureSimilarityMeasure(&p);
```
 fragment
,

## Public Types

- enum Mode : int &#123; **SAD** = 0 , **SSD** = 1 , **NCC** = 2 , **LNCC** = 3 , **LC2** = 4 , **MI** = 5 , **HIST** = 6 , **ML** = 7 , **NGF** = 8 , **SSIM** = 9 , **DP** = 10 , **USERDEFINED** = 100 &#125; — Enumeration of available similarity measures.

## Public Member Functions

- **SimilarityMeasureFactory** (Mode mode=Mode::NCC, Device device=Device_GL)
- [std::vector](http://en.cppreference.com/w/cpp/container/vector.html)&lt; Mode &gt; **availableModes** () const — Returns all available modes of SimilarityMeasures in this factory.
- [std::vector](http://en.cppreference.com/w/cpp/container/vector.html)&lt; Device &gt; **availableDevices** (Mode mode) const — Returns all available implementations for a given mode.
- [std::string](http://en.cppreference.com/w/cpp/string/basic_string.html) **name** (Mode mode) const — Returns the full name of the SimilarityMeasure with the given mode.
- [std::string](http://en.cppreference.com/w/cpp/string/basic_string.html) **acronym** (Mode mode) const — Returns the acronym of the SimilarityMeasure with the given mode.
- Mode **mode** () const — Returns the current SimilarityMeasure mode that the factory will generate.
- Device **device** () const — Returns the current device for the mode generated by the factory.
- void **setMode** (Mode mode) — Sets the SimilarityMeasure mode the factory will generate If the current device is not available for this mode, the device will be changed to the first supported for this mode.
- bool [setDevice](#setdevice) (Device device) — Sets the device for the mode generated by the factory.
- virtual void [setWeights](#setweights) (const [SharedImage](/cpp-sdk/4.5/api/classes/imfusion-sharedimage) \*weights1, const [SharedImage](/cpp-sdk/4.5/api/classes/imfusion-sharedimage) \*weights2) — Sets the images to use as weights for the similarity metric.
- void [configure](#configure) (const [Properties](/cpp-sdk/4.5/api/classes/imfusion-properties) \*p) override — Configure this object instance by de-serializing the given [Properties](/cpp-sdk/4.5/api/classes/imfusion-properties).
- void [configuration](#configuration) ([Properties](/cpp-sdk/4.5/api/classes/imfusion-properties) \*p) const override — Serialize the current object configuration into the given [Properties](/cpp-sdk/4.5/api/classes/imfusion-properties) object.
- void [similarityMeasureConfiguration](#similaritymeasureconfiguration) ([Properties](/cpp-sdk/4.5/api/classes/imfusion-properties) \*p, bool filterUnrelated=true) const — Retrieves the configuration for a SimilarityMeasure of the current mode.
- void [configureSimilarityMeasure](#configuresimilaritymeasure) (const [Properties](/cpp-sdk/4.5/api/classes/imfusion-properties) \*p) — Sets the configuration for the current SimilarityMeasure mode.
- void [setSimilarityMeasureParam](#setsimilaritymeasureparam) (const [std::string](http://en.cppreference.com/w/cpp/string/basic_string.html) &name, const T &value) — Convenient method to directly set a parameter of the SimilarityMeasure.
- bool [similarityMeasureParam](#similaritymeasureparam) (const [std::string](http://en.cppreference.com/w/cpp/string/basic_string.html) &name, T &value) — Convenient method to directly retrieve a parameter of the SimilarityMeasure.
- [SimilarityMeasureImpl](/cpp-sdk/4.5/api/classes/imfusion-similaritymeasureimpl) \* [generate](#generate) ([SharedImage](/cpp-sdk/4.5/api/classes/imfusion-sharedimage) \*img1, [SharedImage](/cpp-sdk/4.5/api/classes/imfusion-sharedimage) \*img2) — Generates a new SimilarityMeasure instance.

## Static Public Member Functions

- static bool [registerSimilarityMeasure](#registersimilaritymeasure) (Mode mode, Device device, const [std::string](http://en.cppreference.com/w/cpp/string/basic_string.html) &name, const [std::string](http://en.cppreference.com/w/cpp/string/basic_string.html) &acronym, [std::function](http://en.cppreference.com/w/cpp/utility/functional/function.html)&lt; [SimilarityMeasureImpl](/cpp-sdk/4.5/api/classes/imfusion-similaritymeasureimpl) \*([SharedImage](/cpp-sdk/4.5/api/classes/imfusion-sharedimage) \*, [SharedImage](/cpp-sdk/4.5/api/classes/imfusion-sharedimage) \*)&gt; createFunc=nullptr) — Registers a new [SimilarityMeasure](/cpp-sdk/4.5/api/classes/imfusion-similaritymeasureimpl) to the factory.
- static void **unregisterSimilarityMeasure** (Mode mode, Device device) — Removes a previous registered similarity measure.

## Member Function Documentation

### registerSimilarityMeasure

```cpp
template<class T> bool ImFusion::SimilarityMeasureFactory::registerSimilarityMeasure (Mode mode, Device device, const std::string & name, const std::string & acronym, std::function < SimilarityMeasureImpl *(SharedImage *, SharedImage *)> createFunc = nullptr)
```

*inline, static*

Registers a new [SimilarityMeasure](/cpp-sdk/4.5/api/classes/imfusion-similaritymeasureimpl) to the factory.

The template parameter must derive from ImFusion::SimilarityMeasureImpl. When registering multiple implementations for the same mode, the same name and acronym can be used. Example:

```cpp
// register SAD with two different implementation
SimilarityMeasureFactory::registerSimilarityMeasure<SimilarityMeasureSAD_CPU>(SimilarityMeasureFactory::SAD, Device_CPU, "Sum of Absolute
Differences", "SAD"); SimilarityMeasureFactory::registerSimilarityMeasure<SimilarityMeasureSAD_GL>(SimilarityMeasureFactory::SAD,
Device_GL, "Sum of Absolute Differences", "SAD");
// register SSD with one implementation
SimilarityMeasureFactory::registerSimilarityMeasure<SimilarityMeasureSSD_CPU>(SimilarityMeasureFactory::SSD, Device_CPU, "Sum of Squared
Differences", "SSD");
```
 fragment

### setDevice

```cpp
bool ImFusion::SimilarityMeasureFactory::setDevice (Device device)
```

Sets the device for the mode generated by the factory.

If the given device is not supported by the current mode, the current device remains unchanged and false is returned.

### setWeights

```cpp
virtual void ImFusion::SimilarityMeasureFactory::setWeights (const SharedImage * weights1, const SharedImage * weights2)
```

*virtual*

Sets the images to use as weights for the similarity metric.

Setting weights only has effect if the similarity measure supports weights.

### configure

```cpp
void ImFusion::SimilarityMeasureFactory::configure (const Properties * p)
```

*override, virtual*

Configure this object instance by de-serializing the given [Properties](/cpp-sdk/4.5/api/classes/imfusion-properties).

The default implementation will do so automatically for all registered [Parameter](/cpp-sdk/4.5/api/classes/imfusion-parameter) and [SubProperty](/cpp-sdk/4.5/api/classes/imfusion-subproperty) instances.

**See also:** [configuration()](#configuration) for the inverse functionality

### configuration

```cpp
void ImFusion::SimilarityMeasureFactory::configuration (Properties * p) const
```

*override, virtual*

Serialize the current object configuration into the given [Properties](/cpp-sdk/4.5/api/classes/imfusion-properties) object.

The default implementation will do so automatically for all registered [Parameter](/cpp-sdk/4.5/api/classes/imfusion-parameter) and [SubProperty](/cpp-sdk/4.5/api/classes/imfusion-subproperty) instances.

**See also:** [configure()](#configure) for the inverse functionality

### similarityMeasureConfiguration

```cpp
void ImFusion::SimilarityMeasureFactory::similarityMeasureConfiguration (Properties * p, bool filterUnrelated = true) const
```

Retrieves the configuration for a SimilarityMeasure of the current mode.

If filterUnrelated is true, p will only contain parameters of the current SimilarityMeasure mode. Otherwise it may also contain parameters previous selected modes (which won't affect the SimilarityMeasure).

### configureSimilarityMeasure

```cpp
void ImFusion::SimilarityMeasureFactory::configureSimilarityMeasure (const Properties * p)
```

Sets the configuration for the current SimilarityMeasure mode.

The parameters are copied to the [Properties](/cpp-sdk/4.5/api/classes/imfusion-properties) instance of the factory, i.e. existing parameters will be overridden, all others stay unmodified.

### setSimilarityMeasureParam

```cpp
template<typename T> void ImFusion::SimilarityMeasureFactory::setSimilarityMeasureParam (const std::string & name, const T & value)
```

Convenient method to directly set a parameter of the SimilarityMeasure.

E.g. to set the 'patchSize' parameter of the [LC2](/cpp-sdk/4.5/api/classes/imfusion-lc2) SimilarityMeasure to 5:

```cpp
SimilarityMeasureFactory factory;
factory.setMode(SimilarityMeasureFactory::LC2);
factory.setSimilarityMeasureParam("patchSize", 5);
```
 fragment

### similarityMeasureParam

```cpp
template<typename T> bool ImFusion::SimilarityMeasureFactory::similarityMeasureParam (const std::string & name, T & value)
```

Convenient method to directly retrieve a parameter of the SimilarityMeasure.

Returns true if the parameter exists.

### generate

```cpp
SimilarityMeasureImpl * ImFusion::SimilarityMeasureFactory::generate (SharedImage * img1, SharedImage * img2)
```

Generates a new SimilarityMeasure instance.

The actual instance created depends on the current mode and device of the factory. The instance is automatically configured with the current SimilarityMeasure properties of the factory. Once generated the SimilarityMeasure instance will be completely independent of the factory, e.g. any changes to the factory's mode, device or properties will NOT affect the instance. The caller is responsible for deleting the instance.
