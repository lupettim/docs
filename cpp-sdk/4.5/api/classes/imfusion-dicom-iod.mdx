---
title: "ImFusion::Dicom::IOD"
sidebarTitle: "IOD"
description: "Base class of a Dicom Information Object Definition ( IOD )."
---

`#include <ImFusion/Dicom/DicomIOD.h>`

## Overview

[Base](/cpp-sdk/4.5/api/structs/imfusion-dicom-base) class of a [Dicom](/cpp-sdk/4.5/api/namespaces/imfusion-dicom) Information Object Definition ([IOD](/cpp-sdk/4.5/api/classes/imfusion-dicom-iod)).

An [IOD](/cpp-sdk/4.5/api/classes/imfusion-dicom-iod) bundles several modules and basically represents a single series instance. The purpose of an [IOD](/cpp-sdk/4.5/api/classes/imfusion-dicom-iod) is to interpret the values from its modules and combine them into an image. Different IODs might handle the same [Module](/cpp-sdk/4.5/api/classes/imfusion-dicom-module) differently (e.g. apply additional restrictions).

For reading, the member functions are usually (e.g. by [DicomLoader](/cpp-sdk/4.5/api/classes/imfusion-dicomloader)) called in the following order:

1. [loadFrames()](#loadframes)
2. [sortAndGroupFrames()](#sortandgroupframes) (only on the first frame)
3. [loadFrameData()](#loadframedata)
4. [loadFrameProperties()](#loadframeproperties)

For writing, the member functions are usually (e.g. by [DicomWriter](/cpp-sdk/4.5/api/classes/imfusion-dicomwriter)) called in the following order:

1. [save()](#save)
2. [savePixelData()](#savepixeldata)

**See also:** PS 3.3A [http://dicom.nema.org/medical/dicom/current/output/chtml/part03/chapter_3.html](http://dicom.nema.org/medical/dicom/current/output/chtml/part03/chapter_3.html)

## Public Member Functions

- **IOD** (DcmDataset &[dataset](#dataset), const OFString &sopClassUID) — Creates a new [IOD](/cpp-sdk/4.5/api/classes/imfusion-dicom-iod) representation using the given DcmDataset and SOP class UID.
- virtual [std::vector](http://en.cppreference.com/w/cpp/container/vector.html)&lt; [FrameDescriptor](/cpp-sdk/4.5/api/structs/imfusion-dicom-framedescriptor) &gt; [loadFrames](#loadframes) ()=0 — Loads the descriptors of all individual frames stored in this [IOD](/cpp-sdk/4.5/api/classes/imfusion-dicom-iod).
- virtual [std::vector](http://en.cppreference.com/w/cpp/container/vector.html)&lt; [FrameDescriptorSet](/cpp-sdk/4.5/api/namespaces/imfusion-dicom) &gt; [sortAndGroupFrames](#sortandgroupframes) (const [std::vector](http://en.cppreference.com/w/cpp/container/vector.html)&lt; [FrameDescriptor](/cpp-sdk/4.5/api/structs/imfusion-dicom-framedescriptor) &gt; &frames) const =0 — Sorts and groups the given frames into sets of associated frames (e.g.
- virtual void [loadFrameData](#loadframedata) (const [FrameDescriptor](/cpp-sdk/4.5/api/structs/imfusion-dicom-framedescriptor) &desc, [MemImage](/cpp-sdk/4.5/api/classes/imfusion-memimage) &image, int slice=-1)=0 — Loads the actual frame data into the given image.
- virtual void [loadFrameProperties](#loadframeproperties) (const [FrameDescriptor](/cpp-sdk/4.5/api/structs/imfusion-dicom-framedescriptor) &desc, [SharedImageSet](/cpp-sdk/4.5/api/classes/imfusion-sharedimageset) &image) — Loads any additional properties into the given image.
- virtual void [save](#save) ([SharedImageSet](/cpp-sdk/4.5/api/classes/imfusion-sharedimageset) &imageSet, int sharedImageIndex, int sliceIndex) — Stores the general image descriptor and metadata of *imageSet* in the DICOM data set.
- virtual void [savePixelData](#savepixeldata) ([SharedImageSet](/cpp-sdk/4.5/api/classes/imfusion-sharedimageset) &imageSet, int sharedImageIndex, int sliceIndex)=0 — Writes the pixel data of *imageSet* in the DICOM data set.
- virtual void [checkErrors](#checkerrors) () const — Checks modules for errors and adds them to the list.
- const T \* **searchModule** () — Returns the first module of the given type or nullptr if no such module exists.
- DcmDataset & [dataset](#dataset) () override — Returns the dataset represented by this [IOD](/cpp-sdk/4.5/api/classes/imfusion-dicom-iod).

## Public Attributes

- const OFString **sopClassUID** — DICOM SOP used.
- [std::string](http://en.cppreference.com/w/cpp/string/basic_string.html) **pathInfo** — Optional information from which file this [IOD](/cpp-sdk/4.5/api/classes/imfusion-dicom-iod) was loaded from (only use for display purposes).
- [CommonModule](/cpp-sdk/4.5/api/classes/imfusion-dicom-commonmodule) **common**
- [ImagePixel](/cpp-sdk/4.5/api/classes/imfusion-dicom-imagepixel) **imagePixel**
- [Element](/cpp-sdk/4.5/api/classes/imfusion-dicom-element)&lt; int &gt; **numberOfFrames** — If this element exists and is larger than 1, multiple frames will be loaded regardless of the SOP Class defining it or not.

## Protected Attributes

- DcmDataset & **m_dataset**

## Member Function Documentation

### loadFrames

```cpp
virtual std::vector < FrameDescriptor > ImFusion::Dicom::IOD::loadFrames ()
```

*pure virtual*

Loads the descriptors of all individual frames stored in this [IOD](/cpp-sdk/4.5/api/classes/imfusion-dicom-iod).

Except for Enhanced Multiframe Dicoms, most IODs will return one image. Does not load any pixel data.

### sortAndGroupFrames

```cpp
virtual std::vector < FrameDescriptorSet > ImFusion::Dicom::IOD::sortAndGroupFrames (const std::vector < FrameDescriptor > & frames) const
```

*pure virtual*

Sorts and groups the given frames into sets of associated frames (e.g.

volumes).

All given frames should be somehow part of the returned frame sets. 2D images (i.e. IMAGESET) can be returned as framesets with only a single item. The exact order of the given frames is undefined. For multiframe Dicoms, this method is only called on the first frame. The [IOD](/cpp-sdk/4.5/api/classes/imfusion-dicom-iod) pointer held by each frame can be used to access special elements that are required for sorting and/or grouping.

**Note:** When overriding this method, it should be treated like a static method, i.e. no members should be accessed.

### loadFrameData

```cpp
virtual void ImFusion::Dicom::IOD::loadFrameData (const FrameDescriptor & desc, MemImage & image, int slice = -1)
```

*pure virtual*

Loads the actual frame data into the given image.

The frameNumber in the given [FrameDescriptor](/cpp-sdk/4.5/api/structs/imfusion-dicom-framedescriptor) is the index of the corresponding frame as returned by loadFrames. If slice is not -1, the frame should be loaded into the given slice of the image.

### loadFrameProperties

```cpp
virtual void ImFusion::Dicom::IOD::loadFrameProperties (const FrameDescriptor & desc, SharedImageSet & image)
```

*virtual*

Loads any additional properties into the given image.

This method is called after the complete image data has been loaded. *desc* will contain valid sharedImageIndex and sliceIndex parameters indicating where this frame has been stored in the image. Implementations must not reorder, remove or resize the images of the given SharedImageSet!

The order or amount of images must not be changed inside the SharedImageSet! Also, the size of the individual images must not change. Otherwise, this would lead to inconsistent sharedImageIndex and sliceIndex values.

This method might throw an exception if the properties somehow make the image completely invalid. This should only be used as a last restort if there is really no way to use the image in any meaningful way!

**Note:** The default implementation will create an [ImageInfoDataComponent](/cpp-sdk/4.5/api/classes/imfusion-imageinfodatacomponent) instance and attach it to *image*. Thus, when overriding this method make sure to call the base class method.

### save

```cpp
virtual void ImFusion::Dicom::IOD::save (SharedImageSet & imageSet, int sharedImageIndex, int sliceIndex)
```

*virtual*

Stores the general image descriptor and metadata of *imageSet* in the DICOM data set.

*sharedImageIndex* and *sliceIndex* describe which part of the image set should be stored.

### savePixelData

```cpp
virtual void ImFusion::Dicom::IOD::savePixelData (SharedImageSet & imageSet, int sharedImageIndex, int sliceIndex)
```

*pure virtual*

Writes the pixel data of *imageSet* in the DICOM data set.

*sharedImageIndex* and *sliceIndex* describe which part of the image set should be stored.

### checkErrors

```cpp
virtual void ImFusion::Dicom::IOD::checkErrors () const
```

*virtual*

Checks modules for errors and adds them to the list.

### dataset

```cpp
DcmDataset & ImFusion::Dicom::IOD::dataset ()
```

*override, virtual*

Returns the dataset represented by this [IOD](/cpp-sdk/4.5/api/classes/imfusion-dicom-iod).

**Examples:** [DicomExtensionExample2.cpp](/cpp-sdk/4.5/api/examples/dicom-extension2-8cpp).
