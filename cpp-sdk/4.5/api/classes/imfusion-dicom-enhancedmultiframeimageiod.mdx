---
title: "ImFusion::Dicom::EnhancedMultiFrameImageIOD"
sidebarTitle: "EnhancedMultiFrameImageIOD"
description: "Class representing Enhanced CT Image IOD (PS 3.3 A.38)."
---

`#include <ImFusion/Dicom/DicomIOD.h>`

## Overview

Class representing Enhanced CT [Image](/cpp-sdk/4.5/api/structs/imfusion-dicom-image)[IOD](/cpp-sdk/4.5/api/classes/imfusion-dicom-iod) (PS 3.3 A.38).

The enhanced multiframe IODs have a different structure than "classic" single frame IODs: Only the basic image properties of the [ImagePixel](/cpp-sdk/4.5/api/classes/imfusion-dicom-imagepixel) module are stored in the top-level DcmItem. Most others are stored in Sequences that can be shared or per-frame.

**See also:** [http://dicom.nema.org/medical/dicom/current/output/chtml/part03/sect_A.38.html](http://dicom.nema.org/medical/dicom/current/output/chtml/part03/sect_A.38.html)

**Examples:** [DicomExtensionExample2.cpp](/cpp-sdk/4.5/api/examples/dicom-extension2-8cpp).

## Classes

- class [FunctionalGroupMacro](/cpp-sdk/4.5/api/classes/imfusion-dicom-enhancedmultiframeimageiod-functionalgroupmacro) — This is a simplified implementation of the function group macros.
- class [DimensionIndexSequenceItem](/cpp-sdk/4.5/api/classes/imfusion-dicom-enhancedmultiframeimageiod-dimensionindexsequenceitem)

## Public Member Functions

- **EnhancedMultiFrameImageIOD** (DcmDataset &[dataset](#dataset), const OFString &[sopClassUID](/cpp-sdk/4.5/api/classes/imfusion-dicom-iod))
- void [checkErrors](#checkerrors) () const override — Checks modules for errors and adds them to the list.
- [std::vector](http://en.cppreference.com/w/cpp/container/vector.html)&lt; [FrameDescriptor](/cpp-sdk/4.5/api/structs/imfusion-dicom-framedescriptor) &gt; [loadFrames](#loadframes) () override — Loads the descriptors of all individual frames stored in this [IOD](/cpp-sdk/4.5/api/classes/imfusion-dicom-iod).
- [std::vector](http://en.cppreference.com/w/cpp/container/vector.html)&lt; [FrameDescriptorSet](/cpp-sdk/4.5/api/namespaces/imfusion-dicom) &gt; [sortAndGroupFrames](#sortandgroupframes) (const [std::vector](http://en.cppreference.com/w/cpp/container/vector.html)&lt; [FrameDescriptor](/cpp-sdk/4.5/api/structs/imfusion-dicom-framedescriptor) &gt; &frames) const override — Sorts and groups the given frames into sets of associated frames (e.g.
- void [loadFrameData](#loadframedata) (const [FrameDescriptor](/cpp-sdk/4.5/api/structs/imfusion-dicom-framedescriptor) &desc, [MemImage](/cpp-sdk/4.5/api/classes/imfusion-memimage) &image, int slice=-1) override — Loads the actual frame data into the given image.
- void [loadFrameProperties](#loadframeproperties) (const [FrameDescriptor](/cpp-sdk/4.5/api/structs/imfusion-dicom-framedescriptor) &desc, [SharedImageSet](/cpp-sdk/4.5/api/classes/imfusion-sharedimageset) &image) override — Loads any additional properties into the given image.
- void [save](#save) ([SharedImageSet](/cpp-sdk/4.5/api/classes/imfusion-sharedimageset) &imageSet, int sharedImageIndex, int sliceIndex) override — Stores the general image descriptor and metadata of *imageSet* in the DICOM data set.
- void [savePixelData](#savepixeldata) ([SharedImageSet](/cpp-sdk/4.5/api/classes/imfusion-sharedimageset) &imageSet, int sharedImageIndex, int sliceIndex) override — Writes the pixel data of *imageSet* in the DICOM data set.
- DcmDataset & [dataset](#dataset) () override — Dataset (or sub-item of a dataset) that will be used by the Modules.

## Public Attributes

- [Sequence](/cpp-sdk/4.5/api/classes/imfusion-dicom-sequence)&lt; [FunctionalGroupMacro](/cpp-sdk/4.5/api/classes/imfusion-dicom-enhancedmultiframeimageiod-functionalgroupmacro) &gt; **sharedSequence**
- [Sequence](/cpp-sdk/4.5/api/classes/imfusion-dicom-sequence)&lt; [FunctionalGroupMacro](/cpp-sdk/4.5/api/classes/imfusion-dicom-enhancedmultiframeimageiod-functionalgroupmacro) &gt; **perFrameSequence**
- [Sequence](/cpp-sdk/4.5/api/classes/imfusion-dicom-sequence)&lt; [DimensionIndexSequenceItem](/cpp-sdk/4.5/api/classes/imfusion-dicom-enhancedmultiframeimageiod-dimensionindexsequenceitem) &gt; **dimensionIndexSequence**
- [PixelSpacingCalibration](/cpp-sdk/4.5/api/classes/imfusion-dicom-pixelspacingcalibration) **pixelSpacingCalib**
- [ModalityLUT](/cpp-sdk/4.5/api/classes/imfusion-dicom-modalitylut) **modalityLut**
- [Element](/cpp-sdk/4.5/api/classes/imfusion-dicom-element)&lt; double &gt; **sliceThickness**
- [Element](/cpp-sdk/4.5/api/classes/imfusion-dicom-element)&lt; double &gt; **spacingBetweenSlices**

## Member Function Documentation

### checkErrors

```cpp
void ImFusion::Dicom::EnhancedMultiFrameImageIOD::checkErrors () const
```

*override, virtual*

Checks modules for errors and adds them to the list.

### loadFrames

```cpp
std::vector < FrameDescriptor > ImFusion::Dicom::EnhancedMultiFrameImageIOD::loadFrames ()
```

*override, virtual*

Loads the descriptors of all individual frames stored in this [IOD](/cpp-sdk/4.5/api/classes/imfusion-dicom-iod).

Except for Enhanced Multiframe Dicoms, most IODs will return one image. Does not load any pixel data.

### sortAndGroupFrames

```cpp
std::vector < FrameDescriptorSet > ImFusion::Dicom::EnhancedMultiFrameImageIOD::sortAndGroupFrames (const std::vector < FrameDescriptor > & frames) const
```

*override, virtual*

Sorts and groups the given frames into sets of associated frames (e.g.

volumes).

All given frames should be somehow part of the returned frame sets. 2D images (i.e. IMAGESET) can be returned as framesets with only a single item. The exact order of the given frames is undefined. For multiframe Dicoms, this method is only called on the first frame. The [IOD](/cpp-sdk/4.5/api/classes/imfusion-dicom-iod) pointer held by each frame can be used to access special elements that are required for sorting and/or grouping.

**Note:** When overriding this method, it should be treated like a static method, i.e. no members should be accessed.

### loadFrameData

```cpp
void ImFusion::Dicom::EnhancedMultiFrameImageIOD::loadFrameData (const FrameDescriptor & desc, MemImage & image, int slice = -1)
```

*override, virtual*

Loads the actual frame data into the given image.

The frameNumber in the given [FrameDescriptor](/cpp-sdk/4.5/api/structs/imfusion-dicom-framedescriptor) is the index of the corresponding frame as returned by loadFrames. If slice is not -1, the frame should be loaded into the given slice of the image.

### loadFrameProperties

```cpp
void ImFusion::Dicom::EnhancedMultiFrameImageIOD::loadFrameProperties (const FrameDescriptor & desc, SharedImageSet & image)
```

*override, virtual*

Loads any additional properties into the given image.

This method is called after the complete image data has been loaded. *desc* will contain valid sharedImageIndex and sliceIndex parameters indicating where this frame has been stored in the image. Implementations must not reorder, remove or resize the images of the given SharedImageSet!

The order or amount of images must not be changed inside the SharedImageSet! Also, the size of the individual images must not change. Otherwise, this would lead to inconsistent sharedImageIndex and sliceIndex values.

This method might throw an exception if the properties somehow make the image completely invalid. This should only be used as a last restort if there is really no way to use the image in any meaningful way!

**Note:** The default implementation will create an [ImageInfoDataComponent](/cpp-sdk/4.5/api/classes/imfusion-imageinfodatacomponent) instance and attach it to *image*. Thus, when overriding this method make sure to call the base class method.

### save

```cpp
void ImFusion::Dicom::EnhancedMultiFrameImageIOD::save (SharedImageSet & imageSet, int sharedImageIndex, int sliceIndex)
```

*override, virtual*

Stores the general image descriptor and metadata of *imageSet* in the DICOM data set.

*sharedImageIndex* and *sliceIndex* describe which part of the image set should be stored.

### savePixelData

```cpp
void ImFusion::Dicom::EnhancedMultiFrameImageIOD::savePixelData (SharedImageSet & imageSet, int sharedImageIndex, int sliceIndex)
```

*override, virtual*

Writes the pixel data of *imageSet* in the DICOM data set.

*sharedImageIndex* and *sliceIndex* describe which part of the image set should be stored.

### dataset

```cpp
DcmDataset & ImFusion::Dicom::EnhancedMultiFrameImageIOD::dataset ()
```

*override, virtual*

Dataset (or sub-item of a dataset) that will be used by the Modules.
