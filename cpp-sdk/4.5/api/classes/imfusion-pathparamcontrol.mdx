---
title: "ImFusion::PathParamControl"
sidebarTitle: "PathParamControl"
description: "Represent a ParamType::Path with a QLineEdit and a QToolButton that opens a file dialog."
---

`#include <ImFusion/GUI/PropertiesWidgetParamControls.h>`

## Overview

Represent a ParamType::Path with a QLineEdit and a QToolButton that opens a file dialog.

## Public Slots

- void **openFileDialog** ()

## Public Member Functions

- **PathParamControl** (const [std::string](http://en.cppreference.com/w/cpp/string/basic_string.html) &name, QWidget \*init=nullptr) — Finds the first QLineEdit and the first QAbstractButton from QWidget and calls the second constructor.
- **PathParamControl** (const [std::string](http://en.cppreference.com/w/cpp/string/basic_string.html) &name, QWidget \*init, QLineEdit \*initL, QAbstractButton \*initB)
- void [updateValue](#updatevalue) (const [std::string](http://en.cppreference.com/w/cpp/string/basic_string.html) &value, const [std::vector](http://en.cppreference.com/w/cpp/container/vector.html)&lt; Properties::Attribute &gt; &) override — Updates the current value and adjusts the attributes.
- void [retrieveValue](#retrievevalue) ([Properties](/cpp-sdk/4.5/api/classes/imfusion-properties) \*p) const override — Retrieves the value from the widget and writes it into the properties.
- QWidget \* [widget](#widget) () const override — Returns the widget for this control.

## Member Function Documentation

### updateValue

```cpp
void ImFusion::PathParamControl::updateValue (const std::string & value, const std::vector < Properties::Attribute > & attributes)
```

*override, virtual*

Updates the current value and adjusts the attributes.

### retrieveValue

```cpp
void ImFusion::PathParamControl::retrieveValue (Properties * p) const
```

*override, virtual*

Retrieves the value from the widget and writes it into the properties.

### widget

```cpp
QWidget * ImFusion::PathParamControl::widget () const
```

*override, virtual*

Returns the widget for this control.

The [ParamControl](/cpp-sdk/4.5/api/classes/imfusion-paramcontrol) does not own this widget, e.g. it must be re-parented to some other QWidget or deleted manually.
