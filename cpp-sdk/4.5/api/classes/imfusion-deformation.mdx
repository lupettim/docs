---
title: "ImFusion::Deformation"
sidebarTitle: "Deformation"
description: "Base class for different kinds of non-linear image transformations."
---

`#include <ImFusion/GL/Deformation.h>`

## Overview

[Base](/cpp-sdk/4.5/api/structs/imfusion-dicom-base) class for different kinds of non-linear image transformations.

The transformations are defined inversely, i.e. they do not describe how a point has to be displaced, but where a point originates from. If x is a point in world coordinates and g(x) the same point after applying the deformation, the displacement disp describes:

```cpp
x = g(x) + disp(g(x))
```
 fragment
and NOT:

```cpp
g(x) = x + disp(x)
```
 fragment
This means, for each point in world coordinates (g(x)) one can directly determine where this point originates from (x). However, one can not directly tell where a point x will be displaced to because non-linear deformations are not always reversible.. While this may seem is very useless to transform individual points, it is very helpful when deforming images. \* When deforming an image, one can iterate the pixels of the resulting deformed image and look-up the values in the original, undeformed image. To get the deformed position of an individual point, use the displacePoint method.

Derived classes should provide a CPU and OpenGL implementation. A generic OpenGL interface is provided by a GlAbstractInclude. Similar as with [GlMask](/cpp-sdk/4.5/api/classes/imfusion-glmask), you can use several different deformation fields in the same shader by using the helper class DeformationMultiInclude.

In order to work with DeformationMultiInclude, derived classes need to comply with the following requirements:

- The GlAbstractInclude define name is "IMAGE_DEFORM"
- Every uniform and every function needs to be prefixed with "deform"
- The GLSL header needs to provide at least the following function:
  ```cpp
  vec3 deformDisplacement(vec3 tc);
  ```
   fragment  This method should take image texture coordinates as input and return the displacement again in texture coordinates.
- When implementing [Deformation::setIncludeArguments()](#setincludearguments), make sure to use the given prefix when accessing the shader uniforms, i.e.
  ```cpp
  setArgument((prefix + "OutsideValue").c_str, ...)
  ```
   fragment  instead of
  ```cpp
  setArgument("deformOutsideValue", ...)
  ```
   fragment

**See also:** DeformationMultiInclude

## Public Member Functions

- **Deformation** (const [std::string](http://en.cppreference.com/w/cpp/string/basic_string.html) &shaderPath) — Initializes the [Deformation](/cpp-sdk/4.5/api/classes/imfusion-deformation) with path to the GlInclude file.
- virtual Deformation \* [clone](#clone) () const =0 — Returns a copy of the deformation.
- virtual vec3 [displacement](#displacement-1-2) (const vec3 &pos) const =0 — Returns the displacement for pos (in world coordinates).
- virtual vec3 [displacement](#displacement-2-2) (const vec2 &pos) const — Returns the displacement at the world position pos.
- virtual vec3 [displacePoint](#displacepoint) (const vec3 &point) const — Returns the displaced position of the given point in world-coordinates.
- virtual bool [isIdentity](#isidentity) () const =0 — Returns if the current state of the deformation transformations the image or not.
- virtual void [setIdentity](#setidentity) ()=0 — Sets all displacements to 0.
- int [setIncludeArguments](#setincludearguments) ([GL::Program](/cpp-sdk/4.5/api/classes/imfusion-gl-program) &p, const [SharedImage](/cpp-sdk/4.5/api/classes/imfusion-sharedimage) \*img=nullptr, const [std::string](http://en.cppreference.com/w/cpp/string/basic_string.html) &prefix="deform") const override=0 — Override default arguments for [GlMask](/cpp-sdk/4.5/api/classes/imfusion-glmask) includes.
- void **setDisabled** (bool flag) — Set a disabled flag in order to hide the visualization of this deformation.
- bool **disabled** () const — Return whether the deformation was set to disabled.
- virtual int [dimension](#dimension) () const =0 — Returns the dimension of the deformation.
- virtual [std::string](http://en.cppreference.com/w/cpp/string/basic_string.html) [id](#id) () const =0 — Returns a unique string identifier for this type of deformation.
- virtual [std::unique_ptr](http://en.cppreference.com/w/cpp/memory/unique_ptr.html)&lt; [SharedImageSet](/cpp-sdk/4.5/api/classes/imfusion-sharedimageset) &gt; [supportingImages](#supportingimages) () const — Returns the images that, together with the configuration, defines the deformation.

## Static Public Member Functions

- static [std::string](http://en.cppreference.com/w/cpp/string/basic_string.html) [defineName](#definename) () — Returns the define name of GlAbstractInclude for all deformations.
- static [std::string](http://en.cppreference.com/w/cpp/string/basic_string.html) **defineSuffix** () — Returns "DEFORM".
- static [std::string](http://en.cppreference.com/w/cpp/string/basic_string.html) **multiIncludeDefine** (const [std::string](http://en.cppreference.com/w/cpp/string/basic_string.html) &replacementToken) — Returns "IMAGE_" + toUpper(replacementToken).

## Public Attributes

- Signal&lt; const [DeformationEvent](/cpp-sdk/4.5/api/classes/imfusion-deformationevent) & &gt; **signalChanged**

## Protected Member Functions

- void **notifyListeners** (const [DeformationEvent](/cpp-sdk/4.5/api/classes/imfusion-deformationevent) &event) const — convenience function to call signalChanged.emitSignal(event).

## Protected Attributes

- bool **m_disabled** — Disabled flag for visualization.

## Member Function Documentation

### clone

```cpp
virtual Deformation * ImFusion::Deformation::clone () const
```

*pure virtual*

Returns a copy of the deformation.

### defineName

```cpp
std::string ImFusion::Deformation::defineName ()
```

*static*

Returns the define name of GlAbstractInclude for all deformations.

Always returns IMAGE_DEFORM

### displacement

```cpp
virtual vec3 ImFusion::Deformation::displacement (const vec3 & pos) const
```

*pure virtual*

Returns the displacement for pos (in world coordinates).

The returned displacement is also in world coordinates. The displacement indicates where pos originated from before applying the deformation.

### displacement

```cpp
virtual vec3 ImFusion::Deformation::displacement (const vec2 & pos) const
```

*virtual*

Returns the displacement at the world position pos.

The base implementation just calls displacement(vec3(pos,0.0))

### displacePoint

```cpp
virtual vec3 ImFusion::Deformation::displacePoint (const vec3 & point) const
```

*virtual*

Returns the displaced position of the given point in world-coordinates.

This method will estimate the inverse displacement.

### isIdentity

```cpp
virtual bool ImFusion::Deformation::isIdentity () const
```

*pure virtual*

Returns if the current state of the deformation transformations the image or not.

### setIdentity

```cpp
virtual void ImFusion::Deformation::setIdentity ()
```

*pure virtual*

Sets all displacements to 0.

### setIncludeArguments

```cpp
int ImFusion::Deformation::setIncludeArguments (GL::Program & p, const SharedImage * img = nullptr, const std::string & prefix = "deform") const
```

*override, pure virtual*

Override default arguments for [GlMask](/cpp-sdk/4.5/api/classes/imfusion-glmask) includes.

### dimension

```cpp
virtual int ImFusion::Deformation::dimension () const
```

*pure virtual*

Returns the dimension of the deformation.

### id

```cpp
virtual std::string ImFusion::Deformation::id () const
```

*pure virtual*

Returns a unique string identifier for this type of deformation.

Usually the name of the class.

**Note:** To avoid conflicts make sure to include potential plugin names into this ID. Due to the modular architecture of the ImFusion library, uniqueness of IDs cannot be checked at compile time. However, [DeformationFactory](/cpp-sdk/4.5/api/classes/imfusion-deformationfactory) will check the uniqueness of the ID during registration.

**See also:** [DeformationFactory](/cpp-sdk/4.5/api/classes/imfusion-deformationfactory)

### supportingImages

```cpp
virtual std::unique_ptr < SharedImageSet > ImFusion::Deformation::supportingImages () const
```

*inline, virtual*

Returns the images that, together with the configuration, defines the deformation.

**See also:** [DeformationFactory](/cpp-sdk/4.5/api/classes/imfusion-deformationfactory)
