---
title: "ImFusion::Seg::SegmentedBlob"
sidebarTitle: "SegmentedBlob"
---

## Public Member Functions

- **SegmentedBlob** (const [SharedImage](/cpp-sdk/4.5/api/classes/imfusion-sharedimage) \*segmentation, unsigned char label=1)
- **SegmentedBlob** (const [std::vector](http://en.cppreference.com/w/cpp/container/vector.html)&lt; vec3 &gt; &contour)
- [std::pair](http://en.cppreference.com/w/cpp/utility/pair.html)&lt; vec3, vec3 &gt; [markers](#markers) (int id, double position) const override — Compute a pair of measuring markers at the specified position.
- [std::vector](http://en.cppreference.com/w/cpp/container/vector.html)&lt; vec3 &gt; [contour](#contour) (int id) const override
- bool [isOk](#isok) () const override

## Protected Member Functions

- void **constructFromContour** (const [std::vector](http://en.cppreference.com/w/cpp/container/vector.html)&lt; vec3 &gt; &contour)

## Protected Attributes

- bool **m_isOk** = false
- [std::vector](http://en.cppreference.com/w/cpp/container/vector.html)&lt; vec3 &gt; **m_contour**
- vec3 **m_positionScale**
- vec3 **m_positionBias**

## Member Function Documentation

### markers

```cpp
std::pair < vec3, vec3 > ImFusion::Seg::SegmentedBlob::markers (int id, double position) const
```

*override, virtual*

Compute a pair of measuring markers at the specified position.

**Parameters:**

- `id` — integer in [0, numMarkers()) that identifies a marker
- `position` — value in [-1, 1] that gives the position of the marker on the axis of the reference shape

**Returns:** pair of points that lie on the contour of the segmentation

### contour

```cpp
std::vector < vec3 > ImFusion::Seg::SegmentedBlob::contour (int id) const
```

*override, virtual*

### isOk

```cpp
bool ImFusion::Seg::SegmentedBlob::isOk () const
```

*override, virtual*
