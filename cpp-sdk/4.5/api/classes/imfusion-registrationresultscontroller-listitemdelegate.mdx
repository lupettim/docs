---
title: "ImFusion::RegistrationResultsController::ListItemDelegate"
sidebarTitle: "ListItemDelegate"
description: "Custom Item Delegate that paints an item in the results list If data(Qt::UserRole + 1).toBool() is true it adds a 'GT' symbol next to the result."
---

`#include <ImFusion/Reg/RegistrationResultsController.h>`

## Overview

Custom Item Delegate that paints an item in the results list If data(Qt::UserRole + 1).toBool() is true it adds a "GT" symbol next to the result.

## Public Member Functions

- **ListItemDelegate** (QObject \*parent=nullptr)
- void **paint** (QPainter \*painter, const QStyleOptionViewItem &option, const QModelIndex &index) const override
- QSize **sizeHint** (const QStyleOptionViewItem &option, const QModelIndex &index) const override
