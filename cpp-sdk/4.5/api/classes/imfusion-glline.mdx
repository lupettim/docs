---
title: "ImFusion::GlLine"
sidebarTitle: "GlLine"
description: "Line segment annotation."
---

`#include <ImFusion/GL/GlLine.h>`

## Overview

Line segment annotation.

## Public Types

- enum class **LengthUnit** &#123; **AUTO** = 0 , **NM** = 1 , **UM** = 2 , **MM** = 3 , **CM** = 4 , **M** = 5 , **KM** = 6 &#125; — Enumeration to determine rendering order of GlObjects in [GlSliceView](/cpp-sdk/4.5/api/classes/imfusion-glsliceview) and [GlVolumeView](/cpp-sdk/4.5/api/classes/imfusion-glvolumeview).

## Public Member Functions

- void [draw](#draw) (const [GlView](/cpp-sdk/4.5/api/classes/imfusion-glview) &[view](/cpp-sdk/4.5/api/classes/imfusion-glannotation)) override — Draw the object in 3D space.
- void **setCenterLabel** (bool v)
- bool **centerLabel** () const
- void **setUseLengthAsLabel** (bool value)
- bool **useLengthAsLabel** () const
- void **setColorGradient** (const vec3 &color1, const vec3 &color2)
- void **colorGradient** (vec3 &color1, vec3 &color2) const
- void **setUseColorGradient** (bool use)
- bool **useColorGradient** () const
- unsigned short [stipplePattern](#stipplepattern) () const — Returns the stipple pattern for stippled line rendering.
- void **setStipplePattern** (unsigned short value) — Sets the stipple pattern for stippled line rendering.
- double [stippleFactor](#stipplefactor) () const — Returns the length of each individual repetition of the stipple pattern.
- void **setStippleFactor** (double value) — Sets the length of each individual repetition of the stipple pattern.
- LengthUnit **lengthUnit** () const — Returns the unit of the line segment length.
- void **setLengthUnit** (LengthUnit unit) — Sets the unit of the line segment length.
- void [configure](#configure) (const [Properties](/cpp-sdk/4.5/api/classes/imfusion-properties) \*p) override — Set one or multiple properties.
- void [configuration](#configuration) ([Properties](/cpp-sdk/4.5/api/classes/imfusion-properties) \*p) const override — Retrieve the properties of this object.
- [std::string](http://en.cppreference.com/w/cpp/string/basic_string.html) [typeName](#typename) () const override — Return unique identifier for this object class used for serialization.

## Protected Member Functions

- void [updatePoints](#updatepoints) () override — Update the internal point structure.

## Member Function Documentation

### draw

```cpp
void ImFusion::GlLine::draw (const GlView & view)
```

*override, virtual*

Draw the object in 3D space.

### stipplePattern

```cpp
unsigned short ImFusion::GlLine::stipplePattern () const
```

*inline*

Returns the stipple pattern for stippled line rendering.

**See also:** GlFixedFunctionPipeline::stipplePattern()

### stippleFactor

```cpp
double ImFusion::GlLine::stippleFactor () const
```

*inline*

Returns the length of each individual repetition of the stipple pattern.

**See also:** GlFixedFunctionPipeline::stippleFactor()

### configure

```cpp
void ImFusion::GlLine::configure (const Properties * p)
```

*override, virtual*

Set one or multiple properties.

### configuration

```cpp
void ImFusion::GlLine::configuration (Properties * p) const
```

*override, virtual*

Retrieve the properties of this object.

### typeName

```cpp
std::string ImFusion::GlLine::typeName () const
```

*inline, override, virtual*

Return unique identifier for this object class used for serialization.

### updatePoints

```cpp
void ImFusion::GlLine::updatePoints ()
```

*override, protected, virtual*

Update the internal point structure.

Should be implemented by subtypes which have custom data depending on the point data. Derived implementation needs to call base class implementation.
