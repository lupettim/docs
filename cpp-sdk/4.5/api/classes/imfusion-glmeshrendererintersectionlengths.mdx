---
title: "ImFusion::GlMeshRendererIntersectionLengths"
sidebarTitle: "GlMeshRendererIntersectionLengths"
description: "Renders the intersection lengths through a mesh in a 3D view."
---

`#include <ImFusion/Mesh/Rendering/GlMeshRendererIntersectionLengths.h>`

## Overview

Renders the intersection lengths through a mesh in a 3D view.

This class does not implement the [GlObject](/cpp-sdk/4.5/api/classes/imfusion-globject) interface by design as it is meant to serve as a reusable component to be integrated into high-level classes.

## Classes

- struct [DisplayOptions](/cpp-sdk/4.5/api/structs/imfusion-glmeshrendererintersectionlengths-displayoptions) — Complete record of rendering options for [GlMeshRendererIntersectionLengths](/cpp-sdk/4.5/api/classes/imfusion-glmeshrendererintersectionlengths).

## Public Member Functions

- void [render](#render) (const [MeshGl](/cpp-sdk/4.5/api/classes/imfusion-meshgl) &mesh, const [DisplayOptions](/cpp-sdk/4.5/api/structs/imfusion-glmeshrendererintersectionlengths-displayoptions) &options, const [GL::ViewState](/cpp-sdk/4.5/api/classes/imfusion-gl-viewstate) &viewState) — Renders the intersection lengths through *mesh* for a *viewState* into the currently active OpenGL context.

## Member Function Documentation

### render

```cpp
void ImFusion::GlMeshRendererIntersectionLengths::render (const MeshGl & mesh, const DisplayOptions & options, const GL::ViewState & viewState)
```

Renders the intersection lengths through *mesh* for a *viewState* into the currently active OpenGL context.

**Parameters:**

- `mesh` — The mesh to render.
- `options` — The rendering options to use.
- `viewState` — The [GL::ViewState](/cpp-sdk/4.5/api/classes/imfusion-gl-viewstate) to use.
