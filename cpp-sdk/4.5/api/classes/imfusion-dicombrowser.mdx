---
title: "ImFusion::DicomBrowser"
sidebarTitle: "DicomBrowser"
description: "The DicomBrowser is a dialog to browse and preview DICOM files."
---

`#include <ImFusion/Dicom/GUI/DicomBrowser.h>`

## Overview

The [DicomBrowser](/cpp-sdk/4.5/api/classes/imfusion-dicombrowser) is a dialog to browse and preview DICOM files.

The [DicomBrowser](/cpp-sdk/4.5/api/classes/imfusion-dicombrowser) shows a hierarchical list of patients, studies, series and images. The DICOM data can be retrieved from folders or DICOMDIR files. The user can select one or multiple items with check boxes. Internally a [DicomBrowserModel](/cpp-sdk/4.5/api/classes/imfusion-dicombrowsermodel) provides all data.

Previews of series and single images are automatically generated when selecting a corresponding item. Previews are generated by dcmtk's DicomImage which does not require OpenGL. For single images, all DICOM tags can be displayed in a separate tab.

## Public Slots

- void **scan** () — Scans the directory currently entered in the 'Directory' tab.
- void **scanDicomDir** () — Scans the DICOMDIR path currently entered in the 'DICOMDIR' tab.
- void **scanWeb** (bool previousAuthInvalid=false) — Scans a DICOMweb server currently entered in the 'DICOMweb' tab.
- void **cancelScan** () — Cancels any scanning.
- void **updateOkButton** () — Enables/Disables the ok button depending if anything is selected.
- void **updateTags** () — Loads all tags of the current selected image into the tags model (clears previous tags).
- void **updateStats** () — Updates the number of patients, studies and series summary below the filter controls.
- void [loadSeriesIntoMemory](#loadseriesintomemory) (const QModelIndex &index) — If the index is a [Series](/cpp-sdk/4.5/api/structs/imfusion-dicom-series), all its images will be loaded into memory in the background.
- void **openContextMenu** (const QPoint &location) — Opens the context menu at the given (global) location.
- void [openConfigDialog](#openconfigdialog) () — Opens the config dialog if one is set.
- void **selectAll** () — Selects all currently visible (i.e. filtered) series.
- void **selectNone** () — Deselects all currently visible (i.e. filtered) series.
- void **connectToPacs** () — Connect to the PACS server and scan for data given the filter criteria in the 'PACS' tab.
- void **updateAvailablePacsServers** () — Updates the combobox containing available PACS servers from [Settings](/cpp-sdk/4.5/api/classes/imfusion-settings).

## Signals

- void **updateTagsFromDataset** ([std::shared_ptr](http://en.cppreference.com/w/cpp/memory/shared_ptr.html)&lt; DcmDataset &gt; dataset)
- void **dicomScanError** (QString title, QString message) — Emitted when there was an error during scanning.

## Public Member Functions

- [DicomBrowser](#dicombrowser) (Dicom::SharedDatasetCache cache=nullptr, QWidget \*parent=nullptr, [std::unique_ptr](http://en.cppreference.com/w/cpp/memory/unique_ptr.html)&lt; [DicomBrowserFilterModel](/cpp-sdk/4.5/api/classes/imfusion-dicombrowserfiltermodel) &gt; filterModel=nullptr) — Constructor.
- void **setBrowserModel** ([std::unique_ptr](http://en.cppreference.com/w/cpp/memory/unique_ptr.html)&lt; [DicomBrowserModel](/cpp-sdk/4.5/api/classes/imfusion-dicombrowsermodel) &gt; existingBrowserModel) — Replaces the current model of the [DicomBrowser](/cpp-sdk/4.5/api/classes/imfusion-dicombrowser) with the given one.
- void **setDatasetCache** (Dicom::SharedDatasetCache cache) — Replaces the current [DatasetCache](/cpp-sdk/4.5/api/classes/imfusion-dicom-datasetcache) with the given one. nullptr disables caching.
- void [setConfigDialog](#setconfigdialog) (QDialog \*dialog) — Set an option dialog that shows configuraton options for loading the files.
- void **setCurrentPath** (const QString &path) — Set the text edit to a new path.
- QString **currentPath** () const — Returns the current path in the text edit.
- [std::string](http://en.cppreference.com/w/cpp/string/basic_string.html) **selectedSeriesUri** () const — Returns a combined URI describing all currently selected series (see DicomIOAlgorithm for format description).
- const [DicomBrowserModel](/cpp-sdk/4.5/api/classes/imfusion-dicombrowsermodel) & **browserModel** () const — Returns the current browser model.
- [DicomBrowserModel](/cpp-sdk/4.5/api/classes/imfusion-dicombrowsermodel) & **browserModel** ()
- const [DicomBrowserFilterModel](/cpp-sdk/4.5/api/classes/imfusion-dicombrowserfiltermodel) & **browserFilterModel** () const — Returns the filter model that wraps browserModel().
- [DicomBrowserFilterModel](/cpp-sdk/4.5/api/classes/imfusion-dicombrowserfiltermodel) & **browserFilterModel** ()
- void [fixTabWidget](#fixtabwidget) (bool fix, int tabIndex, const [std::string](http://en.cppreference.com/w/cpp/string/basic_string.html) &path) — If *fix* is true, selects the tab widget with the given index, and disables the tab widget.
- bool **event** (QEvent \*event) override

## Constructor & Destructor Documentation

### DicomBrowser

```cpp
ImFusion::DicomBrowser::DicomBrowser (Dicom::SharedDatasetCache cache = nullptr, QWidget * parent = nullptr, std::unique_ptr < DicomBrowserFilterModel > filterModel = nullptr)
```

*explicit*

Constructor.

If parent is an instance of [MainWindowBase](/cpp-sdk/4.5/api/classes/imfusion-mainwindowbase), then additional connections are created that otherwise will not be made. If filterModel is nullptr, a default filter model will be created.

## Member Function Documentation

### setConfigDialog

```cpp
void ImFusion::DicomBrowser::setConfigDialog (QDialog * dialog)
```

Set an option dialog that shows configuraton options for loading the files.

### fixTabWidget

```cpp
void ImFusion::DicomBrowser::fixTabWidget (bool fix, int tabIndex, const std::string & path)
```

If *fix* is true, selects the tab widget with the given index, and disables the tab widget.

If *path* is not empty, sets corresponding the url widget to the given path Convenience method for DicomIOController.

### loadSeriesIntoMemory

```cpp
void ImFusion::DicomBrowser::loadSeriesIntoMemory (const QModelIndex & index)
```

*slot*

If the index is a [Series](/cpp-sdk/4.5/api/structs/imfusion-dicom-series), all its images will be loaded into memory in the background.

If the index is a [Study](/cpp-sdk/4.5/api/structs/imfusion-dicom-study) or [Patient](/cpp-sdk/4.5/api/structs/imfusion-dicom-patient), load all images which parent series is checked,

### openConfigDialog

```cpp
void ImFusion::DicomBrowser::openConfigDialog ()
```

*slot*

Opens the config dialog if one is set.
