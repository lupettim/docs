---
title: "ImFusion::CustomVertexProperty< T >"
sidebarTitle: "CustomVertexProperty< T >"
description: "A custom property using vertices as geometry elements."
---

`#include <ImFusion/Mesh/MeshCustomProperties.h>`

## Overview

template&lt;typename T&gt;

class ImFusion::CustomVertexProperty&lt; T &gt;
A custom property using vertices as geometry elements.

## Public Member Functions

- void [clone](#clone) (Mesh &otherMesh) const override — Creates a property of the same type and storage name on another mesh, does not (and indeed cannot possibly) copy any data.

## Member Function Documentation

### clone

```cpp
template<typename T> void ImFusion::CustomVertexProperty < T >::clone (Mesh & meshToCloneTo) const
```

*override, virtual*

Creates a property of the same type and storage name on another mesh, does not (and indeed cannot possibly) copy any data.
