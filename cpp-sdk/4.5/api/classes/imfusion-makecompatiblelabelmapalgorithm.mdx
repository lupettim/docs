---
title: "ImFusion::MakeCompatibleLabelMapAlgorithm"
sidebarTitle: "MakeCompatibleLabelMapAlgorithm"
description: "Algorithm to create a label map compatible with an image."
---

`#include <ImFusion/Seg/MakeCompatibleLabelMapAlgorithm.h>`

## Overview

[Algorithm](/cpp-sdk/4.5/api/classes/imfusion-algorithm) to create a label map compatible with an image.

A label map that is only partially compatible (for example, invalid matrix or spacing) can also be passed to ensure that it is fully compatibility.

## Public Member Functions

- **MakeCompatibleLabelMapAlgorithm** ([SharedImageSet](/cpp-sdk/4.5/api/classes/imfusion-sharedimageset) &img, [SharedImageSet](/cpp-sdk/4.5/api/classes/imfusion-sharedimageset) \*labelMap=nullptr)
- void [compute](#compute) () override — Execute the algorithm.
- [OwningDataList](/cpp-sdk/4.5/api/classes/imfusion-owningdatalist) [takeOutput](#takeoutput) () override — Return any new [Data](/cpp-sdk/4.5/api/classes/imfusion-data) that was created by the [Algorithm](/cpp-sdk/4.5/api/classes/imfusion-algorithm) during the last call to [compute()](#compute).

## Static Public Member Functions

- static bool **createCompatible** (const [DataList](/cpp-sdk/4.5/api/classes/imfusion-datalist) &data, [Algorithm](/cpp-sdk/4.5/api/classes/imfusion-algorithm) \*\*a=0)

## Member Function Documentation

### compute

```cpp
void ImFusion::MakeCompatibleLabelMapAlgorithm::compute ()
```

*override, virtual*

Execute the algorithm.

### takeOutput

```cpp
OwningDataList ImFusion::MakeCompatibleLabelMapAlgorithm::takeOutput ()
```

*override, virtual*

Return any new [Data](/cpp-sdk/4.5/api/classes/imfusion-data) that was created by the [Algorithm](/cpp-sdk/4.5/api/classes/imfusion-algorithm) during the last call to [compute()](#compute).

The default implementation will return an empty list.

**Note:** Since ownership of the data is transferred, you can call this method only once between calls to [compute()](#compute).
