---
title: "ImFusion::Dicom::SegmentationStorageIOD"
sidebarTitle: "SegmentationStorageIOD"
description: "Class representing Segmentation IOD ."
---

`#include <ImFusion/Dicom/DicomIOD_SegmentationStorage.h>`

## Overview

Class representing Segmentation [IOD](/cpp-sdk/4.5/api/classes/imfusion-dicom-iod).

This is basically an [EnhancedMultiFrameImageIOD](/cpp-sdk/4.5/api/classes/imfusion-dicom-enhancedmultiframeimageiod) with some additional segmentation data added on top. Supports an additional, not standard-conform mode where label maps are directly stored as probability maps, instead of being split into individual bitfields for each label value.

**See also:** [https://dicom.innolitics.com/ciods/segmentation](https://dicom.innolitics.com/ciods/segmentation)

## Public Member Functions

- **SegmentationStorageIOD** (DcmDataset &[dataset](/cpp-sdk/4.5/api/classes/imfusion-dicom-enhancedmultiframeimageiod), const OFString &[sopClassUID](/cpp-sdk/4.5/api/classes/imfusion-dicom-iod))
- [std::vector](http://en.cppreference.com/w/cpp/container/vector.html)&lt; [FrameDescriptorSet](/cpp-sdk/4.5/api/namespaces/imfusion-dicom) &gt; [sortAndGroupFrames](#sortandgroupframes) (const [std::vector](http://en.cppreference.com/w/cpp/container/vector.html)&lt; [FrameDescriptor](/cpp-sdk/4.5/api/structs/imfusion-dicom-framedescriptor) &gt; &frames) const override — Sorts and groups the given frames into sets of associated frames (e.g.
- void **setAutoCrop** (bool b)
- void [loadFrameData](#loadframedata) (const [FrameDescriptor](/cpp-sdk/4.5/api/structs/imfusion-dicom-framedescriptor) &desc, [MemImage](/cpp-sdk/4.5/api/classes/imfusion-memimage) &image, int slice=-1) override — Loads the actual frame data into the given image.
- void [loadFrameProperties](#loadframeproperties) (const [FrameDescriptor](/cpp-sdk/4.5/api/structs/imfusion-dicom-framedescriptor) &desc, [SharedImageSet](/cpp-sdk/4.5/api/classes/imfusion-sharedimageset) &image) override — Loads any additional properties into the given image.

## Public Attributes

- [ConfigurableSequence](/cpp-sdk/4.5/api/classes/imfusion-dicom-configurablesequence)&lt; [SegmentItem](/cpp-sdk/4.5/api/classes/imfusion-dicom-segmentitem) &gt; **segmentSequence**
- [Element](/cpp-sdk/4.5/api/classes/imfusion-dicom-element)&lt; [std::string](http://en.cppreference.com/w/cpp/string/basic_string.html) &gt; **segmentationType**
- [Element](/cpp-sdk/4.5/api/classes/imfusion-dicom-element)&lt; [std::string](http://en.cppreference.com/w/cpp/string/basic_string.html) &gt; **segmentationFractionalType**
- [Element](/cpp-sdk/4.5/api/classes/imfusion-dicom-element)&lt; [std::string](http://en.cppreference.com/w/cpp/string/basic_string.html) &gt; **segmentsOverlap**
- [Element](/cpp-sdk/4.5/api/classes/imfusion-dicom-element)&lt; [std::string](http://en.cppreference.com/w/cpp/string/basic_string.html) &gt; **contentLabel**
- [Element](/cpp-sdk/4.5/api/classes/imfusion-dicom-element)&lt; int &gt; **maximumFractionalValue**

## Member Function Documentation

### sortAndGroupFrames

```cpp
std::vector < FrameDescriptorSet > ImFusion::Dicom::SegmentationStorageIOD::sortAndGroupFrames (const std::vector < FrameDescriptor > & frames) const
```

*override, virtual*

Sorts and groups the given frames into sets of associated frames (e.g.

volumes).

All given frames should be somehow part of the returned frame sets. 2D images (i.e. IMAGESET) can be returned as framesets with only a single item. The exact order of the given frames is undefined. For multiframe Dicoms, this method is only called on the first frame. The [IOD](/cpp-sdk/4.5/api/classes/imfusion-dicom-iod) pointer held by each frame can be used to access special elements that are required for sorting and/or grouping.

**Note:** When overriding this method, it should be treated like a static method, i.e. no members should be accessed.

### loadFrameData

```cpp
void ImFusion::Dicom::SegmentationStorageIOD::loadFrameData (const FrameDescriptor & desc, MemImage & image, int slice = -1)
```

*override, virtual*

Loads the actual frame data into the given image.

The frameNumber in the given [FrameDescriptor](/cpp-sdk/4.5/api/structs/imfusion-dicom-framedescriptor) is the index of the corresponding frame as returned by loadFrames. If slice is not -1, the frame should be loaded into the given slice of the image.

### loadFrameProperties

```cpp
void ImFusion::Dicom::SegmentationStorageIOD::loadFrameProperties (const FrameDescriptor & desc, SharedImageSet & image)
```

*override, virtual*

Loads any additional properties into the given image.

This method is called after the complete image data has been loaded. *desc* will contain valid sharedImageIndex and sliceIndex parameters indicating where this frame has been stored in the image. Implementations must not reorder, remove or resize the images of the given SharedImageSet!

The order or amount of images must not be changed inside the SharedImageSet! Also, the size of the individual images must not change. Otherwise, this would lead to inconsistent sharedImageIndex and sliceIndex values.

This method might throw an exception if the properties somehow make the image completely invalid. This should only be used as a last restort if there is really no way to use the image in any meaningful way!

**Note:** The default implementation will create an [ImageInfoDataComponent](/cpp-sdk/4.5/api/classes/imfusion-imageinfodatacomponent) instance and attach it to *image*. Thus, when overriding this method make sure to call the base class method.
