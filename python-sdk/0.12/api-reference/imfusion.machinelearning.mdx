---
title: imfusion.machinelearning
---

# `imfusion.machinelearning`

Submodules containing routines for pre- and post-processing
data to feed to a ML training framework.

## Functions

### `available_cpp_engines`

```python
def available_cpp_engines() -> list[str]
```

Returns the list of registered C++ engines available for usage in MachineLearningModel.

### `available_engines`

```python
def available_engines() -> list[str]
```

Returns the list of all registered engines available for usage in MachineLearningModel.

### `available_py_engines`

```python
def available_py_engines() -> list[str]
```

Returns the list of registered Python engines available for usage in MachineLearningModel.

### `is_semantic_segmentation_map`

```python
def is_semantic_segmentation_map(sis: imfusion.SharedImageSet) -> bool
```

Checks whether a SharedImageSet is a semantic segmentation map.

Args:
    sis: The SharedImageSet to check
Returns:
    bool: True if the image is a semantic segmentation map, False otherwise

### `is_target`

```python
def is_target(sis: imfusion.SharedImageSet) -> bool
```

Checks whether a SharedImageSet is tagged as a target.

Args:
    sis: The SharedImageSet to check
Returns:
    bool: True if the image is tagged as target, False otherwise

### `propertylist_to_data_loader_specs`

```python
def propertylist_to_data_loader_specs(properties: list[imfusion.Properties]) -> list[DataLoaderSpecs]
```

Parse a properties object into a vector of DataLoaderSpecs.

### `register_filter_func`

```python
def register_filter_func(name: str, func: typing.Callable[[DataItem], bool]) -> None
```

Register a user-defined function to be used as a Dataset filter operation.

This function registers a custom predicate function that can be used to filter
DataItems in a Dataset pipeline using the `Dataset.filter` decorator.

Args:
    name: Unique identifier for the filter function
    func: Function that takes a const DataItem pointer and returns True to keep the item, False to filter it out

### `register_map_func`

```python
def register_map_func(name: str, func: typing.Callable[[DataItem], None]) -> None
```

Register a user-defined function to be used as a Dataset map operation.

This function registers a custom transformation function that can be applied
to DataItems in a Dataset pipeline using the `Dataset.map` decorator.

Args:
    name: Unique identifier for the map function
    func: Function that takes a DataItem pointer and modifies it in-place

### `register_py_op_cls`

```python
def register_py_op_cls(cls: typing.Any) -> None
```

Register a Python Operation class with the operation factory.

This function is typically called automatically when inheriting from Operation or InvertibleOperation,
but can be called manually if needed.

Args:
    cls: The Python class to register

### `tag_as_target`

```python
def tag_as_target(sis: imfusion.SharedImageSet) -> None
```

Tags a SharedImageSet as a target (ground truth) for machine learning.

Args:
    sis: The SharedImageSet to tag as target

### `untag_as_target`

```python
def untag_as_target(sis: imfusion.SharedImageSet) -> None
```

Removes the target tag from a SharedImageSet.

Args:
    sis: The SharedImageSet to untag

## Classes

### `AbstractOperation`

```python
class AbstractOperation(Operation)
```

Deprecated alias for Operation class.

.. deprecated::
    Use `Operation` instead. This class exists only for backward compatibility.

#### Methods

##### `__init__`

```python
def __init__(self, name = '', processing_policy = ...)
```

Initialize an AbstractOperation (deprecated).

.. deprecated::
    Use `Operation` instead.

Args:
    name: Name identifier for the operation
    processing_policy: Policy determining which fields to process

### `AddCenterBoxOperation`

```python
class AddCenterBoxOperation(Operation)
```

Add an additional channel to the input image with a binary box at its center.
The purpose of that operation is to give a location information to the model.

Args:
    box_half_width: Half-width of the box in pixels.
    device: Specifies whether this Operation should run on CPU or GPU.
    seed: Specifies seeding for any randomness that might be contained in this operation.
    error_on_unexpected_behaviour: Specifies whether to throw an exception instead of warning about unexpected behavior.
    apply_to: Specifies fields in a DataItem that this Operation should process (if empty, will select suitable fields based on the current `~imfusion.machinelearning.Operation.processing_policy`)
    record_identifier: Unused for this operation as it is not invertible

#### Methods

##### `__init__`

```python
def __init__(self, *, device: ComputingDevice | None = None, apply_to: list[str] | None = None, seed: int | None = None, error_on_unexpected_behaviour: bool | None = None) -> None
```

##### `__init__`

```python
def __init__(self, box_half_width: int, *, device: ComputingDevice | None = None, apply_to: list[str] | None = None, seed: int | None = None, error_on_unexpected_behaviour: bool | None = None) -> None
```

### `AddDegradedLabelAsChannelOperation`

```python
class AddDegradedLabelAsChannelOperation(Operation)
```

Operation that adds a guidance channel from the label map information, in order to train an interactive segmentation or segmentation refinement model.

 This operation is designed for generating training data (in the context of interactive segmentation) from:
 - A label map indicating the ground truth, only available at training time
 - Points (blob coordinates) simulating clicks from the user that will be encoded as blobs in the new guidance channel

 The operation creates an additional image channel that encodes both pieces of information:
 - Spherical blobs are rendered at the specified coordinates with the given radius
 - The sign (positive/negative) of values in this channel indicates whether the location belongs to the label (label==1) or not

 **Standard mode** (`p_invert=false`, default) used in interactive scenarios where the user gives the information that one pixel should be inside/outside the output:
 - Inside blobs: +1.0 where label==1, -1.0 (or 0.0 if `p_onlyPositive=true`) where label!=1
 - Outside blobs: 0.0 everywhere

 **Inverted mode** (`p_invert=true`) used in refinement scenarios where the user wants to refine a first result at a particular location while freezing all the rest:
 - Inside blobs: 0.0 (marking the annotation location)
 - Outside blobs: +1.0 where label==1, -1.0 (or 0.0 if `p_onlyPositive=true`) where label!=1

A dilation parameter allows to dilate/erode the label map that will be used for determining the sign of the values in the guidance channel. This operation is internal and does not modify the actual label map.

Note:
    Requires exactly one label map in the input DataItem.
    Distributes blob coordinates across images for multi-image batches.

Args:
    blob_radius: Radius of each spherical blob in millimeters. Default: 5.0
    invert: Inverts the spatial behavior. False (default): blobs have signed values, background is 0. True: blobs are 0, background has signed values. Default: False
    blob_coordinates: Coordinates of blob centers in world coordinates. For batches, automatically distributed across images. Default: []
    only_positive: Controls negative value suppression. False (default): use both +1 and -1 values. True: replace negative values with 0, keeping only positive guidance. Default: False
    label_dilation: Morphologically dilate (positive values) or erode (negative values) the label map in pixels/voxels before determining signs. Zero (default) uses label as-is. Default: 0.0
    device: Specifies whether this Operation should run on CPU or GPU.
    seed: Specifies seeding for any randomness that might be contained in this operation.
    error_on_unexpected_behaviour: Specifies whether to throw an exception instead of warning about unexpected behavior.
    apply_to: Specifies fields in a DataItem that this Operation should process (if empty, will select suitable fields based on the current `~imfusion.machinelearning.Operation.processing_policy`)
    record_identifier: Unused for this operation as it is not invertible

#### Methods

##### `__init__`

```python
def __init__(self, *, device: ComputingDevice | None = None, apply_to: list[str] | None = None, seed: int | None = None, error_on_unexpected_behaviour: bool | None = None) -> None
```

##### `__init__`

```python
def __init__(self, blob_radius: float = 5.0, invert: bool = False, blob_coordinates: list[numpy.ndarray[numpy.float64[3, 1]]] = [], only_positive: bool = False, label_dilation: float = 0.0, *, device: ComputingDevice | None = None, apply_to: list[str] | None = None, seed: int | None = None, error_on_unexpected_behaviour: bool | None = None) -> None
```

### `AddPixelwisePredictionChannelOperation`

```python
class AddPixelwisePredictionChannelOperation(Operation)
```

Run an existing pixelwise model and add result to the input image as additional channels.
The prediction is automatically resampled to the input image resolution.

Args:
    config_path: path to the YAML configuration file of the pixelwise model
    device: Specifies whether this Operation should run on CPU or GPU.
    seed: Specifies seeding for any randomness that might be contained in this operation.
    error_on_unexpected_behaviour: Specifies whether to throw an exception instead of warning about unexpected behavior.
    apply_to: Specifies fields in a DataItem that this Operation should process (if empty, will select suitable fields based on the current `~imfusion.machinelearning.Operation.processing_policy`)
    record_identifier: Unused for this operation as it is not invertible

#### Methods

##### `__init__`

```python
def __init__(self, *, device: ComputingDevice | None = None, apply_to: list[str] | None = None, seed: int | None = None, error_on_unexpected_behaviour: bool | None = None) -> None
```

##### `__init__`

```python
def __init__(self, config_path: str, *, device: ComputingDevice | None = None, apply_to: list[str] | None = None, seed: int | None = None, error_on_unexpected_behaviour: bool | None = None) -> None
```

### `AddPositionChannelOperation`

```python
class AddPositionChannelOperation(Operation)
```

Add additional channels with the position of the pixels.
Execute the algorithm AddPositionAsChannelAlgorithm internally, and uses the same configuration (parameter names and values).

Args:

    device: Specifies whether this Operation should run on CPU or GPU.
    seed: Specifies seeding for any randomness that might be contained in this operation.
    error_on_unexpected_behaviour: Specifies whether to throw an exception instead of warning about unexpected behavior.
    apply_to: Specifies fields in a DataItem that this Operation should process (if empty, will select suitable fields based on the current `~imfusion.machinelearning.Operation.processing_policy`)
    record_identifier: Unused for this operation as it is not invertible

#### Methods

##### `__init__`

```python
def __init__(self, *, device: ComputingDevice | None = None, apply_to: list[str] | None = None, seed: int | None = None, error_on_unexpected_behaviour: bool | None = None) -> None
```

##### `__init__`

```python
def __init__(self, *, device: ComputingDevice | None = None, apply_to: list[str] | None = None, seed: int | None = None, error_on_unexpected_behaviour: bool | None = None) -> None
```

### `AddRandomNoiseOperation`

```python
class AddRandomNoiseOperation(Operation)
```

Apply a pixelwise random noise to the image intensities.
    | For `type == "uniform"`: noise is drawn in :math:`[-    extnormal{intensity},   extnormal{intensity}]`.
    | For `type == "gaussian`: noise is drawn from a Gaussian with zero mean and standard deviation equal to :math:`  extnormal{intensity}`.
    | For `type == "gamma"`: noise is drawn from a Gamma distribution with :math:`k =         heta =  extnormal{intensity}` (note that this noise has a mean of 1.0 so it is biased).
    | For `type == "shot"`: noise is drawn from a Gaussian with zero mean and standard deviation equal to :math:`     extnormal{intensity} * \sqrt{   extnormal{pixel_value}}`.

Args:
    type: Distribution of the noise ('uniform', 'gaussian', 'gamma', 'shot'). Default: 'uniform'
    intensity: Value related to the standard deviation of the generated noise. Default: 0.2
    probability: Value in [0.0, 1.0] indicating the probability of this operation to be performed. Default: 1.0
    device: Specifies whether this Operation should run on CPU or GPU.
    seed: Specifies seeding for any randomness that might be contained in this operation.
    error_on_unexpected_behaviour: Specifies whether to throw an exception instead of warning about unexpected behavior.
    apply_to: Specifies fields in a DataItem that this Operation should process (if empty, will select suitable fields based on the current `~imfusion.machinelearning.Operation.processing_policy`)
    record_identifier: Unused for this operation as it is not invertible

#### Methods

##### `__init__`

```python
def __init__(self, *, device: ComputingDevice | None = None, apply_to: list[str] | None = None, seed: int | None = None, error_on_unexpected_behaviour: bool | None = None) -> None
```

##### `__init__`

```python
def __init__(self, type: str = 'uniform', intensity: float = 0.2, probability: float = 1.0, *, device: ComputingDevice | None = None, apply_to: list[str] | None = None, seed: int | None = None, error_on_unexpected_behaviour: bool | None = None) -> None
```

### `AdjustShiftScaleOperation`

```python
class AdjustShiftScaleOperation(Operation)
```

Apply a shift and scale to each channel of the input image.
If shift and scale are vectors with multiple values, then for each channel c,
:math:`     extnormal{output}_c = ( extnormal{input}_c +    extnormal{shift}_c) /   extnormal{scale}_c`.
If shift and scale have a single value, then for each channel c,
:math:`     extnormal{output}_c = ( extnormal{input} +      extnormal{shift}_c) /   extnormal{scale}`.

Args:
    shift: Shift parameters as double (one value per channel, or one single value for all channels). Default: [0.0]
    scale: Scaling parameter as double (one value per channel, or one single value for all channels). Default: [1.0]
    device: Specifies whether this Operation should run on CPU or GPU.
    seed: Specifies seeding for any randomness that might be contained in this operation.
    error_on_unexpected_behaviour: Specifies whether to throw an exception instead of warning about unexpected behavior.
    apply_to: Specifies fields in a DataItem that this Operation should process (if empty, will select suitable fields based on the current `~imfusion.machinelearning.Operation.processing_policy`)
    record_identifier: Unused for this operation as it is not invertible

#### Methods

##### `__init__`

```python
def __init__(self, *, device: ComputingDevice | None = None, apply_to: list[str] | None = None, seed: int | None = None, error_on_unexpected_behaviour: bool | None = None) -> None
```

##### `__init__`

```python
def __init__(self, shift: list[float] = [0.0], scale: list[float] = [1.0], *, device: ComputingDevice | None = None, apply_to: list[str] | None = None, seed: int | None = None, error_on_unexpected_behaviour: bool | None = None) -> None
```

### `ApplyTopDownFlagOperation`

```python
class ApplyTopDownFlagOperation(Operation)
```

Flip the input image if it has a `topDown` flag set to false.

.. note::

    The topDown flag is not accessible from Python

Args:

    device: Specifies whether this Operation should run on CPU or GPU.
    seed: Specifies seeding for any randomness that might be contained in this operation.
    error_on_unexpected_behaviour: Specifies whether to throw an exception instead of warning about unexpected behavior.
    apply_to: Specifies fields in a DataItem that this Operation should process (if empty, will select suitable fields based on the current `~imfusion.machinelearning.Operation.processing_policy`)
    record_identifier: Unused for this operation as it is not invertible

#### Methods

##### `__init__`

```python
def __init__(self, *, device: ComputingDevice | None = None, apply_to: list[str] | None = None, seed: int | None = None, error_on_unexpected_behaviour: bool | None = None) -> None
```

##### `__init__`

```python
def __init__(self, *, device: ComputingDevice | None = None, apply_to: list[str] | None = None, seed: int | None = None, error_on_unexpected_behaviour: bool | None = None) -> None
```

### `ApproximateToHigherResolutionOperation`

```python
class ApproximateToHigherResolutionOperation(Operation)
```

Replicate the input image of the operation from the original reference image (in `~imfusion.ReferenceImageDataComponent`)
This operation is to be used mainly as post-processing, when a model produces a filtered image at a sub-resolution: it then tries to replicate the output from the original image so that no resolution is lost.
It consists in estimating a multiplicative scalar field between the input and the downsampled original image, upsample it and then re-apply it on the original image.

Args:
    epsilon: Used to avoid division by zero in case the original image has zero values. Default: 1.0
    device: Specifies whether this Operation should run on CPU or GPU.
    seed: Specifies seeding for any randomness that might be contained in this operation.
    error_on_unexpected_behaviour: Specifies whether to throw an exception instead of warning about unexpected behavior.
    apply_to: Specifies fields in a DataItem that this Operation should process (if empty, will select suitable fields based on the current `~imfusion.machinelearning.Operation.processing_policy`)
    record_identifier: Unused for this operation as it is not invertible

#### Methods

##### `__init__`

```python
def __init__(self, *, device: ComputingDevice | None = None, apply_to: list[str] | None = None, seed: int | None = None, error_on_unexpected_behaviour: bool | None = None) -> None
```

##### `__init__`

```python
def __init__(self, epsilon: float = 1.0, *, device: ComputingDevice | None = None, apply_to: list[str] | None = None, seed: int | None = None, error_on_unexpected_behaviour: bool | None = None) -> None
```

### `ArgMaxOperation`

```python
class ArgMaxOperation(Operation)
```

Create a label map with the indices corresponding of the input channel with the highest value.
The output of this operation is zero indexed, i.e. no matter which channels where selected the output
is always in range [0; n - 1] where n is the number of selected channels (+ 1 if background threshold
selected).

Args:
    selected_channels: List of channels to be selected for the argmax. If empty, use all channels (default).
                                     Indices are zero indexed, e.g. [0, 1, 2, 3] selects the first 4 channels.
    background_threshold: If set, the arg-max operation assumes the background is not explicitly
                                                               encoded, and is only set when all activations are below background_threshold.
                                                               The output then encodes 0 as the background. E.g. if the first 4 channels
                                                               were selected, the possible output values would be [0, 1, 2, 3, 4] with 0 for
                                                               the background and the rest for the selected channels.
    device: Specifies whether this Operation should run on CPU or GPU.
    seed: Specifies seeding for any randomness that might be contained in this operation.
    error_on_unexpected_behaviour: Specifies whether to throw an exception instead of warning about unexpected behavior.
    apply_to: Specifies fields in a DataItem that this Operation should process (if empty, will select suitable fields based on the current `~imfusion.machinelearning.Operation.processing_policy`)
    record_identifier: Unused for this operation as it is not invertible

#### Methods

##### `__init__`

```python
def __init__(self, *, device: ComputingDevice | None = None, apply_to: list[str] | None = None, seed: int | None = None, error_on_unexpected_behaviour: bool | None = None) -> None
```

##### `__init__`

```python
def __init__(self, selected_channels: list[int] = [], background_threshold: float | None = None, *, device: ComputingDevice | None = None, apply_to: list[str] | None = None, seed: int | None = None, error_on_unexpected_behaviour: bool | None = None) -> None
```

### `AxisFlipOperation`

```python
class AxisFlipOperation(Operation)
```

Flip image content along specified set of axes.

Args:
    axes: List of strings from {'x','y','z'} specifying the axes to flip. For 2D images, only 'x' and 'y' are valid.
    device: Specifies whether this Operation should run on CPU or GPU.
    seed: Specifies seeding for any randomness that might be contained in this operation.
    error_on_unexpected_behaviour: Specifies whether to throw an exception instead of warning about unexpected behavior.
    apply_to: Specifies fields in a DataItem that this Operation should process (if empty, will select suitable fields based on the current `~imfusion.machinelearning.Operation.processing_policy`)
    record_identifier: Unused for this operation as it is not invertible

#### Methods

##### `__init__`

```python
def __init__(self, *, device: ComputingDevice | None = None, apply_to: list[str] | None = None, seed: int | None = None, error_on_unexpected_behaviour: bool | None = None) -> None
```

##### `__init__`

```python
def __init__(self, axes: list[str], *, device: ComputingDevice | None = None, apply_to: list[str] | None = None, seed: int | None = None, error_on_unexpected_behaviour: bool | None = None) -> None
```

### `AxisRotationOperation`

```python
class AxisRotationOperation(Operation)
```

Rotate image around image axis with axis-specific rotation angles that are signed multiples of 90 degrees.

Args:
    axes: List of strings from {'x','y','z'} specifying the axes to rotate around. For 2D images, only ['z'] is valid.
    angles: List of integers (with same lengths as axis) specifying the rotation angles in degrees. Only +- 0/90/180/270 are valid.
    device: Specifies whether this Operation should run on CPU or GPU.
    seed: Specifies seeding for any randomness that might be contained in this operation.
    error_on_unexpected_behaviour: Specifies whether to throw an exception instead of warning about unexpected behavior.
    apply_to: Specifies fields in a DataItem that this Operation should process (if empty, will select suitable fields based on the current `~imfusion.machinelearning.Operation.processing_policy`)
    record_identifier: Unused for this operation as it is not invertible

#### Methods

##### `__init__`

```python
def __init__(self, *, device: ComputingDevice | None = None, apply_to: list[str] | None = None, seed: int | None = None, error_on_unexpected_behaviour: bool | None = None) -> None
```

##### `__init__`

```python
def __init__(self, axes: list[str], angles: list[int], *, device: ComputingDevice | None = None, apply_to: list[str] | None = None, seed: int | None = None, error_on_unexpected_behaviour: bool | None = None) -> None
```

### `BakeDeformationOperation`

```python
class BakeDeformationOperation(Operation)
```

Deform an image with its attached Deformation and store the result into the returned output image.
This operation will return a clone of the input image if it does not have any deformation attached.
The output image will not have an attached Deformation.

Args:
    adjust_size: whether the size of the output image would be automatically adjusted to fit the deformed content. Default: True

    device: Specifies whether this Operation should run on CPU or GPU.
    seed: Specifies seeding for any randomness that might be contained in this operation.
    error_on_unexpected_behaviour: Specifies whether to throw an exception instead of warning about unexpected behavior.
    apply_to: Specifies fields in a DataItem that this Operation should process (if empty, will select suitable fields based on the current `~imfusion.machinelearning.Operation.processing_policy`)
    record_identifier: Unused for this operation as it is not invertible

#### Methods

##### `__init__`

```python
def __init__(self, *, device: ComputingDevice | None = None, apply_to: list[str] | None = None, seed: int | None = None, error_on_unexpected_behaviour: bool | None = None) -> None
```

##### `__init__`

```python
def __init__(self, adjust_size: bool = True, *, device: ComputingDevice | None = None, apply_to: list[str] | None = None, seed: int | None = None, error_on_unexpected_behaviour: bool | None = None) -> None
```

### `BakePhotometricInterpretationOperation`

```python
class BakePhotometricInterpretationOperation(Operation)
```

Bake the Photometric Interpretation into the intensities of the image.
If the image has a Photometric Interpretation of MONOCHROME1, the intensities will run be inverted using:
:math:`     extnormal{output} =     extnormal{max} - (      extnormal{input} -      extnormal{min})`

Args:

    device: Specifies whether this Operation should run on CPU or GPU.
    seed: Specifies seeding for any randomness that might be contained in this operation.
    error_on_unexpected_behaviour: Specifies whether to throw an exception instead of warning about unexpected behavior.
    apply_to: Specifies fields in a DataItem that this Operation should process (if empty, will select suitable fields based on the current `~imfusion.machinelearning.Operation.processing_policy`)
    record_identifier: Unused for this operation as it is not invertible

#### Methods

##### `__init__`

```python
def __init__(self, *, device: ComputingDevice | None = None, apply_to: list[str] | None = None, seed: int | None = None, error_on_unexpected_behaviour: bool | None = None) -> None
```

##### `__init__`

```python
def __init__(self, *, device: ComputingDevice | None = None, apply_to: list[str] | None = None, seed: int | None = None, error_on_unexpected_behaviour: bool | None = None) -> None
```

### `BakeTransformationOperation`

```python
class BakeTransformationOperation(Operation)
```

Apply the rotation contained in the matrix of the input volume.
The internal memory buffer will be re-organized but the image location in world coordinate will not change.
The output matrix is guaranteed to have no rotation (but may still have a translation component).
Pixels outside the original image extent will be padded according to the padding_mode parameter.
Note: If a mask is present, this operation assumes that it is an ExplicitMask and will process it as well.

Args:
    padding_mode: defines which type of padding is used in ["zero", "clamp", "mirror"]. Default: `~imfusion.PaddingMode.ZERO`

    device: Specifies whether this Operation should run on CPU or GPU.
    seed: Specifies seeding for any randomness that might be contained in this operation.
    error_on_unexpected_behaviour: Specifies whether to throw an exception instead of warning about unexpected behavior.
    apply_to: Specifies fields in a DataItem that this Operation should process (if empty, will select suitable fields based on the current `~imfusion.machinelearning.Operation.processing_policy`)
    record_identifier: Unused for this operation as it is not invertible

#### Methods

##### `__init__`

```python
def __init__(self, *, device: ComputingDevice | None = None, apply_to: list[str] | None = None, seed: int | None = None, error_on_unexpected_behaviour: bool | None = None) -> None
```

##### `__init__`

```python
def __init__(self, padding_mode: imfusion.PaddingMode = imfusion.PaddingMode.ZERO, *, device: ComputingDevice | None = None, apply_to: list[str] | None = None, seed: int | None = None, error_on_unexpected_behaviour: bool | None = None) -> None
```

### `BlobsFromKeypointsOperation`

```python
class BlobsFromKeypointsOperation(Operation)
```

Transforms keypoints into an actual image (blob map with the same size of the image).
Requires an input image called "data" (can be overwritten with the parameter `image_field_name`) and some keypoints called "keypoints" (can be overwritten with the parameter `apply_to`).

Args:
    blob_radius: Size of the generated blobs in mm. Default: 5.0
    image_field_name: Field name of the reference image. Default: "data"
    blobs_field_name: Field name of the output blob map. Default: "label"
    label_map_mode: Generate ubyte label map instead of multi-channel gaussian blobs. Default: False
    sharp_blobs: Specifies whether to sharpen the profiles of the blob function, making its support more compact. Default: False
    blob_radius_units: The units to use when interpreting the blob_radius parameter. Default: "mm"
    device: Specifies whether this Operation should run on CPU or GPU.
    seed: Specifies seeding for any randomness that might be contained in this operation.
    error_on_unexpected_behaviour: Specifies whether to throw an exception instead of warning about unexpected behavior.
    apply_to: Specifies fields in a DataItem that this Operation should process (if empty, will select suitable fields based on the current `~imfusion.machinelearning.Operation.processing_policy`)
    record_identifier: Unused for this operation as it is not invertible

#### Methods

##### `__init__`

```python
def __init__(self, *, device: ComputingDevice | None = None, apply_to: list[str] | None = None, seed: int | None = None, error_on_unexpected_behaviour: bool | None = None) -> None
```

##### `__init__`

```python
def __init__(self, blob_radius: float = 5.0, image_field_name: str = 'data', blobs_field_name: str = 'label', label_map_mode: bool = False, sharp_blobs: bool = False, blob_radius_units: ParamUnit = ..., *, device: ComputingDevice | None = None, apply_to: list[str] | None = None, seed: int | None = None, error_on_unexpected_behaviour: bool | None = None) -> None
```

### `BoundingBoxElement`

```python
class BoundingBoxElement(DataElement)
```

DataElement for storing and processing bounding box annotations.

BoundingBoxElement wraps a BoundingBoxSet to represent 3D bounding box annotations
in ML pipelines. Bounding boxes are commonly used for object detection and
region-of-interest specifications.

#### Methods

##### `__init__`

```python
def __init__(self, boundingbox_set: BoundingBoxSet) -> None
```

Initialize a BoundingBoxElement

Args:
        boundingbox_set: In case the argument is a numpy array, the array shape is expected to be [N, C, B, 2, 3], where N is the batch size,
                C the number of different keypoint types (channel), B the number of instances of the same box type. Each Box is expected to have dimensions [2, 3].
                If the argument is a nested list, the same concept applies also to the size of each level of nesting.

##### `boxes`

*property*

```python
def boxes(self) -> BoundingBoxSet
```

Access to the underlying BoundingBoxSet.

##### `boxes`

```python
def boxes(self, arg1: BoundingBoxSet) -> None
```

### `BoundingBoxSet`

```python
class BoundingBoxSet(imfusion.Data)
```

Class for managing sets of bounding boxes

The class is meant to be used in parallel with SharedImageSet.
For each frame in the set, and for each type of bounding box (i.e. car, airplane, lung, cat),
there is a list of boxes that encompass an instance of that type in the reference image.
In terms of tensor dimensions, this would be represented as [N, C, B],
where N is the batch size, C is the number of channels (i.e. types of boxes),
and B is the number of boxes for the same instance type.
Each Box has a dimension of [2, 3], consisting of a pair of vec3 for describing center and extent.
See the Box class for more information.

Note:
    The API for this class is experimental and may change soon.

#### Methods

##### `__init__`

```python
def __init__(self, boxes: list[list[list[Box]]]) -> None
```

Initialize a BoundingBoxSet from a nested vector of Box objects.

Args:
        boxes: 3D nested vector [N, C, B] of Box objects where N=batch size, C=channels (box types), B=boxes per type

##### `__init__`

```python
def __init__(self, boxes: list[list[list[tuple[numpy.ndarray[numpy.float64[3, 1]], numpy.ndarray[numpy.float64[3, 1]]]]]]) -> None
```

Initialize a BoundingBoxSet from nested vectors of (center, extent) pairs.

Args:
        boxes: 3D nested vector [N, C, B] where each box is a pair of vec3 (center, extent)

##### `__init__`

```python
def __init__(self, boxes: list[list[list[tuple[list[float], list[float]]]]]) -> None
```

Initialize a BoundingBoxSet from nested lists of ([center], [extent]) pairs.

Args:
        boxes: 3D nested list where each box is ((center_list, extent_list)) with 3D coordinates

##### `__init__`

```python
def __init__(self, array: numpy.ndarray[numpy.float64]) -> None
```

Initialize a BoundingBoxSet from a numpy array.

Args:
        array: Numpy array with shape [N, C, B, 2, 3] where N=batch size, C=channels, B=boxes, 2=(center, extent), 3=coordinates

##### `data`

*property*

```python
def data(self) -> list[list[list[Box]]]
```

The bounding box data stored as a 5D nested vector [N, C, B, 2, 3] where N=batch size, C=channels, B=number of boxes per channel, 2=(center, extent), and 3=coordinates

##### `data`

```python
def data(self, arg0: list[list[list[Box]]]) -> None
```

##### `load`

*staticmethod*

```python
def load(location: str | os.PathLike) -> BoundingBoxSet | None
```

Load a BoundingBoxSet from an ImFusion file.

Args:
        location: input path.

##### `save`

```python
def save(self, location: str | os.PathLike) -> None
```

Save a BoundingBoxSet as an ImFusion file.

Args:
        location: output path.

### `Box`

```python
class Box
```

Bounding Box class for ML tasks.
Since bounding boxes are axis aligned by definition, a Box is represented by its center and its extent.
This representation allows for easy rotation, augmentation etc.

#### Methods

##### `__init__`

```python
def __init__(self, center: numpy.ndarray[numpy.float64[3, 1]], extent: numpy.ndarray[numpy.float64[3, 1]]) -> None
```

Initialize a Box with center and extent.

Args:
        center: 3D vector specifying the center point of the box
        extent: 3D vector specifying the size/extent of the box

##### `__init__`

```python
def __init__(self, center_and_extent: tuple[numpy.ndarray[numpy.float64[3, 1]], numpy.ndarray[numpy.float64[3, 1]]]) -> None
```

Initialize a Box from a tuple of (center, extent).

Args:
        center_and_extent: Tuple of two 3D vectors (center, extent)

##### `center`

*property*

```python
def center(self) -> numpy.ndarray[numpy.float64[3, 1]]
```

The center point of the bounding box as a 3D vector (x, y, z)

##### `center`

```python
def center(self, arg0: numpy.ndarray[numpy.float64[3, 1]]) -> None
```

##### `extent`

*property*

```python
def extent(self) -> numpy.ndarray[numpy.float64[3, 1]]
```

The extent (size) of the bounding box as a 3D vector (width, height, depth)

##### `extent`

```python
def extent(self, arg0: numpy.ndarray[numpy.float64[3, 1]]) -> None
```

### `CenterROISampler`

```python
class CenterROISampler(ImageROISampler)
```

Sampler which samples one ROI from the input image and label map with a target size.
The ROI is centered on the image center.
The arrays will be padded if the target size is larger than the input image.

Args:
    roi_size: Target size of the ROIs to be extracted as [Width, Height, Slices]
    device: Specifies whether this Operation should run on CPU or GPU.
    seed: Specifies seeding for any randomness that might be contained in this operation.
    error_on_unexpected_behaviour: Specifies whether to throw an exception instead of warning about unexpected behavior.
    apply_to: Specifies fields in a DataItem that this Operation should process (if empty, will select suitable fields based on the current `~imfusion.machinelearning.Operation.processing_policy`)
    record_identifier: Unused for this operation as it is not invertible

#### Methods

##### `__init__`

```python
def __init__(self, *, device: ComputingDevice | None = None, apply_to: list[str] | None = None, seed: int | None = None, error_on_unexpected_behaviour: bool | None = None) -> None
```

##### `__init__`

```python
def __init__(self, roi_size: numpy.ndarray[numpy.int32[3, 1]], *, device: ComputingDevice | None = None, apply_to: list[str] | None = None, seed: int | None = None, error_on_unexpected_behaviour: bool | None = None) -> None
```

### `CheckDataOperation`

```python
class CheckDataOperation(Operation)
```

Checks if all input data match a set of expected conditions.
If parameters are zero or empty, they are not checked.

Args:
    num_dimensions: Expected number of dimensions in the input. Set to 0 to skip this check. Default: 0
    num_images: Expected number of images in the input. Set to 0 to skip this check. Default: 0
    num_channels: Expected number of channels in the input. Set to 0 to skip this check. Default: 0
    data_type: Expected datatype of input. Must be one of: ["", "float", "uint8", "int8", "uint16", "int16", "uint32", "int32", "double"]. Empty string skips this check. Default: ""
    dimensions: Expected spatial dimensions [width, height, depth] of input image. Set all dimensions to 0 to skip checking it. Default: [0,0,0]
    spacing: Expected spacing [x, y, z] of input image in mm. Set all components to 0 to skip checking it. Default: [0,0,0]
    label_match_input: Whether label dimensions and channel count must match the input image. Default: False
    label_type: Expected datatype of labels. Must be one of: ["", "float", "uint8", "int8", "uint16", "int16", "uint32", "int32", "double"]. Empty string skips this check. Default: ""
    label_values: List of required label values (excluding 0). No other values are allowed. When `check_label_values_are_subset` is false, all must be present. Empty list skips this check. Default: []
    label_dimensions: Expected spatial dimensions [width, height, depth] of label image. Set all dimensions to 0 to skip checking it. Default: [0,0,0]
    label_channels: Expected number of channels in the label image. Set to 0 to skip this check. Default: 0
    check_rotation_matrix: Whether to verify the input image has no rotation matrix. Default: False
    check_deformation: Whether to verify the input image has no deformation. Default: False
    check_shift_scale: Whether to verify the input image has identity intensity transformation. Default: False
    fail_on_error: Whether to raise an exception on validation failure (True) or just log an error (False). Default: True
    save_path_on_error: Path where to save the failing input as an ImFusion file (.imf) when validation fails. Empty string disables saving. Default: ""
    check_label_values_are_subset: Whether to check if the label values are a subset of the label values provided with `label_values`, otherwise check if all values are present in the label image. Default: False
    device: Specifies whether this Operation should run on CPU or GPU.
    seed: Specifies seeding for any randomness that might be contained in this operation.
    error_on_unexpected_behaviour: Specifies whether to throw an exception instead of warning about unexpected behavior.
    apply_to: Specifies fields in a DataItem that this Operation should process (if empty, will select suitable fields based on the current `~imfusion.machinelearning.Operation.processing_policy`)
    record_identifier: Unused for this operation as it is not invertible

#### Methods

##### `__init__`

```python
def __init__(self, *, device: ComputingDevice | None = None, apply_to: list[str] | None = None, seed: int | None = None, error_on_unexpected_behaviour: bool | None = None) -> None
```

##### `__init__`

```python
def __init__(self, num_dimensions: int = 0, num_images: int = 0, num_channels: int = 0, data_type: str = '', dimensions: numpy.ndarray[numpy.int32[3, 1]] = ..., spacing: numpy.ndarray[numpy.float64[3, 1]] = ..., label_match_input: bool = False, label_type: str = '', label_values: list[int] = [], label_dimensions: numpy.ndarray[numpy.int32[3, 1]] = ..., label_channels: int = 0, check_rotation_matrix: bool = False, check_deformation: bool = False, check_shift_scale: bool = False, fail_on_error: bool = True, save_path_on_error: str = '', check_label_values_are_subset: bool = False, *, device: ComputingDevice | None = None, apply_to: list[str] | None = None, seed: int | None = None, error_on_unexpected_behaviour: bool | None = None) -> None
```

### `ClipOperation`

```python
class ClipOperation(Operation)
```

Clip the intensities to a minimum and maximum value: all intensities outside this range will be clipped to the range border.

Args:
    min: Minimum intensity of the output image. Default: 0.0
    max: Maximum intensity of the output image. Default: 1.0
    device: Specifies whether this Operation should run on CPU or GPU.
    seed: Specifies seeding for any randomness that might be contained in this operation.
    error_on_unexpected_behaviour: Specifies whether to throw an exception instead of warning about unexpected behavior.
    apply_to: Specifies fields in a DataItem that this Operation should process (if empty, will select suitable fields based on the current `~imfusion.machinelearning.Operation.processing_policy`)
    record_identifier: Unused for this operation as it is not invertible

#### Methods

##### `__init__`

```python
def __init__(self, *, device: ComputingDevice | None = None, apply_to: list[str] | None = None, seed: int | None = None, error_on_unexpected_behaviour: bool | None = None) -> None
```

##### `__init__`

```python
def __init__(self, min: float = 0.0, max: float = 1.0, *, device: ComputingDevice | None = None, apply_to: list[str] | None = None, seed: int | None = None, error_on_unexpected_behaviour: bool | None = None) -> None
```

### `ConcatenateNeighboringFramesToChannelsOperation`

```python
class ConcatenateNeighboringFramesToChannelsOperation(Operation)
```

This function iterates over each frame, augmenting the channel dimension by appending or adding information from neighboring frames from both sides.
For instance, with radius=1 concatenation, an image with dimensions (10, 1, 256, 256, 1) becomes an (10, 1, 256, 256, 3) image, meaning each frame will now include
its predecessor (channel 0), itself (channel 1), and its successor (channel 2). For multi-channel inputs, only the first channel is used for concatenation;
other channels are appended after these in the output. With reduction_mode, central and augmented frames can be reduced to a single frame to preserve the original
number of channels.

Args:
    radius: Defines the number of neighboring frames added to each side within the channel dimension. Default: 0
    reduction_mode: Determines if and how to reduce neighboring frames. Options: "none" (default, concatenates), "average", "maximum".
    same_padding: Use frame replication (not zero-padding) at sequence edges. Default: True
    device: Specifies whether this Operation should run on CPU or GPU.
    seed: Specifies seeding for any randomness that might be contained in this operation.
    error_on_unexpected_behaviour: Specifies whether to throw an exception instead of warning about unexpected behavior.
    apply_to: Specifies fields in a DataItem that this Operation should process (if empty, will select suitable fields based on the current `~imfusion.machinelearning.Operation.processing_policy`)
    record_identifier: Unused for this operation as it is not invertible

#### Methods

##### `__init__`

```python
def __init__(self, *, device: ComputingDevice | None = None, apply_to: list[str] | None = None, seed: int | None = None, error_on_unexpected_behaviour: bool | None = None) -> None
```

##### `__init__`

```python
def __init__(self, radius: int = 0, reduction_mode: str = 'none', same_padding: bool = True, *, device: ComputingDevice | None = None, apply_to: list[str] | None = None, seed: int | None = None, error_on_unexpected_behaviour: bool | None = None) -> None
```

### `ConvertSlicesToVolumeOperation`

```python
class ConvertSlicesToVolumeOperation(Operation)
```

Stacks a set of 2D images extracted along a specified axis into an actual 3D volume.

Args:
    axis: Axis along which to extract slices (must be either 'x', 'y' or 'z')
    device: Specifies whether this Operation should run on CPU or GPU.
    seed: Specifies seeding for any randomness that might be contained in this operation.
    error_on_unexpected_behaviour: Specifies whether to throw an exception instead of warning about unexpected behavior.
    apply_to: Specifies fields in a DataItem that this Operation should process (if empty, will select suitable fields based on the current `~imfusion.machinelearning.Operation.processing_policy`)
    record_identifier: Unused for this operation as it is not invertible

#### Methods

##### `__init__`

```python
def __init__(self, *, device: ComputingDevice | None = None, apply_to: list[str] | None = None, seed: int | None = None, error_on_unexpected_behaviour: bool | None = None) -> None
```

##### `__init__`

```python
def __init__(self, axis: str = 'z', *, device: ComputingDevice | None = None, apply_to: list[str] | None = None, seed: int | None = None, error_on_unexpected_behaviour: bool | None = None) -> None
```

### `ConvertToGrayOperation`

```python
class ConvertToGrayOperation(Operation)
```

Convert the input image to a single channel image by averaging all channels.

Args:

    device: Specifies whether this Operation should run on CPU or GPU.
    seed: Specifies seeding for any randomness that might be contained in this operation.
    error_on_unexpected_behaviour: Specifies whether to throw an exception instead of warning about unexpected behavior.
    apply_to: Specifies fields in a DataItem that this Operation should process (if empty, will select suitable fields based on the current `~imfusion.machinelearning.Operation.processing_policy`)
    record_identifier: Unused for this operation as it is not invertible

#### Methods

##### `__init__`

```python
def __init__(self, *, device: ComputingDevice | None = None, apply_to: list[str] | None = None, seed: int | None = None, error_on_unexpected_behaviour: bool | None = None) -> None
```

##### `__init__`

```python
def __init__(self, *, device: ComputingDevice | None = None, apply_to: list[str] | None = None, seed: int | None = None, error_on_unexpected_behaviour: bool | None = None) -> None
```

### `ConvertVolumeToSlicesOperation`

```python
class ConvertVolumeToSlicesOperation(Operation)
```

Unstacks a 3D volume to a set of 2D images extracted along one of the axes.

Args:
    axis: Axis along which to extract slices (must be either 'x', 'y' or 'z')
    device: Specifies whether this Operation should run on CPU or GPU.
    seed: Specifies seeding for any randomness that might be contained in this operation.
    error_on_unexpected_behaviour: Specifies whether to throw an exception instead of warning about unexpected behavior.
    apply_to: Specifies fields in a DataItem that this Operation should process (if empty, will select suitable fields based on the current `~imfusion.machinelearning.Operation.processing_policy`)
    record_identifier: Unused for this operation as it is not invertible

#### Methods

##### `__init__`

```python
def __init__(self, *, device: ComputingDevice | None = None, apply_to: list[str] | None = None, seed: int | None = None, error_on_unexpected_behaviour: bool | None = None) -> None
```

##### `__init__`

```python
def __init__(self, axis: str = 'z', *, device: ComputingDevice | None = None, apply_to: list[str] | None = None, seed: int | None = None, error_on_unexpected_behaviour: bool | None = None) -> None
```

### `ConvolutionalCRFOperation`

```python
class ConvolutionalCRFOperation(Operation)
```

Adapt segmentation map or raw output of model to image content.

Args:
    adaptiveness: Indicates how much the segmentation should be adapted to the image content. Range [0, 1]. Default: 0.5
    smooth_weight: Weight of the smoothness kernel. Higher values create a greater penalty for nearby pixels having different labels. Default: 0.1
    radius:  Radius of the message passing window in pixels. Default: 5
    downsampling: Amount of downsampling used in message passing, makes the effective radius of the message passing window larger. Default: 2
    device: Specifies whether this Operation should run on CPU or GPU.
    seed: Specifies seeding for any randomness that might be contained in this operation.
    error_on_unexpected_behaviour: Specifies whether to throw an exception instead of warning about unexpected behavior.
    apply_to: Specifies fields in a DataItem that this Operation should process (if empty, will select suitable fields based on the current `~imfusion.machinelearning.Operation.processing_policy`)
    record_identifier: Unused for this operation as it is not invertible

#### Methods

##### `__init__`

```python
def __init__(self, *, device: ComputingDevice | None = None, apply_to: list[str] | None = None, seed: int | None = None, error_on_unexpected_behaviour: bool | None = None) -> None
```

##### `__init__`

```python
def __init__(self, adaptiveness: float = 0.5, smooth_weight: float = 0.1, radius: int = 5, downsampling: int = 2, *, device: ComputingDevice | None = None, apply_to: list[str] | None = None, seed: int | None = None, error_on_unexpected_behaviour: bool | None = None) -> None
```

### `CopyOperation`

```python
class CopyOperation(Operation)
```

Copies a set of fields of a data item.

Args:
    source: list of the elements to be copied
    target: list of names of the new elements (must match the size of source)
    device: Specifies whether this Operation should run on CPU or GPU.
    seed: Specifies seeding for any randomness that might be contained in this operation.
    error_on_unexpected_behaviour: Specifies whether to throw an exception instead of warning about unexpected behavior.
    apply_to: Specifies fields in a DataItem that this Operation should process (if empty, will select suitable fields based on the current `~imfusion.machinelearning.Operation.processing_policy`)
    record_identifier: Unused for this operation as it is not invertible

#### Methods

##### `__init__`

```python
def __init__(self, *, device: ComputingDevice | None = None, apply_to: list[str] | None = None, seed: int | None = None, error_on_unexpected_behaviour: bool | None = None) -> None
```

##### `__init__`

```python
def __init__(self, source: list[str], target: list[str], *, device: ComputingDevice | None = None, apply_to: list[str] | None = None, seed: int | None = None, error_on_unexpected_behaviour: bool | None = None) -> None
```

### `CropAroundLabelMapOperation`

```python
class CropAroundLabelMapOperation(Operation)
```

Crops the input image and label to the bounds of the specified label value, and sets the label value to 1 and all other values to zero in the resulting label.

Args:
    label_values: Label values to select. Default: [1]
    margin: Margin, in pixels. Default: 1
    reorder: Whether label values in result should be mapped to 1,2,3... based on input in label_values. Default: False
    device: Specifies whether this Operation should run on CPU or GPU.
    seed: Specifies seeding for any randomness that might be contained in this operation.
    error_on_unexpected_behaviour: Specifies whether to throw an exception instead of warning about unexpected behavior.
    apply_to: Specifies fields in a DataItem that this Operation should process (if empty, will select suitable fields based on the current `~imfusion.machinelearning.Operation.processing_policy`)
    record_identifier: Unused for this operation as it is not invertible

#### Methods

##### `__init__`

```python
def __init__(self, *, device: ComputingDevice | None = None, apply_to: list[str] | None = None, seed: int | None = None, error_on_unexpected_behaviour: bool | None = None) -> None
```

##### `__init__`

```python
def __init__(self, label_values: list[int] = [1], margin: int = 1, reorder: bool = False, *, device: ComputingDevice | None = None, apply_to: list[str] | None = None, seed: int | None = None, error_on_unexpected_behaviour: bool | None = None) -> None
```

### `CropOperation`

```python
class CropOperation(Operation)
```

Crop input images and label maps with a given size and offset.

Args:
    size: List of integers representing the target dimensions of the image to be cropped. If -1 is specified, the whole dimension will be kept, starting from the corresponding offset.
    offset: List of integers representing the position of the lower corner of the cropped image
    device: Specifies whether this Operation should run on CPU or GPU.
    seed: Specifies seeding for any randomness that might be contained in this operation.
    error_on_unexpected_behaviour: Specifies whether to throw an exception instead of warning about unexpected behavior.
    apply_to: Specifies fields in a DataItem that this Operation should process (if empty, will select suitable fields based on the current `~imfusion.machinelearning.Operation.processing_policy`)
    record_identifier: Unused for this operation as it is not invertible

#### Methods

##### `__init__`

```python
def __init__(self, *, device: ComputingDevice | None = None, apply_to: list[str] | None = None, seed: int | None = None, error_on_unexpected_behaviour: bool | None = None) -> None
```

##### `__init__`

```python
def __init__(self, size: numpy.ndarray[numpy.int32[3, 1]], offset: numpy.ndarray[numpy.int32[3, 1]], *, device: ComputingDevice | None = None, apply_to: list[str] | None = None, seed: int | None = None, error_on_unexpected_behaviour: bool | None = None) -> None
```

### `CutOutOperation`

```python
class CutOutOperation(Operation)
```

Cut out input images and label maps with a given size, offset and fill values.

Args:
    size: List of 3-dim vectors representing the target dimensions of the image to be cut out. Default: [1, 1, 1]
    offset: List of 3-dim vectors representing the position of the lower corner of the cut out area. Default: [0, 0, 0]
    fill_value: List of intensity value (floats) for filling out cutout region. Default: [0.0]
    size_units: Units of the size parameter (ParamUnit.MM or "mm", ParamUnit.FRACTION or "fraction", ParamUnit.VOXEL or "voxel"). Default: `~imfusion.machinelearning.ParamUnit.MM`
    offset_units: Units of the offset parameter (ParamUnit.MM or "mm", ParamUnit.FRACTION or "fraction", ParamUnit.VOXEL or "voxel"). Default: `~imfusion.machinelearning.ParamUnit.VOXEL`
            
Note: `~imfusion.ParamUnit` can be automatically converted from a string. This means you can directly pass a string like "mm", "fraction", or "voxel" to the param_units parameters instead of using the enum values.
    device: Specifies whether this Operation should run on CPU or GPU.
    seed: Specifies seeding for any randomness that might be contained in this operation.
    error_on_unexpected_behaviour: Specifies whether to throw an exception instead of warning about unexpected behavior.
    apply_to: Specifies fields in a DataItem that this Operation should process (if empty, will select suitable fields based on the current `~imfusion.machinelearning.Operation.processing_policy`)
    record_identifier: Unused for this operation as it is not invertible

#### Methods

##### `__init__`

```python
def __init__(self, *, device: ComputingDevice | None = None, apply_to: list[str] | None = None, seed: int | None = None, error_on_unexpected_behaviour: bool | None = None) -> None
```

##### `__init__`

```python
def __init__(self, size: list[numpy.ndarray[numpy.float64[3, 1]]] = ..., offset: list[numpy.ndarray[numpy.float64[3, 1]]] = ..., fill_value: list[float] = [0.0], size_units: ParamUnit = ..., offset_units: ParamUnit = ..., *, device: ComputingDevice | None = None, apply_to: list[str] | None = None, seed: int | None = None, error_on_unexpected_behaviour: bool | None = None) -> None
```

### `DataElement`

```python
class DataElement
```

Base class for elements containing typed data in a DataItem.

DataElement is the fundamental building block for storing heterogeneous data types
in machine learning pipelines. Each element wraps a specific data type (images, keypoints,
bounding boxes, tensors, etc.) and provides a uniform interface for processing.

DataElements support:
- Batch processing with consistent batch sizes
- Target tagging for training labels
- Cloning and splitting operations
- Type-specific content access

#### Methods

##### `batch_size`

*property*

```python
def batch_size(self) -> int
```

Returns the batch size of the element.

##### `clone`

```python
def clone(self, with_data: bool = True) -> DataElement
```

Create a copy of the element.

##### `components`

*property*

```python
def components(self) -> imfusion.DataComponentList
```

Returns the list of DataComponents for this element.

##### `content`

*property*

```python
def content(self) -> imfusion.Data
```

Access to the underlying Data.

##### `content`

```python
def content(self, arg1: imfusion.Data) -> None
```

##### `dimension`

*property*

```python
def dimension(self) -> int
```

Returns the dimensionality of the underlying data

##### `is_target`

*property*

```python
def is_target(self) -> bool
```

Returns true if this element is marked as a target.

##### `ndim`

*property*

```python
def ndim(self) -> int
```

Returns the dimensionality of the underlying data

##### `numpy`

```python
def numpy(self, copy = False)
```

Convert a DataElement to a numpy array.

This method enables numpy array protocol support for DataElement objects,
allowing them to be used with numpy.array() and similar functions.

:param copy: If True, force a copy of the data. Default: False
:returns: numpy array representation of the DataElement

##### `split`

```python
def split(self) -> list[DataElement]
```

Split an element into several ones along the batch dimension.

##### `stack`

*staticmethod*

```python
def stack(elements: list[DataElement]) -> DataElement
```

Stack several elements along the batch dimension.

##### `tag_as_target`

```python
def tag_as_target(self) -> None
```

Mark this element as being a target.

##### `torch`

```python
def torch(self: typing.Union[imfusion.machinelearning.DataElement, imfusion.SharedImageSet, imfusion.SharedImage], device: torch.device = None, dtype: torch.dtype = None, same_as: torch.Tensor = None) -> torch.Tensor
```

Convert SharedImageSet or a SharedImage to a torch.Tensor.

:param self: Instance of SharedImageSet or SharedImage (this function bound as a method to SharedImageSet and SharedImage)
:param device: Target device for the new torch.Tensor
:param dtype: Type of the new torch.Tensor
:param same_as: Template tensor whose device and dtype configuration should be matched.
                `device` and `dtype` are still applied afterwards.
:return: New torch.Tensor

##### `type`

*property*

```python
def type(self) -> ElementType
```

Returns the type of the underlying data

##### `untag_as_target`

```python
def untag_as_target(self) -> None
```

Remove target status from this element.

### `DataElementException`

```python
class DataElementException(Exception)
```

### `DataItem`

```python
class DataItem(imfusion.Data)
```

Class managing a dictionary of DataElements.
This class is used as the container for applying Operations
to a collection of heterogeneous data in a consistent way.
This class implements the concept of batch size for the contained elements.
As such, a DataItem can be split or stacked along the batch axis like the contained DataElements,
but because of that it enforces that all DataElement stored have consistent batch size.

#### Methods

##### `__init__`

```python
def __init__(self, elements: dict[str, DataElement] = {}) -> None
```

Construct a DataItem with existing Elements if provided.

Args:
        elements (Dict[str, imfusion.DataElement]): elements to be inserted into the DataItem, default: {}

##### `batch_size`

*property*

```python
def batch_size(self) -> int | None
```

Returns the batch size of the fields, zero if no elements are present, or None if there are inconsistencies within them.

##### `clear`

```python
def clear(self) -> None
```

Clears the data item and leaves it empty.

##### `clone`

```python
def clone(self, with_data: bool = True) -> DataItem
```

Returns a deep copy of the data item.

##### `contains`

```python
def contains(self, field_name: str) -> bool
```

Checks if the data item contains a field with the given name.

Args:
        field_name: Name of the field to check for
Returns:
        bool: True if the field exists, False otherwise

##### `dimension`

*property*

```python
def dimension(self) -> int
```

Returns the dimensionality of the elements, or zero if no elements are present or if there are inconsistencies within them.

##### `fields`

*property*

```python
def fields(self) -> set[str]
```

Returns the set of fields contained in the data item.

##### `get`

```python
def get(self, field: str) -> DataElement
```

Returns a reference to an element (raises a KeyError if field is not in item)

Args:
        field (str): Name of the field to retrieve.

##### `get`

```python
def get(self, field: str, default: DataElement) -> DataElement
```

Returns a reference to an element (or the default value if field is not in item)

Args:
        field (str): Name of the field to retrieve.
        default (imfusion.machinelearning.DataElement): default value to return if field is not in DataItem.

##### `get_all`

```python
def get_all(self, element_type: ElementType) -> set[DataElement]
```

Returns a set of all elements of the specified type.

This method filters the DataItem to find all DataElements matching the given ElementType.

Args:
        element_type: The type of elements to retrieve (e.g., ElementType.Image, ElementType.Keypoint)
Returns:
        set: Set of DataElements matching the specified type

##### `items`

```python
def items(self) -> typing.Iterator[tuple[str, DataElement]]
```

Returns an iterator over (field_name, DataElement) pairs in the DataItem

##### `keys`

```python
def keys(self) -> typing.Iterator[str]
```

Returns an iterator over all field names in the DataItem

##### `load`

*staticmethod*

```python
def load(location: str | os.PathLike) -> DataItem
```

Load data item from ImFusion file.

Args:
        location: input path.

##### `merge`

*staticmethod*

```python
def merge(items: list[DataItem]) -> DataItem
```

Merge several data items by setting all their fields to the output item

Args:
        items (List[imfusion.machinelearning.DataItem]): List of input items to merge.

Note:
        Raises an exception is the same field is contained in more than one item.

##### `ndim`

*property*

```python
def ndim(self) -> int
```

Returns the dimensionality of the elements, or zero if no elements are present or if there are inconsistencies within them.

##### `pop`

```python
def pop(self, field: str) -> DataElement
```

Remove the DataElement associated to the given field and returns it.

Args:
        field (str): Name of the field to remove.

##### `save`

```python
def save(self, location: str | os.PathLike) -> None
```

Save data item as ImFusion file.

Args:
        location: output path.

##### `set`

```python
def set(self, field: str, element: DataElement) -> None
```

Set a DataElement to the DataItem.

Args:
        field   (str): field name
        element (imfusion.machinelearning.DataElement): element to be inserted into the DataItem, if the field exists it's overwritten.

##### `set`

```python
def set(self, field: str, element: ImageElement) -> None
```

Set a ImageElement to the DataItem.

Args:
        field   (str): field name
        element (imfusion.machinelearning.ImageElement): element to be inserted into the DataItem, if the field exists it's overwritten.

##### `set`

```python
def set(self, field: str, element: KeypointsElement) -> None
```

Set a KeypointsElement to the DataItem.

Args:
        field   (str): field name
        element (imfusion.machinelearning.KeypointsElement): element to be inserted into the DataItem, if the field exists it's overwritten.

##### `set`

```python
def set(self, field: str, element: BoundingBoxElement) -> None
```

Set a BoundingBoxElement to the DataItem.

Args:
        field   (str): field name
        element (imfusion.machinelearning.BoundingBoxElement): element to be inserted into the DataItem, if the field exists it's overwritten.

##### `set`

```python
def set(self, field: str, element: VectorElement) -> None
```

Set a VectorElement to the DataItem.

Args:
        field   (str): field name
        element (imfusion.machinelearning.VectorElement): element to be inserted into the DataItem, if the field exists it's overwritten.

##### `set`

```python
def set(self, field: str, element: TensorSetElement) -> None
```

Set a TensorSetElement to the DataItem.

Args:
        field   (str): field name
        element (imfusion.machinelearning.TensorSetElement): element to be inserted into the DataItem, if the field exists it's overwritten.

##### `set`

```python
def set(self, field: str, shared_image_set: imfusion.SharedImageSet) -> None
```

Set a SharedImageSet to the DataItem.

Args:
        field   (str): field name
        element (imfusion.SharedImageSet): element to be inserted into the DataItem, if the field exists it's overwritten.

##### `set`

```python
def set(self, field: str, keypoint_set: KeypointSet) -> None
```

Set a KeypointSet to the DataItem.

Args:
        field   (str): field name
        element (imfusion.machinelearning.KeypointSet): element to be inserted into the DataItem, if the field exists it's overwritten.

##### `set`

```python
def set(self, field: str, bounding_box_set: BoundingBoxSet) -> None
```

Set a BoundingBoxSet to the DataItem.

Args:
        field   (str): field name
        element (imfusion.machinelearning.BoundingBoxSet): element to be inserted into the DataItem, if the field exists it's overwritten.

##### `set`

```python
def set(self, field: str, tensorset: TensorSet) -> None
```

Set a TensorSet to the DataItem.

Args:
        field   (str): field name
        element (imfusion.machinelearning.TensorSet): element to be inserted into the DataItem, if the field exists it's overwritten.

##### `split`

*staticmethod*

```python
def split(item: DataItem) -> list[DataItem]
```

Split a data item along the batch channels into items, each with batch size 1

Args:
        item (imfusion.machinelearning.DataItem): Item to split.

##### `stack`

*staticmethod*

```python
def stack(items: list[DataItem]) -> DataItem
```

Stack several data items along the batch dimension.

Args:
        items (List[imfusion.machinelearning.DataItem]): List of input items to stack.

##### `update`

```python
def update(self, other: DataItem, clone: bool = True) -> None
```

Update the contents of self with elements from other

Args:
        other (DataItem): DataItem that holds the information that should be added to self.
        clone (bool): Indicates whether `other` should be cloned before the update. If False `other` will be invalidated, default=True.
Note:
        Raises an exception if the batch_size of `other` does not match `self`.

##### `values`

```python
def values(self) -> typing.Iterator[DataElement]
```

Returns an iterator over all DataElements in the DataItem

### `DataItemException`

```python
class DataItemException(Exception)
```

### `DataLoaderError`

```python
class DataLoaderError(Exception)
```

### `DataLoaderSpecs`

```python
class DataLoaderSpecs
```

Specification for configuring a data loader in a Dataset pipeline.

This class contains the configuration parameters for a data loader, including
its name, properties, execution phase, and input/output field names.

#### Methods

##### `__init__`

```python
def __init__(self, name: str, configuration: imfusion.Properties, phase: Phase, inputs: list[str], output: str) -> None
```

Initialize a DataLoaderSpecs.

Args:
        name: Name of the data loader
        configuration: Properties object containing loader configuration
        phase: Execution phase (Training/Validation/Inference/Always)
        inputs: List of input field names
        output: Output field name

##### `configuration`

*property*

```python
def configuration(self) -> imfusion.Properties
```

Configuration properties for the data loader

##### `configuration`

```python
def configuration(self, arg0: imfusion.Properties) -> None
```

##### `inputs`

*property*

```python
def inputs(self) -> list[str]
```

Input field names

##### `inputs`

```python
def inputs(self, arg0: list[str]) -> None
```

##### `name`

*property*

```python
def name(self) -> str
```

Name of the data loader

##### `name`

```python
def name(self, arg0: str) -> None
```

##### `output`

*property*

```python
def output(self) -> str
```

Output field name

##### `output`

```python
def output(self, arg0: str) -> None
```

##### `phase`

*property*

```python
def phase(self) -> Phase
```

Execution phase for the data loader

##### `phase`

```python
def phase(self, arg0: Phase) -> None
```

### `Dataset`

```python
class Dataset
```

Class for creating an iterable dataset by chaining data loading and transforming operations executed in a lazy fashion.
The Dataset implements an iterable interface, which allows to use ``iter()`` and ``next()`` built-ins as well as range based loops.

#### Methods

##### `__init__`

```python
def __init__(self, data_lists: list[tuple[dict[int, str], list[str]]], shuffle: bool = False, verbose: bool = False) -> None
```

Constructs a dataset from lists of filenames.

Args:
        data_lists (list): list txt files, each listing the file paths. Complete type: List[Str]
        shuffle (bool): shuffle file order. Default: false
        verbose (bool): enable verbose logging. Default: false

##### `__init__`

```python
def __init__(self, read_from: str, reader_properties: imfusion.Properties, verbose: bool = False) -> None
```

Constructs a dataset by specifying a reader type as a string.

Args:
        read_from (string): specifies the type of reader that is created implicitly. Options: "filesystem".
        reader_properties (Properties): properties used to configure the reader.
        verbose (bool): print debug information when running the data loader. Default: false

##### `__init__`

```python
def __init__(self, verbose: bool = False) -> None
```

Constructs an empty dataset.

Args:
        verbose (bool): print debug information when running the data loader.
                                        Default: false

##### `available_decorators`

*staticmethod*

```python
def available_decorators() -> list[str]
```

Returns the keys of the register decorator functions

##### `available_filter_functions`

*staticmethod*

```python
def available_filter_functions() -> list[str]
```

Returns filter function keys to be used in Dataset.filter decorator function

##### `available_map_functions`

*staticmethod*

```python
def available_map_functions() -> list[str]
```

Returns map function keys to be used in Dataset.map decorator function

##### `batch`

```python
def batch(self, batch_size: int = 1, pad: bool = False, overlap: int = 0) -> Dataset
```

Batches the next ``batch_size`` items in a single one before returning it.

Args:
        batch_size (int): batch size.
        pad (bool): last batch gets filled up with the last data item until the batch matches batch_size.
        overlap (int): number of items overlapping in consecutive batches. Must be less than batch_size.

##### `build_pipeline`

```python
def build_pipeline(self, property_list: list[DataLoaderSpecs], config_phase: Phase = Phase.ALWAYS) -> None
```

Configures the Dataset decorators to be used based on a list of Properties.

Args:
        property_list (list): list of imfusion.Properties or list of dict()
        config_phase (Phase): configuration phase.

##### `cache`

```python
def cache(self, make_exclusive_cpu: bool = True, lazy: bool = True, compression_level: int = 0, shuffle: bool = False) -> Dataset
```

Deprecated - please use 'memory_cache' now.

##### `disk_cache`

```python
def disk_cache(self, location: str = '', lazy: bool = True, reload_from_disk: bool = True, compression: bool = False, shuffle: bool = False) -> Dataset
```

Caches the dataset already loaded in a persistent manner (on a disk location)
Raises a DataLoaderError if the dataset is not countable.

Args:
        location (string): path to the folder where all the data will be cached.
        lazy (bool): if false, the cache is filled upon construction (otherwise as items are requested).
        reload_from_disk (bool): try to reload the cache from a previous session (`reload` is the deprecated name of this parameter).
        compression (bool): use ZStandard compression.
        shuffle (bool): re-shuffle the cache order every epoch.

##### `filter`

```python
def filter(self, func: typing.Callable[[DataItem], bool]) -> Dataset
```

Filters the dataset according to a user defined function.
Note: Filtering makes the dataset uncountable, since the func output is conditional.

Args:
        func (def func(dict) -&gt; bool): filtering criterion to be applied to each input item. The input must be of the form dict[str, SharedImageSet]

##### `filter`

```python
def filter(self, func_name: str) -> Dataset
```

Filters the dataset according to a user defined function.
Note: Filtering makes the dataset uncountable, since the func output is conditional.

Args:
        func_name (str): name of a registered filter function specifying a criterion to be applied to each input item. The input must be of the form dict[str, SharedImageSet]

##### `map`

```python
def map(self, func: typing.Callable[[DataItem], None], num_parallel_calls: int = 1) -> Dataset
```

Applies a mapping to each item of the dataset.
Optionally specify the number ``num_parallel_calls`` of asynchronous threads which are used for the mapping.

Args:
        func (def func(dict) -&gt; dict): function mapping the input items. The input and output must be of the form dict[str, SharedImageSet]
        num_parallel_calls (int): specify the number ``num_parallel_calls`` of asynchronous threads which are used for the mapping. Defaults to 1.

##### `map`

```python
def map(self, func_name: str, num_parallel_calls: int = 1) -> Dataset
```

Applies a mapping to each item of the dataset.
Optionally specify the number ``num_parallel_calls`` of asynchronous threads which are used for the mapping.

Args:
        func_name (str): name of a registered function, mapping the input items. The input and output must be of the form dict[str, SharedImageSet]
        num_parallel_calls (int): specify the number ``num_parallel_calls`` of asynchronous threads which are used for the mapping. Defaults to 1.

##### `memory_cache`

```python
def memory_cache(self, make_exclusive_cpu: bool = True, lazy: bool = True, compression_level: int = 0, shuffle: bool = False, num_threads: int = 1) -> Dataset
```

Caches the dataset already loaded.
Raises a DataLoaderError if the dataset is not countable.
Raises a MemoryError if the system runs out of memory.

Args:
        make_exclusive_cpu (bool): keep the data exclusively on CPU.
        lazy (bool): if false, the cache is filled upon construction (otherwise as items are requested).
        compression_level (int): controls compression, valid values are between 0 and 20. Higher means more compression, but slower. 0 disables compression.
        shuffle (bool): re-shuffle the cache order every epoch.
        num_threads (int): number of threads to use for copying from the cache

##### `prefetch`

```python
def prefetch(self, prefetch_size: int, sync_to_gl: bool = True) -> Dataset
```

Prefetches items from the underlying loader in a background thread.

Args:
        prefetch_size (int): number of items to prefetch.
        sync_to_gl (bool): synchronize the objects to GL memory after being prefetched.

##### `preprocess`

```python
def preprocess(self, preprocessing_pipeline: list[tuple[str, imfusion.Properties, Phase]], exec_phase: Phase = Phase.ALWAYS) -> Dataset
```

Adds a generic preprocessing step to the data pipeline.
The processing is performed by the underlying sequence of `~imfusion.machinelearning.Operation`.

Args:
        preprocessing_pipeline: List of specifications to construct the underlying `~imfusion.machinelearning.OperationsSequence`.
                Each specification must be a tuple consisting of the name of the operation, its `~imfusion.Phase`, and `~imfusion.Properties` for configuring it.
        exec_phase: Execution phase for the entire preprocessing pipeline.
                The execution will run only those operations whose phase (specified in the specs) corresponds to the current exec_phase, with the following exceptions:

                        1) Operations marked with phase == Phase.Always are always run regardless of the exec_phase.
                        2) If exec_phase == Phase.Always, all operations in the preprocessing pipeline are run regardless of their individual phase.

##### `preprocess`

```python
def preprocess(self, operations: list[Operation]) -> Dataset
```

Adds a generic preprocessing step to the data pipeline.
The processing is performed by the underlying sequence of `~imfusion.machinelearning.Operation`.

Args:
        operations: List of operations that will do the actual processing.

##### `randomize`

```python
def randomize(self, buffer_size: int, seed: int = -1) -> Dataset
```

Applies iterative replacement shuffling to the dataset. Maintains a fixed-size buffer that provides
randomized items through iterative replacement. Initially fills the buffer, then continuously returns
random items while replacing them with new items from the source. This avoids the batch filling delays
of traditional shuffle approaches.

Args:
        buffer_size (int): size of the replacement buffer.
        seed (int): seed for the random selection.

##### `read`

```python
def read(self, reader_type: str, reader_properties: imfusion.Properties, verbose: bool = False) -> Dataset
```

Constructs a dataset by specifying a reader type as a string.

Args:
        reader_type: specifies the type of reader that is created implicitly.
                Options: "filesystem" (MemoryReader needs to be fixed to work with properties)
        reader_properties: properties used to configure the reader.
        verbose: print debug information when running the data loader.
                                        Default: false

##### `reinit`

```python
def reinit(self) -> None
```

Reinit the dataset, clearing state surviving reset() (i.e. data caches).

##### `repeat`

```python
def repeat(self, num_epoch_repetitions: int, num_item_repetitions: int = 1) -> Dataset
```

Repeats the dataset ``num_epoch_repetitions`` times and each individual item ``num_item_repetitions`` times.

Args:
        num_epoch_repetitions (int): number of times the underlying dataset epoch is repeated. If num_epoch_repetitions == -1, it repeats the dataset infinitely.
        num_item_repetitions (int): number of times each item is repeated. If num_item_repetitions == -1, it repeats the item infinitely.

##### `reset`

```python
def reset(self) -> None
```

Resets the data loader.

##### `sample`

```python
def sample(self, sampling_pipeline: list[tuple[str, imfusion.Properties]], *, sampler_selection_seed: int = 1) -> Dataset
```

Adds a ROI sampling step to the data pipeline.
During this step the loaded image is reduced to a region of interest (ROI).
The strategy for sampling this regions location is determined by the `ImageROISampler`\s, which is randomly chosen from the underlying sampler set each time this step executes.

Args:
        sampling_set_config: List of tuples of sampler name and corresponding `~imfusion.Properties` for configuring it.
        sampler_selection_seed: Seed for the random generator of the samplers selection

##### `sample`

```python
def sample(self, samplers: list[ImageROISampler], weights: list[float] | None = None, *, sampler_selection_seed: int = 1) -> Dataset
```

Adds a ROI sampling step to the data pipeline.
During this step the loaded image is reduced to a region of interest (ROI).
The strategy for sampling this regions location is determined by the `ImageROISampler`\s, which is randomly chosen from the underlying sampler set each time this step executes.

Args:
        samplers: List of sampler to choose from when sampling.
        weights: Probability weights for the samplers specifying the relative probability of choosing each sampler.
        sampler_selection_seed (unsigned int): Seed for the random generator of the samplers selection

##### `sample`

```python
def sample(self, sampler: ImageROISampler) -> Dataset
```

Adds a ROI sampling step to the data pipeline.
During this step the loaded image is reduced to a region of interest (ROI).
The strategy for sampling this regions location is determined by the `ImageROISampler`\s, which is randomly chosen from the underlying sampler set each time this step executes.

Args:
        sampler: Sampler to choose from when sampling.

##### `set_random_seed`

```python
def set_random_seed(self, seed: int) -> None
```

Seeds the data loading pipeline.

##### `shuffle`

```python
def shuffle(self, shuffle_buffer: int = -1, seed: int = -1) -> Dataset
```

Shuffles the next ``how_many`` items of the dataset. Defaults to -1, i.e. shuffles the entire dataset.
If ``how_many`` is not specified and the dataset is not countable, it throws a DataLoaderError.

Args:
        shuffle_buffer (int): number of consecutive items to shuffle. Defaults to all items of the dataset
        seed (int): seed for the random shuffling.

##### `size`

*property*

```python
def size(self) -> int | None
```

Returns the length of the dataset or None if the set is uncountable.

##### `split`

```python
def split(self, num_items: int = -1) -> Dataset
```

Splits the content of the SharedImagesSets into SIS containing a single image.

Args:
        num_items: Keep only the first `num_items` frames. Default is -1, which keeps all frames.

Note:
        Calling this method will make the dataset uncountable

##### `verbose`

*property*

```python
def verbose(self) -> bool
```

Flag indicating whether extra information is logged when fetching data items.

.. note:
        Due to how this class is implemented, setting this flag only affects subsequent calls to decorator methods
        (e.g. `~imfusion.machinelearning.Dataset.batch`, `~imfusion.machinelearning.Dataset.sample`, etc.)
        and not past ones.

##### `verbose`

```python
def verbose(self, arg1: bool) -> None
```

### `DefaultROISampler`

```python
class DefaultROISampler(ImageROISampler)
```

Sampler which simply returns the image and the label map, after padding of a specified dimension divisor:
each spatial dimension of the output arrays will be divisible by `dimension_divisor`.

Args:
    dimension_divisor: Divisor of dimensions of the output images
    device: Specifies whether this Operation should run on CPU or GPU.
    seed: Specifies seeding for any randomness that might be contained in this operation.
    error_on_unexpected_behaviour: Specifies whether to throw an exception instead of warning about unexpected behavior.
    apply_to: Specifies fields in a DataItem that this Operation should process (if empty, will select suitable fields based on the current `~imfusion.machinelearning.Operation.processing_policy`)
    record_identifier: Unused for this operation as it is not invertible

#### Methods

##### `__init__`

```python
def __init__(self, *, device: ComputingDevice | None = None, apply_to: list[str] | None = None, seed: int | None = None, error_on_unexpected_behaviour: bool | None = None) -> None
```

##### `__init__`

```python
def __init__(self, dimension_divisor: int, *, device: ComputingDevice | None = None, apply_to: list[str] | None = None, seed: int | None = None, error_on_unexpected_behaviour: bool | None = None) -> None
```

### `DeformationOperation`

```python
class DeformationOperation(Operation)
```

Apply a deformation to the image using a specified control point grid and specified displacements.

Args:
    num_subdivisions: list specifying the number of subdivisions for each dimension (the number of control points is subdivisions+1). For 2D images, there must be 0 subdivision in the last component. Default: [1, 1, 1]
    displacements: list of 3-dim vectors specifying the displacement (mm) for each control point. Should have length equal to the number of control points. Default: []
    padding_mode: defines which type of padding is used in ["zero", "clamp", "mirror"]. Default: `~imfusion.PaddingMode.ZERO`
    adjust_size: configures whether the resulting image should adjust its size to encompass the deformation. Default: False

    device: Specifies whether this Operation should run on CPU or GPU.
    seed: Specifies seeding for any randomness that might be contained in this operation.
    error_on_unexpected_behaviour: Specifies whether to throw an exception instead of warning about unexpected behavior.
    apply_to: Specifies fields in a DataItem that this Operation should process (if empty, will select suitable fields based on the current `~imfusion.machinelearning.Operation.processing_policy`)
    record_identifier: Unused for this operation as it is not invertible
Note: `~imfusion.PaddingMode` can be automatically converted from a string. This means you can directly pass a string like "zero", "clamp", or "mirror" to the padding_mode parameters instead of using the enum values.

#### Methods

##### `__init__`

```python
def __init__(self, *, device: ComputingDevice | None = None, apply_to: list[str] | None = None, seed: int | None = None, error_on_unexpected_behaviour: bool | None = None) -> None
```

##### `__init__`

```python
def __init__(self, num_subdivisions: numpy.ndarray[numpy.int32[3, 1]] = ..., displacements: list[numpy.ndarray[numpy.float32[3, 1]]] = [], padding_mode: imfusion.PaddingMode = imfusion.PaddingMode.ZERO, adjust_size: bool = False, *, device: ComputingDevice | None = None, apply_to: list[str] | None = None, seed: int | None = None, error_on_unexpected_behaviour: bool | None = None) -> None
```

### `DiceMetric`

```python
class DiceMetric(Metric)
```

Computes the Dice coefficient for segmentation tasks.

The Dice coefficient measures the overlap between predicted and target segmentation masks.

#### Methods

##### `__init__`

```python
def __init__(self, ignore_background: bool = True) -> None
```

Constructs a DiceMetric.

Args:
        ignore_background: If True, ignores the background class (label 0) when computing the Dice coefficient. Default: True

##### `compute_dice`

```python
def compute_dice(self, prediction: imfusion.SharedImageSet, target: imfusion.SharedImageSet) -> list[dict[int, float]]
```

Computes the Dice coefficient for the given prediction and target segmentations.

The Dice coefficient measures overlap between two segmentation masks, with values
ranging from 0 (no overlap) to 1 (perfect overlap).

Args:
        prediction: The predicted segmentation mask
        target: The target/ground truth segmentation mask
Returns:
        float: The Dice coefficient value between 0 and 1

### `Engine`

```python
class Engine
```

Generic interface for machine learning models serialized by specific frameworks (e.g. PyTorch, ONNX, etc.).

This class is used by the `MachineLearningModel` to forward the prediction request to the framework
that was used to serialize the model.

See `imfusion.machinelearning.engines` for examples of Python engine implementations.

#### Methods

##### `__call__`

```python
def __call__(self, input: DataItem) -> DataItem
```

Runs the prediction.

##### `__init__`

```python
def __init__(self, name: str) -> None
```

Initialize a custom Engine subclass.

This constructor is used when creating custom engine implementations in Python.

Args:
        name: Name identifier for the engine type (e.g., 'torch', 'onnx')

##### `available_providers`

```python
def available_providers(self) -> list[ExecutionProvider]
```

Returns the execution providers available to the Engine

##### `check_input_fields`

```python
def check_input_fields(self, input: DataItem) -> None
```

Checks that input fields specified in the model yaml config are present in the input item.

##### `check_output_fields`

```python
def check_output_fields(self, input: DataItem) -> None
```

Checks the output fields specified in the model yaml config are present in the item returned by `predict`.

##### `config`

*property*

```python
def config(self) -> EngineConfiguration
```

Engine configuration object. Modify properties directly (e.g., config.force_cpu = True).
Property changes automatically trigger synchronization with deprecated parameters via signals.

##### `configure`

```python
def configure(self, properties: imfusion.Properties) -> None
```

Configures the Engine.

##### `connect_signals`

```python
def connect_signals(self) -> None
```

Connects signals like on_model_file_changed, on_force_cpu_changed.

##### `force_cpu`

*property*

```python
def force_cpu(self) -> bool
```

If set, forces the model to run on CPU.

##### `force_cpu`

```python
def force_cpu(self, arg1: bool) -> None
```

##### `init`

```python
def init(self, properties: imfusion.Properties) -> None
```

Initializes the Engine.

##### `input_fields`

*property*

```python
def input_fields(self) -> list[str]
```

Names of the model input heads.

##### `input_fields`

```python
def input_fields(self, arg1: list[str]) -> None
```

##### `is_identical`

```python
def is_identical(self, other: Engine) -> bool
```

Compares this engine instance with another one.

##### `load_model_artifact`

```python
def load_model_artifact(self) -> bytes
```

Loads the model artifact specified in property `model_file` either from the model file or from a resource repository.

Returns:
        bytes: The model artifact as a bytes object.

Raises:
        RuntimeError: If the model artifact could not be loaded.

##### `model_file`

*property*

```python
def model_file(self) -> str
```

Path to the yaml model configuration.

##### `model_file`

```python
def model_file(self, arg1: str) -> None
```

##### `name`

*property*

```python
def name(self) -> str
```

The name/type of this engine (e.g., 'onnx', 'torch', 'openvino')

##### `on_force_cpu_changed`

```python
def on_force_cpu_changed(self) -> None
```

Signal triggered when p_force_cpu changes.

##### `on_model_file_changed`

```python
def on_model_file_changed(self) -> None
```

Signal triggered when p_model_file changes.

##### `output_fields`

*property*

```python
def output_fields(self) -> list[str]
```

Names of the model output heads.

##### `output_fields`

```python
def output_fields(self, arg1: list[str]) -> None
```

##### `output_fields_to_ignore`

*property*

```python
def output_fields_to_ignore(self) -> list[str]
```

Model output heads to discard.

##### `output_fields_to_ignore`

```python
def output_fields_to_ignore(self, arg1: list[str]) -> None
```

##### `predict`

```python
def predict(self, input: DataItem) -> DataItem
```

Runs the prediction.

##### `provider`

```python
def provider(self) -> ExecutionProvider | None
```

Returns the execution provider currently used by the Engine.

##### `version`

*property*

```python
def version(self) -> str
```

Version of the model configuration.

##### `version`

```python
def version(self, arg1: str) -> None
```

### `EngineConfiguration`

```python
class EngineConfiguration
```

Configuration settings for a machine learning engine.

This class contains all the configuration parameters needed to set up and run
a machine learning inference engine, including model paths, device settings,
and input/output field specifications.

#### Attributes

##### `default_input_name`

```python
default_input_name: typing.ClassVar[str] = 'Input'
```

##### `default_output_name`

```python
default_output_name: typing.ClassVar[str] = 'Prediction'
```

#### Methods

##### `configure`

```python
def configure(self, properties: imfusion.Properties) -> None
```

Configures the EngineConfiguration.

##### `engine_specific_parameters`

*property*

```python
def engine_specific_parameters(self) -> imfusion.Properties
```

Parameter that are specific to the type of Engine.

##### `engine_specific_parameters`

```python
def engine_specific_parameters(self, arg0: imfusion.Properties) -> None
```

##### `force_cpu`

*property*

```python
def force_cpu(self) -> bool
```

If set, forces the model to run on CPU.

##### `force_cpu`

```python
def force_cpu(self, arg1: bool) -> None
```

##### `input_fields`

*property*

```python
def input_fields(self) -> list[str]
```

Names of the model input heads.

##### `input_fields`

```python
def input_fields(self, arg1: list[str]) -> None
```

##### `model_file`

*property*

```python
def model_file(self) -> str
```

Path to the yaml model configuration.

##### `model_file`

```python
def model_file(self, arg1: str) -> None
```

##### `output_fields`

*property*

```python
def output_fields(self) -> list[str]
```

Names of the model output heads.

##### `output_fields`

```python
def output_fields(self, arg1: list[str]) -> None
```

##### `output_fields_to_ignore`

*property*

```python
def output_fields_to_ignore(self) -> list[str]
```

Model output heads to discard.

##### `output_fields_to_ignore`

```python
def output_fields_to_ignore(self, arg1: list[str]) -> None
```

##### `to_properties`

```python
def to_properties(self) -> imfusion.Properties
```

Converts the EngineConfiguration to a Properties object.

##### `type`

*property*

```python
def type(self) -> str
```

Type of Engine, i.e. torch, onnx, openvino...

##### `type`

```python
def type(self, arg1: str) -> None
```

##### `version`

*property*

```python
def version(self) -> str
```

Version of the model configuration.

##### `version`

```python
def version(self, arg1: str) -> None
```

### `EnsureExplicitMaskOperation`

```python
class EnsureExplicitMaskOperation(Operation)
```

Converts the existing mask of all input images into explicit masks.
If an image does not have a mask, no mask will be created.
Warning: This operation might be computationally extensive since it processes every frame of the SharedImageSet independently.

Args:

    device: Specifies whether this Operation should run on CPU or GPU.
    seed: Specifies seeding for any randomness that might be contained in this operation.
    error_on_unexpected_behaviour: Specifies whether to throw an exception instead of warning about unexpected behavior.
    apply_to: Specifies fields in a DataItem that this Operation should process (if empty, will select suitable fields based on the current `~imfusion.machinelearning.Operation.processing_policy`)
    record_identifier: Unused for this operation as it is not invertible

#### Methods

##### `__init__`

```python
def __init__(self, *, device: ComputingDevice | None = None, apply_to: list[str] | None = None, seed: int | None = None, error_on_unexpected_behaviour: bool | None = None) -> None
```

##### `__init__`

```python
def __init__(self, *, device: ComputingDevice | None = None, apply_to: list[str] | None = None, seed: int | None = None, error_on_unexpected_behaviour: bool | None = None) -> None
```

### `EnsureOneToOneMatrixMappingOperation`

```python
class EnsureOneToOneMatrixMappingOperation(Operation)
```

Ensures that it is possible to get/set the matrix of each frame of the input image set independently.
This operation is targeted at TrackedSharedImageSets, which might define their matrices via a tracking sequence with timestamps (there is then no one-to-one correspondence between matrices and images, but matrices are looked-up and interpolated via their timestamps). In such cases, the operation creates a new tracking sequence with as many samples as images and turns off the timestamp usage.

Args:

    device: Specifies whether this Operation should run on CPU or GPU.
    seed: Specifies seeding for any randomness that might be contained in this operation.
    error_on_unexpected_behaviour: Specifies whether to throw an exception instead of warning about unexpected behavior.
    apply_to: Specifies fields in a DataItem that this Operation should process (if empty, will select suitable fields based on the current `~imfusion.machinelearning.Operation.processing_policy`)
    record_identifier: Unused for this operation as it is not invertible

#### Methods

##### `__init__`

```python
def __init__(self, *, device: ComputingDevice | None = None, apply_to: list[str] | None = None, seed: int | None = None, error_on_unexpected_behaviour: bool | None = None) -> None
```

##### `__init__`

```python
def __init__(self, *, device: ComputingDevice | None = None, apply_to: list[str] | None = None, seed: int | None = None, error_on_unexpected_behaviour: bool | None = None) -> None
```

### `ExtractRandomSubsetOperation`

```python
class ExtractRandomSubsetOperation(Operation)
```

Extracts a random subset from a SharedImageSet.

Args:
    subset_size: Size of the extracted subset of images. Default: 1
    keep_order: If true the extracted subset will have the same ordering as the input. Default: False
    probability: Probability of applying this Operation. Default: 1.0
    device: Specifies whether this Operation should run on CPU or GPU.
    seed: Specifies seeding for any randomness that might be contained in this operation.
    error_on_unexpected_behaviour: Specifies whether to throw an exception instead of warning about unexpected behavior.
    apply_to: Specifies fields in a DataItem that this Operation should process (if empty, will select suitable fields based on the current `~imfusion.machinelearning.Operation.processing_policy`)
    record_identifier: Unused for this operation as it is not invertible

#### Methods

##### `__init__`

```python
def __init__(self, *, device: ComputingDevice | None = None, apply_to: list[str] | None = None, seed: int | None = None, error_on_unexpected_behaviour: bool | None = None) -> None
```

##### `__init__`

```python
def __init__(self, subset_size: int = 1, keep_order: bool = False, probability: float = 1.0, *, device: ComputingDevice | None = None, apply_to: list[str] | None = None, seed: int | None = None, error_on_unexpected_behaviour: bool | None = None) -> None
```

### `ExtractSubsetOperation`

```python
class ExtractSubsetOperation(Operation)
```

Extracts a subset from a SharedImageSet.

Args:
    subset: Indices of the selected images.
    device: Specifies whether this Operation should run on CPU or GPU.
    seed: Specifies seeding for any randomness that might be contained in this operation.
    error_on_unexpected_behaviour: Specifies whether to throw an exception instead of warning about unexpected behavior.
    apply_to: Specifies fields in a DataItem that this Operation should process (if empty, will select suitable fields based on the current `~imfusion.machinelearning.Operation.processing_policy`)
    record_identifier: Unused for this operation as it is not invertible

#### Methods

##### `__init__`

```python
def __init__(self, *, device: ComputingDevice | None = None, apply_to: list[str] | None = None, seed: int | None = None, error_on_unexpected_behaviour: bool | None = None) -> None
```

##### `__init__`

```python
def __init__(self, subset: list[int], *, device: ComputingDevice | None = None, apply_to: list[str] | None = None, seed: int | None = None, error_on_unexpected_behaviour: bool | None = None) -> None
```

### `ForegroundGuidedLabelUpsamplingOperation`

```python
class ForegroundGuidedLabelUpsamplingOperation(Operation)
```

Generates a high-resolution label map by upsampling a multi-class softmax prediction guided by a high-resolution binary segmentation.
This operation combines a high-resolution binary segmentation (e.g., from a sigmoid prediction) with a lower-resolution 
multi-class one-hot encoded segmentation (e.g., from a softmax prediction) to produce a refined high-resolution multi-class label map.
The approach is inspired by pan-sharpening techniques used in remote sensing (https://arxiv.org/abs/1504.04531).
The multi-class one hot image should contain the background class as the first channel.

Args:
    apply_to: List of field names for input images, expected order: ["highResSigmoid", "lowResSoftmax"]
    output_field: Name for the output field. If not specified, overwrites first input field
    remove_fields: Remove input fields after processing. Default: True
    apply_sigmoid: Use sigmoid intensities to guide foreground/background decision. If False, outputs most likely
                                              non-background class (if any, otherwise background) from softmax. Default: True
    guidance_weight: Weight of sigmoid vs softmax for foreground decision [0-1]. Lower values can reduce false positives.
                                                     Ignored if apply_sigmoid=False. Default: 1.0
    boundary_refinement_max_iter: Maximum iterations for boundary refinement at output resolution. Higher values
                                                                        may be needed for larger resolution differences. Ideal values depend on the data and `boundary_refinement_smooth`. Default: 3
    boundary_refinement_smooth: Smoothing factor for boundary refinement. Larger values remove smaller label patches. Default: 1.0
    boundary_refinement_add_only: Optional list of label values to restrict the refinement to additions only. Default: []
    device: Specifies whether this Operation should run on CPU or GPU.
    seed: Specifies seeding for any randomness that might be contained in this operation.
    error_on_unexpected_behaviour: Specifies whether to throw an exception instead of warning about unexpected behavior.
    record_identifier: Unused for this operation as it is not invertible

#### Methods

##### `__init__`

```python
def __init__(self, *, device: ComputingDevice | None = None, seed: int | None = None, error_on_unexpected_behaviour: bool | None = None) -> None
```

##### `__init__`

```python
def __init__(self, apply_to: list[str] = ['highResSigmoid', 'lowResSoftmax'], output_field: str | None = None, remove_fields: bool = True, apply_sigmoid: bool = True, guidance_weight: float = 1.0, boundary_refinement_max_iter: int = 3, boundary_refinement_smooth: float = 1.0, boundary_refinement_add_only: list[int] = [], *, device: ComputingDevice | None = None, seed: int | None = None, error_on_unexpected_behaviour: bool | None = None) -> None
```

### `GammaCorrectionOperation`

```python
class GammaCorrectionOperation(Operation)
```

Apply a gamma correction which changes the overall contrast (see https://en.wikipedia.org/wiki/Gamma_correction)

Args:
    gamma: Power applied to the normalized intensities. Default: 1.0
    device: Specifies whether this Operation should run on CPU or GPU.
    seed: Specifies seeding for any randomness that might be contained in this operation.
    error_on_unexpected_behaviour: Specifies whether to throw an exception instead of warning about unexpected behavior.
    apply_to: Specifies fields in a DataItem that this Operation should process (if empty, will select suitable fields based on the current `~imfusion.machinelearning.Operation.processing_policy`)
    record_identifier: Unused for this operation as it is not invertible

#### Methods

##### `__init__`

```python
def __init__(self, *, device: ComputingDevice | None = None, apply_to: list[str] | None = None, seed: int | None = None, error_on_unexpected_behaviour: bool | None = None) -> None
```

##### `__init__`

```python
def __init__(self, gamma: float = 1.0, *, device: ComputingDevice | None = None, apply_to: list[str] | None = None, seed: int | None = None, error_on_unexpected_behaviour: bool | None = None) -> None
```

### `GenerateRandomKeypointsOperation`

```python
class GenerateRandomKeypointsOperation(Operation)
```

Generate uniformly distributed random keypoints in the image. Optionally the distribution is restricted to label values that are nonzero,
otherwise (or if there are no nonzero label values), then the keypoints are sampled from the entire image extent. There is
a fixed (but configurable) number of keypoints per channel, and a fixed (but configurable) number of output channels
in the output keypoint element.

Args:
    num_points: Number of points to generate per channel. Default: 1.
    num_channels: Number of channels in the output keypoint set. Default: 1.
    sample_from_label: Whether or not points should be drawn from the label if possible. Default: False.
    output_field_name: Name of the output keypoints field. Default: "keypoints".
    device: Specifies whether this Operation should run on CPU or GPU.
    seed: Specifies seeding for any randomness that might be contained in this operation.
    error_on_unexpected_behaviour: Specifies whether to throw an exception instead of warning about unexpected behavior.
    apply_to: Specifies fields in a DataItem that this Operation should process (if empty, will select suitable fields based on the current `~imfusion.machinelearning.Operation.processing_policy`)
    record_identifier: Unused for this operation as it is not invertible

#### Methods

##### `__init__`

```python
def __init__(self, *, device: ComputingDevice | None = None, apply_to: list[str] | None = None, seed: int | None = None, error_on_unexpected_behaviour: bool | None = None) -> None
```

##### `__init__`

```python
def __init__(self, num_points: int = 1, num_channels: int = 1, sample_from_label: bool = False, output_field_name: str = 'keypoints', *, device: ComputingDevice | None = None, apply_to: list[str] | None = None, seed: int | None = None, error_on_unexpected_behaviour: bool | None = None) -> None
```

### `HighPassOperation`

```python
class HighPassOperation(Operation)
```

Smooths the input image with a Gaussian kernel with `half_kernel_size`, then subtracts the smoothed image from the input, resulting in a reduction of low-frequency components.

Args:
    half_kernel_size: half kernel size in pixels. Corresponding standard deviation is half_kernel_size / 3.
    device: Specifies whether this Operation should run on CPU or GPU.
    seed: Specifies seeding for any randomness that might be contained in this operation.
    error_on_unexpected_behaviour: Specifies whether to throw an exception instead of warning about unexpected behavior.
    apply_to: Specifies fields in a DataItem that this Operation should process (if empty, will select suitable fields based on the current `~imfusion.machinelearning.Operation.processing_policy`)
    record_identifier: Unused for this operation as it is not invertible

#### Methods

##### `__init__`

```python
def __init__(self, *, device: ComputingDevice | None = None, apply_to: list[str] | None = None, seed: int | None = None, error_on_unexpected_behaviour: bool | None = None) -> None
```

##### `__init__`

```python
def __init__(self, half_kernel_size: int, *, device: ComputingDevice | None = None, apply_to: list[str] | None = None, seed: int | None = None, error_on_unexpected_behaviour: bool | None = None) -> None
```

### `ImageElement`

```python
class ImageElement(SISBasedElement)
```

DataElement for storing and processing image data.

ImageElement wraps a SharedImageSet to represent image inputs or outputs in ML pipelines.
It inherits from SISBasedElement and provides access to the underlying image data
through the `sis` property.

#### Methods

##### `__init__`

```python
def __init__(self, image: imfusion.SharedImageSet) -> None
```

Initialize an ImageElement from a SharedImageSet.

Args:
        image (imfusion.SharedImageSet): image to be converted to a ImageElement

##### `from_torch`

*staticmethod*

```python
def from_torch(tensor: torch.Tensor) -> ImageElement
```

Create an ImageElement from a torch Tensor.

The tensor's channel dimension should be at index 1 (NCHW or NCDHW format).
This is a convenience wrapper around SharedImageSet.from_torch() that automatically
wraps the result in an ImageElement.

:param tensor: Instance of torch.Tensor to convert
:return: New ImageElement containing the converted data

### `ImageMathOperation`

```python
class ImageMathOperation(Operation)
```

Computes a specified formula involving images from the input dataitem.
Supported operations between images of same shape or an image and a scalar:

    - Addition/Substraction (``+``, ``-``)
    - Multiplication/Division (``*``, ``/``)

Parenthesis can be used to specify operation priorities.
For instance, if the dataitem contains 3 elements: image, additive_noise, multiplicative_noise, one can compute:
noisy_image = image * multiplicative_noise + additive_noise
and store the output in the dataitem under the "noisy_image" field.

The different images are expected to have the same shape.

The resulting image is of type float and does not have a matrix or spacing. These will be copied from the "metaDataFrom" element.

Args:
    formula (string): Formula to be computed. Variables from the dataitem must be referred to by their dataitem field (see example above).
    meta_data_from (string): Optional ImageElement to get the matrix and spacing information from. If not specified or empty, the output won't have a matrix or spacing information. Default: ''
    device: Specifies whether this Operation should run on CPU or GPU.
    seed: Specifies seeding for any randomness that might be contained in this operation.
    error_on_unexpected_behaviour: Specifies whether to throw an exception instead of warning about unexpected behavior.
    apply_to: Specifies fields in a DataItem that this Operation should process (if empty, will select suitable fields based on the current `~imfusion.machinelearning.Operation.processing_policy`)
    record_identifier: Unused for this operation as it is not invertible

#### Methods

##### `__init__`

```python
def __init__(self, *, device: ComputingDevice | None = None, apply_to: list[str] | None = None, seed: int | None = None, error_on_unexpected_behaviour: bool | None = None) -> None
```

##### `__init__`

```python
def __init__(self, formula: str, meta_data_from: str = '', *, device: ComputingDevice | None = None, apply_to: list[str] | None = None, seed: int | None = None, error_on_unexpected_behaviour: bool | None = None) -> None
```

### `ImageMattingOperation`

```python
class ImageMattingOperation(Operation)
```

Refine edges of label-map based on the intensities of the input image.
This can make coarse predictions smoother or may correct wrong predictions on the boundaries.
It applies the method from the paper "Guided Image Filtering" by Kaiming He et al.

Args:
    img_size: target image dimension. No downsampling if 0.
    kernel_size: guided filter kernel size.
    epsilon: guided filter epsilon.
    num_iters: guided filter number of iterations.
    device: Specifies whether this Operation should run on CPU or GPU.
    seed: Specifies seeding for any randomness that might be contained in this operation.
    error_on_unexpected_behaviour: Specifies whether to throw an exception instead of warning about unexpected behavior.
    apply_to: Specifies fields in a DataItem that this Operation should process (if empty, will select suitable fields based on the current `~imfusion.machinelearning.Operation.processing_policy`)
    record_identifier: Unused for this operation as it is not invertible

#### Methods

##### `__init__`

```python
def __init__(self, *, device: ComputingDevice | None = None, apply_to: list[str] | None = None, seed: int | None = None, error_on_unexpected_behaviour: bool | None = None) -> None
```

##### `__init__`

```python
def __init__(self, img_size: int, kernel_size: int, epsilon: float, num_iters: int, *, device: ComputingDevice | None = None, apply_to: list[str] | None = None, seed: int | None = None, error_on_unexpected_behaviour: bool | None = None) -> None
```

### `ImageROISampler`

```python
class ImageROISampler(Operation)
```

Base class for ROI samplers

#### Methods

##### `available_cpp_samplers`

*staticmethod*

```python
def available_cpp_samplers() -> list[str]
```

Returns the list of registered C++ samplers.

##### `compute_roi`

```python
def compute_roi(self, image: imfusion.SharedImageSet) -> imfusion.RegionOfInterest | None
```

Compute ROI on the given image.

##### `extract_roi`

```python
def extract_roi(self, image: imfusion.SharedImageSet, roi: imfusion.RegionOfInterest | None) -> imfusion.SharedImageSet
```

Extract ROIs from an image.

##### `label_padding_mode`

*property*

```python
def label_padding_mode(self) -> imfusion.PaddingMode
```

The label padding mode property.

##### `label_padding_mode`

```python
def label_padding_mode(self, arg1: imfusion.PaddingMode) -> None
```

##### `padding_mode`

*property*

```python
def padding_mode(self) -> imfusion.PaddingMode
```

The image padding mode property.

##### `padding_mode`

```python
def padding_mode(self, arg1: imfusion.PaddingMode) -> None
```

##### `requires_label`

*property*

```python
def requires_label(self) -> bool
```

Bool indicating whether ROI must be computed on the label map.

### `ImageSamplerError`

```python
class ImageSamplerError(Exception)
```

### `ImagewiseClassificationMetrics`

```python
class ImagewiseClassificationMetrics(Metric)
```

Computes imagewise classification metrics.

This metric evaluates classification performance on entire images rather than individual pixels,
computing confusion matrices and overall classification accuracy.

#### Methods

##### `__init__`

```python
def __init__(self, num_classes: int = 2) -> None
```

Constructs an ImagewiseClassificationMetrics.

Args:
        num_classes: Number of classes in the classification task. Default: 2

##### `compute_results`

```python
def compute_results(self, prediction: imfusion.SharedImageSet, target: imfusion.SharedImageSet) -> list[ImagewiseClassificationMetrics.Result]
```

Computes classification metrics for the given prediction and target.

Args:
        prediction: The predicted class labels
        target: The target/ground truth class labels
Returns:
        Results: Object containing the prediction, target, and confusion matrix

#### Nested Classes

##### `Result`

```python
class Result
```

Results container for imagewise classification metric computations.

###### Methods

####### `confusion_matrix`

*property*

```python
def confusion_matrix(self) -> numpy.ndarray[numpy.float64[m, n]]
```

Confusion matrix for the classification results

####### `confusion_matrix`

```python
def confusion_matrix(self, arg0: numpy.ndarray[numpy.float64[m, n]]) -> None
```

####### `prediction`

*property*

```python
def prediction(self) -> int
```

Predicted class label

####### `prediction`

```python
def prediction(self, arg0: int) -> None
```

####### `target`

*property*

```python
def target(self) -> int
```

Target/ground truth class label

####### `target`

```python
def target(self, arg0: int) -> None
```

### `InverseOperation`

```python
class InverseOperation(Operation)
```

Operation that inverts a specific operation by using the InversionComponent.
        This operation provides a way to invert a specific operation by its record identifier.
        It retrieves the inverse operation specifications from the InversionComponent of
        the processed elements, creates an appropriate inverse operation, and then after
        successful processing, removes the inversion information.
        The process works as follows:
                
        1. The InverseOperation searches for elements with the InversionComponent matching the target identifier
        2. It creates and configures an operation based on these specifications
        3. It applies this inverse operation to the input
        4. After successful processing, it removes the inversion information from all processed elements. This guarantees LIFO order when operations with the same identifier are applied multiple times.

        Note: This inverts only operations that explicitly support inversion and that
        have recorded themselves with the specified record identifier. Inversions may not be be able to fully recover the input image,
        e.g. inverting a cropping operation yields a padded image, not the original image.

        Usage example:
         
        ```python

                # Apply a padding operation with a specific record identifier
                pad_op = PadOperation((10, 10), (10, 10), (0, 0))
                props = Properties({"record_identifier": "my padding"})
                pad_op.configure(props)
                padded_image = pad_op.process(some_input_image)

                # Create an inverse operation to undo the padding,
                # using the record_identifier as target identifier for inversion.
                inv_op = InverseOperation("my padding")
                unpadded_image = inv_op.process(padded_image)

        Note: The InverseOperation reuses the created inverse operation when possible,
        only creating a new one when the type changes, and only reconfiguring
        when the properties change. If element-specific properties are needed, they
        should be set by the Operation that is to be inverted in process() via data components
        and used in process() of the InverseOperation.

        Args:
                target_identifier: The identifier of the operation to invert. Default: ""

device: Specifies whether this Operation should run on CPU or GPU.
seed: Specifies seeding for any randomness that might be contained in this operation.
error_on_unexpected_behaviour: Specifies whether to throw an exception instead of warning about unexpected behavior.
apply_to: Specifies fields in a DataItem that this Operation should process (if empty, will select suitable fields based on the current `~imfusion.machinelearning.Operation.processing_policy`)
record_identifier: Unused for this operation as it is not invertible

#### Methods

##### `__init__`

```python
def __init__(self, *, device: ComputingDevice | None = None, apply_to: list[str] | None = None, seed: int | None = None, error_on_unexpected_behaviour: bool | None = None) -> None
```

##### `__init__`

```python
def __init__(self, target_identifier: str = '', *, device: ComputingDevice | None = None, apply_to: list[str] | None = None, seed: int | None = None, error_on_unexpected_behaviour: bool | None = None) -> None
```

### `InvertOperation`

```python
class InvertOperation(Operation)
```

Invert the intensities of the image: :math:`        extnormal{output} = -   extnormal{input}`.

Args:

    device: Specifies whether this Operation should run on CPU or GPU.
    seed: Specifies seeding for any randomness that might be contained in this operation.
    error_on_unexpected_behaviour: Specifies whether to throw an exception instead of warning about unexpected behavior.
    apply_to: Specifies fields in a DataItem that this Operation should process (if empty, will select suitable fields based on the current `~imfusion.machinelearning.Operation.processing_policy`)
    record_identifier: Unused for this operation as it is not invertible

#### Methods

##### `__init__`

```python
def __init__(self, *, device: ComputingDevice | None = None, apply_to: list[str] | None = None, seed: int | None = None, error_on_unexpected_behaviour: bool | None = None) -> None
```

##### `__init__`

```python
def __init__(self, *, device: ComputingDevice | None = None, apply_to: list[str] | None = None, seed: int | None = None, error_on_unexpected_behaviour: bool | None = None) -> None
```

### `InvertibleOperation`

```python
class InvertibleOperation(Operation)
```

Base class for operations that support inversion.

Basic Usage:

1. Inherit from InvertibleOperation
2. Implement the inverse_specs() method
3. Implement the DataElement-specific process methods (process_images(), process_points(), process_boxes()) (or override process(DataItem) and call super().process(item))
4. Use InverseOperation with the same identifier to create and apply the inverse

Implementation Patterns
-----------------------

**Pattern 1: Override process_images() (Recommended)**

```python

class PyImageIntensityScalingOperation(ml.InvertibleOperation):

    def __init_(self, scale_factor: float = 2.0):
        ml.InvertibleOperation.__init_(self, "PyImageIntensityScalingOperation", ml.Operation.ProcessingPolicy.EVERYTHING)
        self.scale_factor: float = scale_factor

    def process_images(self, images):
        """ Scale the images by the scale_factor """
        return ml.LinearIntensityMappingOperation(factor=self.scale_factor, bias=0.0).process(images)

    def configure(self, properties: Properties) -> bool:
        """
        Configure this Operation. Since the inverse of this operation is itself with inverted parameter,
        this method is used to set up the inverse operation parameters.
        """
        params = properties.asdict()
        if "scale_factor" in params:
            self.scale_factor = params["scale_factor"]
            properties.remove_param("scale_factor")
        return super().configure(properties)

    def inverse_specs(self):
        """ Return specs for this operation with inverse parameters """
        props = Properties({"scale_factor": (1.0 / self.scale_factor) if self.scale_factor != 0 else np.nan})
        return ml.Operation.Specs("PyImageIntensityScalingOperation", props, ml.Phase.ALWAYS)

```
**Pattern 2: Override process(DataItem) (Advanced)**

```python

class PyDataItemIntensityScalingOperation(ml.InvertibleOperation):
    def __init_(self, scale_factor: float = 2.0):
        ml.InvertibleOperation.__init_(self, "PyDataItemIntensityScalingOperation", ml.Operation.ProcessingPolicy.EVERYTHING)
        self.scale_factor: float = scale_factor

    def process(self, input: Union[ml.DataItem, imf.SharedImageSet]) -> Optional[imf.SharedImageSet]:
        """ Record this operation for inversion (in this case, before running the actual transformation),
        and apply the actual transformation.

        Due to overloaded process() method, the input can be either a DataItem or a SharedImageSet.
        In the case of a DataItem, the operation is applied inplace.
        """
        # record the operation for inversion:
        ret = super().process(input)
        # apply the actual transformation:
        if isinstance(input, ml.DataItem):
            assert ret is None, f"DataItem is not expected to be returned, but got {ret}"
            ml.LinearIntensityMappingOperation(
                factor=self.scale_factor).process(input)
        elif isinstance(input, imf.SharedImageSet):
            return ml.LinearIntensityMappingOperation(
                factor=self.scale_factor).process(ret)
        else:
            raise ValueError(f"Invalid input type: {type(input)}")

    def process_images(self, sis: imf.SharedImageSet) -> imf.SharedImageSet:
        """ Pass-through method to define the compatible data element.
        The recording and inversion happen in the InvertibleOperation.process() call.
        """
        return sis

    def inverse_specs(self) -> ml.Operation.Specs:
        """ Return specs for an operation that can perform the inverse
        Note: The inverse operation can be any registered operation, not necessarily this class
        """
        props = Properties({"factor": (1.0 / self.scale_factor) if self.scale_factor != 0 else np.nan, "bias": 0.})
        return ml.Operation.Specs("LinearIntensityMapping", props, ml.Phase.ALWAYS)

```
Complete Workflow Example
-------------------------

```python

# Create forward operation
forward_op = PyImageIntensityScalingOperation(scale_factor=2.0)
forward_op.record_identifier = "scale_2x" # this is required for inversion information to be stored

# Apply forward transformation
forward_op.process(data_item)

# Create and apply inverse operation
inverse_op = ml.InverseOperation("scale_2x") # inversion information is retrieved from the target record_identifier
inverse_op.process(data_item)  # Undoes the scaling

```
How It Works Internally
-----------------------

- When process() is called, InvertibleOperation records operation details in an InversionComponent
- The InversionComponent stores the operation name and configuration needed for inversion
- InverseOperation uses this recorded information plus your inverse_specs() to create the inverse
- The system supports both Python operations inverting themselves and delegating to other operations
- If needed, data-specific inversion information may be attached to the DataElement in the forward operation so it can be used by the specified inverse operation

#### Methods

##### `__call__`

```python
def __call__(self, item: DataItem) -> None
```

Execute the operation on the input DataItem in-place, i.e. the input item will be modified.

##### `__call__`

```python
def __call__(self, images: imfusion.SharedImageSet, in_place: bool = False) -> imfusion.SharedImageSet
```

Execute the operation on the input images and returns its output.
Args:
        images (SharedImageSet): the input images.
        in_place (bool): If False, the input is guaranteed to be unchanged and the function will return a new object. If True, the input will be changed and the function will return it. (Default: False).

##### `__call__`

```python
def __call__(self, points: KeypointSet, in_place: bool = False) -> KeypointSet
```

Execute the operation on the input keypoints. The output will always be a different set of keypoints, i.e. this function never works in-place.
Args:
        points (SharedImageSet): the input points.
        in_place (bool): if True, the input will be changed and the function will return it. If False, the input is guaranteed to be unchanged and the function will return a new object (Default: False).

##### `__call__`

```python
def __call__(self, boxes: BoundingBoxSet, in_place: bool = False) -> BoundingBoxSet
```

Execute the operation on the input bounding boxes. The output will always be a different set of bounding boxes, i.e. this function never works in-place.
Args:
        boxes (SharedImageSet): the input boxes.
        in_place (bool): If False, the input is guaranteed to be unchanged and the function will return a new object. If True, the input will be changed and the function will return it. (Default: False).

##### `__init__`

```python
def __init__(self, name: str, processing_policy: Operation.ProcessingPolicy, *, device: ComputingDevice | None = None, apply_to: list[str] | None = None, seed: int | None = None, error_on_unexpected_behaviour: bool | None = None) -> None
```

##### `active_fields`

*property*

```python
def active_fields(self) -> set[str] | None
```

Fields in the data item that this operation will process.

##### `active_fields`

```python
def active_fields(self, arg1: set[str] | None) -> None
```

##### `computing_device`

*property*

```python
def computing_device(self) -> ComputingDevice
```

The computing device property.

##### `computing_device`

```python
def computing_device(self, arg1: ComputingDevice) -> None
```

##### `configuration`

```python
def configuration(self) -> imfusion.Properties
```

Returns the current configuration as a Properties object

##### `configure`

```python
def configure(self, properties: imfusion.Properties) -> bool
```

Configures the operation with the given properties

##### `does_not_modify_input`

*property*

```python
def does_not_modify_input(self) -> bool
```

Returns True if the operation guarantees not to modify the input data

##### `error_on_unexpected_behaviour`

*property*

```python
def error_on_unexpected_behaviour(self) -> bool
```

Treat unexpected behaviour warnings as errors.

##### `error_on_unexpected_behaviour`

```python
def error_on_unexpected_behaviour(self, arg1: bool) -> None
```

##### `name`

*property*

```python
def name(self) -> str
```

The name of the operation

##### `process`

```python
def process(self, item: DataItem) -> None
```

Execute the operation on the input DataItem in-place, i.e. the input item will be modified.

##### `process`

```python
def process(self, images: imfusion.SharedImageSet, in_place: bool = False) -> imfusion.SharedImageSet
```

Execute the operation on the input images and returns its output.
Args:
        images (SharedImageSet): the input images.
        in_place (bool): If False, the input is guaranteed to be unchanged and the function will return a new object. If True, the input will be changed and the function will return it. (Default: False).

##### `process`

```python
def process(self, points: KeypointSet, in_place: bool = False) -> KeypointSet
```

Execute the operation on the input keypoints. The output will always be a different set of keypoints, i.e. this function never works in-place.
Args:
        points (SharedImageSet): the input points.
        in_place (bool): if True, the input will be changed and the function will return it. If False, the input is guaranteed to be unchanged and the function will return a new object (Default: False).

##### `process`

```python
def process(self, boxes: BoundingBoxSet, in_place: bool = False) -> BoundingBoxSet
```

Execute the operation on the input bounding boxes. The output will always be a different set of bounding boxes, i.e. this function never works in-place.
Args:
        boxes (SharedImageSet): the input boxes.
        in_place (bool): If False, the input is guaranteed to be unchanged and the function will return a new object. If True, the input will be changed and the function will return it. (Default: False).

##### `processing_policy`

*property*

```python
def processing_policy(self) -> Operation.ProcessingPolicy
```

The processing_policy property. Resetting it overrides the default operation behaviour on label.

##### `processing_policy`

```python
def processing_policy(self, arg1: Operation.ProcessingPolicy) -> None
```

##### `record_identifier`

*property*

```python
def record_identifier(self) -> str
```

Identifier used to record this operation for inversion. Setting this enables inversion if the operation supports it.

##### `record_identifier`

```python
def record_identifier(self, arg1: str) -> None
```

##### `seed`

*property*

```python
def seed(self) -> int | None
```

Random seed for operations with randomness

##### `seed`

```python
def seed(self, arg1: int) -> None
```

##### `seed_random_engine`

```python
def seed_random_engine(self, seed: int) -> None
```

Seeds the random number generator for this operation

##### `supports_inversion`

*property*

```python
def supports_inversion(self) -> bool
```

Returns whether this operation supports inversion.

### `KeepLargestComponentOperation`

```python
class KeepLargestComponentOperation(Operation)
```

Create a label map with the largest components above the specified threshold.
The output label map encodes each component with a different label value (1 for the largest, 2 for the second largest, etc.).
Input images may be float or integer, output are unsigned 8-bit integer images (i.e. max 255 components).
The operation will automatically set the default processing policy based on its input (if the input contains more than than one image, then only the label maps will be processed).

Args:
    max_number_components: the maximum number of components to keep. Default: 1
    min_component_size: the minimum size of a component to keep. Default: -1, i.e. no minimum
    max_component_size: the maximum size of a component to keep Default: -1, i.e. no maximum
    threshold: the threshold to use for the binarization. Default: 0.5
    device: Specifies whether this Operation should run on CPU or GPU.
    seed: Specifies seeding for any randomness that might be contained in this operation.
    error_on_unexpected_behaviour: Specifies whether to throw an exception instead of warning about unexpected behavior.
    apply_to: Specifies fields in a DataItem that this Operation should process (if empty, will select suitable fields based on the current `~imfusion.machinelearning.Operation.processing_policy`)
    record_identifier: Unused for this operation as it is not invertible

#### Methods

##### `__init__`

```python
def __init__(self, *, device: ComputingDevice | None = None, apply_to: list[str] | None = None, seed: int | None = None, error_on_unexpected_behaviour: bool | None = None) -> None
```

##### `__init__`

```python
def __init__(self, max_number_components: int = 1, min_component_size: int = -1, max_component_size: int = -1, threshold: float = 0.5, *, device: ComputingDevice | None = None, apply_to: list[str] | None = None, seed: int | None = None, error_on_unexpected_behaviour: bool | None = None) -> None
```

### `KeypointSet`

```python
class KeypointSet(imfusion.Data)
```

Class for managing sets of keypoints

The class is meant to be used in parallel with SharedImageSet.
For each frame in the set, and for each type of keypoint (i.e. body, pedicles, etc..),
there is a list of points indicating an instance of that type in the reference image.
In terms of tensor dimensions, this would be represented as [N, C, K],
where N is the batch size, C is the number of channels (i.e. types of keypoints),
and K is the number of keypoints for the same instance type.
Each Keypoint is a vec3 having a further dimension [3].

Note:
    This class API is experimental and might change soon.

#### Methods

##### `__init__`

```python
def __init__(self, points: list[list[list[numpy.ndarray[numpy.float64[3, 1]]]]]) -> None
```

Initialize a KeypointSet from a nested vector of vec3 points.

Args:
        points: 4D nested vector [N, C, K, 3] where N=batch size, C=channels (keypoint types), K=keypoints per type, 3=coordinates

##### `__init__`

```python
def __init__(self, points: list[list[list[list[float]]]]) -> None
```

Initialize a KeypointSet from a nested list of points.

Args:
        points: 4D nested list [[[[x,y,z]]]] where each innermost list has 3 coordinates

##### `__init__`

```python
def __init__(self, array: numpy.ndarray[numpy.float64]) -> None
```

Initialize a KeypointSet from a numpy array.

Args:
        array: Numpy array with shape [N, C, K, 3] where N=batch size, C=channels, K=keypoints per channel, 3=coordinates

##### `data`

*property*

```python
def data(self) -> list[list[list[numpy.ndarray[numpy.float64[3, 1]]]]]
```

The keypoint data stored as a 4D nested vector [N, C, K, 3] where N=batch size, C=channels, K=number of keypoints per channel, and 3=coordinates (x,y,z)

##### `data`

```python
def data(self, arg0: list[list[list[numpy.ndarray[numpy.float64[3, 1]]]]]) -> None
```

##### `load`

*staticmethod*

```python
def load(location: str | os.PathLike) -> KeypointSet | None
```

Load a KeypointSet from an ImFusion file.

Args:
        location: input path.

##### `save`

```python
def save(self, location: str | os.PathLike) -> None
```

Save a KeypointSet as an ImFusion file.

Args:
        location: output path.

### `KeypointsElement`

```python
class KeypointsElement(DataElement)
```

DataElement for storing and processing keypoint annotations.

KeypointsElement wraps a KeypointSet to represent 3D keypoint annotations
in ML pipelines. Keypoints are commonly used for anatomical landmarks,
pose estimation, or other spatial point annotations.

#### Methods

##### `__init__`

```python
def __init__(self, keypoint_set: KeypointSet) -> None
```

Initialize a KeypointsElement.

Args:
        keypoint_set: In case the argument is a numpy array, the array shape is expected to be [N, C, K, 3], where N is the batch size,
                C the number of different keypoint types (channel), K the number of instances of the same point type, which are
                expected to have dimension 3. If the argument is a nested list, the same concept applies also to the size of each level of nesting.

##### `keypoints`

*property*

```python
def keypoints(self) -> KeypointSet
```

Access to the underlying KeypointSet.

##### `keypoints`

```python
def keypoints(self, arg1: KeypointSet) -> None
```

### `KeypointsFromBlobsOperation`

```python
class KeypointsFromBlobsOperation(Operation)
```

Extracts keypoints from blob image.
Takes ImageElement specified in :code:'apply_to' as input.
If :code:'apply_to' is not specified and there is only one image in the data item, this image will automatically be selected.

Args:
    keypoints_field_name: Field name of the output keypoints. Default: "keypoints"
    keypoint_extraction_mode: Extraction mode: 0: Max, 1: Mean, 2: Local Max. Default: 0
    blob_intensity_cutoff: Minimum blob intensity to be considered in analysis. Default: 0.02
    min_cluster_distance: In case of local aggregation methods, minimum distance allowed among clusters. Default: 10.0
    min_cluster_weight: In case of local aggregation methods, minimum intensity for cluster to be consider independent. Default: 0.1
    max_internal_clusters: In case of local aggregation methods, maximum number of internal clusters to be considered;
                                                    to avoid excessive numbers that stall the algorithm. If there are more, the lower weighted ones are removed first. Default: 1000
    run_smoothing: Runs a Gaussian smoothing with 1 pixel standard deviation to improve stability of local maxima. Default: False
    smoothing_half_kernel: Runs a Gaussian smoothing with 1 pixel standard deviation to improve stability of local maxima. Default: 2
    run_intensity_based_refinement: Runs blob intensity based refinement of clustered keypoints. Default: False
    apply_to: Field containing the blob image.
                                                    If not specified and if there is only one image in the data item, this image will automatically be selected. Default: []
    device: Specifies whether this Operation should run on CPU or GPU.
    seed: Specifies seeding for any randomness that might be contained in this operation.
    error_on_unexpected_behaviour: Specifies whether to throw an exception instead of warning about unexpected behavior.
    record_identifier: Unused for this operation as it is not invertible

#### Methods

##### `__init__`

```python
def __init__(self, *, device: ComputingDevice | None = None, seed: int | None = None, error_on_unexpected_behaviour: bool | None = None) -> None
```

##### `__init__`

```python
def __init__(self, keypoints_field_name: str = 'keypoints', keypoint_extraction_mode: int = 0, blob_intensity_cutoff: float = 0.02, min_cluster_distance: float = 10.0, min_cluster_weight: float = 0.1, max_internal_clusters: int = 1000, run_smoothing: bool = False, smoothing_half_kernel: int = 2, run_intensity_based_refinement: bool = False, apply_to: list[str] = [], *, device: ComputingDevice | None = None, seed: int | None = None, error_on_unexpected_behaviour: bool | None = None) -> None
```

### `LabelROISampler`

```python
class LabelROISampler(ImageROISampler)
```

Sampler which samples ROIs from the input image and label map, such that one particular label appears.
For each ROI, one of the `labels_values` will be selected and the sampler will make sure that the ROI includes this label.
If the `sample_boundaries_only` flag is set to true, regions will at least have two different label values.
If the constraints are not feasible, the sampler will either extract a random ROI with the target size or return an empty image, based on the flag `fallback_to_random`.
(The actual purpose of returning an empty image is to actually chain this sampler with a FilterDataLoader, so that images without a valid label are just completely skipped).

Args:
    roi_size: Target size of the ROIs to be extracted as [Width, Height, Slices]
    labels_values: List of integers representing the target labels
    sample_boundaries_only: Make sure that the ROI contains a boundary (i.e. at least two different label values)
    fallback_to_random: Whether to sample a random ROI or return an empty one when the target label values are not found. Default: True
    device: Specifies whether this Operation should run on CPU or GPU.
    seed: Specifies seeding for any randomness that might be contained in this operation.
    error_on_unexpected_behaviour: Specifies whether to throw an exception instead of warning about unexpected behavior.
    apply_to: Specifies fields in a DataItem that this Operation should process (if empty, will select suitable fields based on the current `~imfusion.machinelearning.Operation.processing_policy`)
    record_identifier: Unused for this operation as it is not invertible

#### Methods

##### `__init__`

```python
def __init__(self, *, device: ComputingDevice | None = None, apply_to: list[str] | None = None, seed: int | None = None, error_on_unexpected_behaviour: bool | None = None) -> None
```

##### `__init__`

```python
def __init__(self, roi_size: numpy.ndarray[numpy.int32[3, 1]], labels_values: list[int], sample_boundaries_only: bool, fallback_to_random: bool = True, *, device: ComputingDevice | None = None, apply_to: list[str] | None = None, seed: int | None = None, error_on_unexpected_behaviour: bool | None = None) -> None
```

### `LinearIntensityMappingOperation`

```python
class LinearIntensityMappingOperation(Operation)
```

Apply a linear shift and scale to the image intensities.
:math:`     extnormal{output} =     extnormal{factor} *     extnormal{input} +      extnormal{bias}`

Args:
    factor: Multiplying factor (see formula). Default: 1.0
    bias: Additive bias (see formula). Default: 0.0
    device: Specifies whether this Operation should run on CPU or GPU.
    seed: Specifies seeding for any randomness that might be contained in this operation.
    error_on_unexpected_behaviour: Specifies whether to throw an exception instead of warning about unexpected behavior.
    apply_to: Specifies fields in a DataItem that this Operation should process (if empty, will select suitable fields based on the current `~imfusion.machinelearning.Operation.processing_policy`)
    record_identifier: Unused for this operation as it is not invertible

#### Methods

##### `__init__`

```python
def __init__(self, *, device: ComputingDevice | None = None, apply_to: list[str] | None = None, seed: int | None = None, error_on_unexpected_behaviour: bool | None = None) -> None
```

##### `__init__`

```python
def __init__(self, factor: float = 1.0, bias: float = 0.0, *, device: ComputingDevice | None = None, apply_to: list[str] | None = None, seed: int | None = None, error_on_unexpected_behaviour: bool | None = None) -> None
```

### `MRIBiasFieldCorrectionOperation`

```python
class MRIBiasFieldCorrectionOperation(Operation)
```

Perform bias field correction using an implicitly trained neural network (see MRIBiasFieldCorrectionAlgorithm for more details and the parameters description).

Args:
    iterations: For values &gt; 1, the field is iteratively refined. Default: 1
    config_path: Path of the machine learning model (use "GENERIC3D" or "GENERIC2D" for the default models). Default: "GENERIC3D"
    field_smoothing_half_kernel: For values &gt; 0, additional smoothing with a Gaussian kernel. Default: -1
    preserve_mean_intensity: Preserve the mean image intensity in the output. Default: True
    output_is_field: Produce the field, not the corrected image. Default: False
    field_dimensions: Internal field dimensions (zeroes represent the model default dimensions). Default: [0, 0, 0]
    device: Specifies whether this Operation should run on CPU or GPU.
    seed: Specifies seeding for any randomness that might be contained in this operation.
    error_on_unexpected_behaviour: Specifies whether to throw an exception instead of warning about unexpected behavior.
    apply_to: Specifies fields in a DataItem that this Operation should process (if empty, will select suitable fields based on the current `~imfusion.machinelearning.Operation.processing_policy`)
    record_identifier: Unused for this operation as it is not invertible

#### Methods

##### `__init__`

```python
def __init__(self, *, device: ComputingDevice | None = None, apply_to: list[str] | None = None, seed: int | None = None, error_on_unexpected_behaviour: bool | None = None) -> None
```

##### `__init__`

```python
def __init__(self, iterations: int = 1, config_path: str = 'GENERIC3D', field_smoothing_half_kernel: int = -1, preserve_mean_intensity: bool = True, output_is_field: bool = False, field_dimensions: numpy.ndarray[numpy.int32[3, 1]] = ..., *, device: ComputingDevice | None = None, apply_to: list[str] | None = None, seed: int | None = None, error_on_unexpected_behaviour: bool | None = None) -> None
```

### `MRIBiasFieldGenerationOperation`

```python
class MRIBiasFieldGenerationOperation(Operation)
```

Apply or generate a multiplicative intensity modulation field.
If the output is a field, it is shifted as close to mean 1 as possible while remaining positive everywhere.
If the output is not a field, the image intensity is shifted so that the mean intensity of the input image is preserved.

Args:
    length_scale_mm: Length scale (in mm) of the Gaussian radial basis function. Default: 100.0
    field_amplitude: Total field amplitude (centered around one). I.e. 0.4 for a 40% field. Default: 0.4
    center: Relative center of the Gaussian with respect to the image axes. Values from [0..1] for locations inside the image. Default: [0.25, 0.25, 0.25]
    distance_scaling: Relative scaling of the x, y, z world coordinates for field anisotropy. Default: [1, 1, 1]
    invert_field: Invert the final field: field &lt;- 2 - field. Default: False
    output_is_field: Produce the field, not the corrupted image. Note, the additive normalization method depends on this. Default: False
    device: Specifies whether this Operation should run on CPU or GPU.
    seed: Specifies seeding for any randomness that might be contained in this operation.
    error_on_unexpected_behaviour: Specifies whether to throw an exception instead of warning about unexpected behavior.
    apply_to: Specifies fields in a DataItem that this Operation should process (if empty, will select suitable fields based on the current `~imfusion.machinelearning.Operation.processing_policy`)
    record_identifier: Unused for this operation as it is not invertible

#### Methods

##### `__init__`

```python
def __init__(self, *, device: ComputingDevice | None = None, apply_to: list[str] | None = None, seed: int | None = None, error_on_unexpected_behaviour: bool | None = None) -> None
```

##### `__init__`

```python
def __init__(self, length_scale_mm: float = 100.0, field_amplitude: float = 0.4, center: numpy.ndarray[numpy.float64[3, 1]] = ..., distance_scaling: numpy.ndarray[numpy.float64[3, 1]] = ..., invert_field: bool = False, output_is_field: bool = False, *, device: ComputingDevice | None = None, apply_to: list[str] | None = None, seed: int | None = None, error_on_unexpected_behaviour: bool | None = None) -> None
```

### `MachineLearningModel`

```python
class MachineLearningModel
```

Class for creating a MachineLearningModel.

#### Methods

##### `__call__`

```python
def __call__(self, input: DataItem) -> DataItem
```

Method to execute a generic multiple input/multiple output model
The input and output type of a machine learning model is the DataItem, which allows
to give and retrieve an heterogeneous map-type container of the data needed and returned by the model.

Args:
        input (DataItem): Input data item containing all data to be used for inference

##### `__call__`

```python
def __call__(self, images: imfusion.SharedImageSet) -> imfusion.SharedImageSet
```

Convenience method to execute a single-input/single-output image-based model.

Args:
        images (SharedImageSet): Input image set to be used for inference

##### `__init__`

```python
def __init__(self, config_path: str | os.PathLike, default_prediction_output: PredictionOutput = PredictionOutput.UNKNOWN) -> None
```

Create a MachineLearningModel.
If the resource required by the MachineLearningModel could not be acquired, raises a RuntimeError.

Args:
        config_path: Path to the configuration file used to create ModelConfiguration object owned by the model.
        default_prediction_output: Parameter used to specify the prediction output of a model if this is missing from the config file.
                The prediction output type must be specified either here or in the configuration file under the key `PredictionOutput`.
                If it is specified in both places, the one from the config file is used.

##### `engine`

```python
def engine(self) -> Engine
```

Returns the underlying engine used by the model.
This can be useful for setting CPU/GPU mode, querying whether CUDA is available, etc.

##### `label_names`

*property*

```python
def label_names(self) -> dict[str, list[str]]
```

Dict of the list of label names for each output. Keys are the engine output names if specified, else "Prediction".

##### `predict`

```python
def predict(self, input: DataItem) -> DataItem
```

Method to execute a generic multiple input/multiple output model
The input and output type of a machine learning model is the DataItem, which allows
to give and retrieve an heterogeneous map-type container of the data needed and returned by the model.

Args:
        input (DataItem): Input data item containing all data to be used for inference

##### `predict`

```python
def predict(self, images: imfusion.SharedImageSet) -> imfusion.SharedImageSet
```

Convenience method to execute a single-input/single-output image-based model.

Args:
        images (SharedImageSet): Input image set to be used for inference

### `MakeFloatOperation`

```python
class MakeFloatOperation(Operation)
```

Convert the input image to float with original values (internal shifts and scales are baked in).

Args:

    device: Specifies whether this Operation should run on CPU or GPU.
    seed: Specifies seeding for any randomness that might be contained in this operation.
    error_on_unexpected_behaviour: Specifies whether to throw an exception instead of warning about unexpected behavior.
    apply_to: Specifies fields in a DataItem that this Operation should process (if empty, will select suitable fields based on the current `~imfusion.machinelearning.Operation.processing_policy`)
    record_identifier: Unused for this operation as it is not invertible

#### Methods

##### `__init__`

```python
def __init__(self, *, device: ComputingDevice | None = None, apply_to: list[str] | None = None, seed: int | None = None, error_on_unexpected_behaviour: bool | None = None) -> None
```

##### `__init__`

```python
def __init__(self, *, device: ComputingDevice | None = None, apply_to: list[str] | None = None, seed: int | None = None, error_on_unexpected_behaviour: bool | None = None) -> None
```

### `MarkAsTargetOperation`

```python
class MarkAsTargetOperation(Operation)
```

Mark elements from the input data item as learning "target" which might affect the behaviour of the subsequent operations
that rely on `~imfusion.machinelearning.Operation.ProcessingPolicy` or use other custom target-specific logic.

Args:
    apply_to: fields to mark as targets (will initialize the underlying `apply_to` parameter)
    device: Specifies whether this Operation should run on CPU or GPU.
    seed: Specifies seeding for any randomness that might be contained in this operation.
    error_on_unexpected_behaviour: Specifies whether to throw an exception instead of warning about unexpected behavior.
    record_identifier: Unused for this operation as it is not invertible

#### Methods

##### `__init__`

```python
def __init__(self, *, device: ComputingDevice | None = None, seed: int | None = None, error_on_unexpected_behaviour: bool | None = None) -> None
```

##### `__init__`

```python
def __init__(self, apply_to: list[str], *, device: ComputingDevice | None = None, seed: int | None = None, error_on_unexpected_behaviour: bool | None = None) -> None
```

### `MergeAsChannelsOperation`

```python
class MergeAsChannelsOperation(Operation)
```

Merge multiple DataElements into a single one along the channel dimension.
Only applicable for ImageElements and VectorElements.

Args:
    apply_to: fields which should be merged.
    output_field: name of the resulting field.
    remove_fields: remove fields used for merging from the data item. Default: True
    device: Specifies whether this Operation should run on CPU or GPU.
    seed: Specifies seeding for any randomness that might be contained in this operation.
    error_on_unexpected_behaviour: Specifies whether to throw an exception instead of warning about unexpected behavior.
    record_identifier: Unused for this operation as it is not invertible

#### Methods

##### `__init__`

```python
def __init__(self, *, device: ComputingDevice | None = None, seed: int | None = None, error_on_unexpected_behaviour: bool | None = None) -> None
```

##### `__init__`

```python
def __init__(self, apply_to: list[str] = [], output_field: str = '', remove_fields: bool = True, *, device: ComputingDevice | None = None, seed: int | None = None, error_on_unexpected_behaviour: bool | None = None) -> None
```

### `Metric`

```python
class Metric
```

Base class for computing metrics on machine learning predictions.

Metrics are used to evaluate model performance by comparing predictions to ground truth targets.

#### Methods

##### `__call__`

```python
def __call__(self, item: DataItem) -> list[dict[str, numpy.ndarray[numpy.float64[m, n]]]]
```

Computes the metric on a DataItem containing predictions and targets.

Args:
        item: DataItem containing the prediction and target data
Returns:
        dict: Dictionary with metric results

##### `compute`

```python
def compute(self, item: DataItem) -> list[dict[str, numpy.ndarray[numpy.float64[m, n]]]]
```

Computes the metric on a DataItem containing predictions and targets.

Args:
        item: DataItem containing the prediction and target data
Returns:
        dict: Dictionary with metric results

##### `configuration`

```python
def configuration(self) -> imfusion.Properties
```

Returns the current configuration of the metric.

Returns:
        Properties: Current configuration properties

##### `configure`

```python
def configure(self, properties: imfusion.Properties) -> None
```

Configures the metric with the given properties.

Args:
        properties: Configuration properties for the metric

##### `data_scheme`

*property*

```python
def data_scheme(self) -> dict[str, ElementType]
```

Returns the required data scheme for this metric.

The data scheme specifies which fields in the DataItem are required for computing the metric.

### `MetricException`

```python
class MetricException(Exception)
```

### `ModelConfiguration`

```python
class ModelConfiguration
```

Configuration class for `MachineLearningModel` parameters.

This class parses YAML configuration files and validates their consistency.
It supports versioned configurations to maintain API compatibility.

Version Management:
- The `p_version` parameter tracks the configuration format version at the time the model was created.
- When changes to the ModelConfiguration class API are made, `VERSION_COUNT` is incremented.
- Older configurations are automatically upgraded to the latest version.
- Use the `save` function to convert configurations to the latest version.

#### Attributes

##### `VERSION_COUNT`

```python
VERSION_COUNT: typing.ClassVar[int] = 8
```

#### Methods

##### `__init__`

```python
def __init__(self, config_path: str, default_prediction_output: PredictionOutput = PredictionOutput.UNKNOWN) -> None
```

Create a ModelConfiguration.
If the resource required by the ModelConfiguration could not be acquired, raises a RuntimeError.
Args:
        config_path: Path to the YAML configuration file used to create ModelConfiguration object.
        default_prediction_output: type of prediction output, can be [Image, Vector, Keypoints, BoundingBoxes, Tensor]. For legacy configuration (Version &lt; 3) this parameter has to be given programmatically.

##### `compare_with`

```python
def compare_with(self, other: ModelConfiguration, ignore_version: bool = False) -> bool
```

Compare this configuration with another ModelConfiguration.

This method performs a deep comparison of all configuration parameters
between this instance and the provided configuration.

Args:
        other (ModelConfiguration): The configuration to compare against.
        ignore_version (bool, optional): If True, version differences are ignored during comparison. Defaults to False.
        
Returns:
        bool: True if the configurations are identical, False otherwise.

##### `save`

```python
def save(self, config_path: str) -> bool
```

Save the ModelConfiguration to a file.
Note: This can be useful for converting an old configuration to the latest version.
Args:
        config_path: Path to the configuration file used to save the ModelConfiguration.

##### `version`

*property*

```python
def version(self) -> int
```

Version of the model configuration.

### `MorphologicalFilterOperation`

```python
class MorphologicalFilterOperation(Operation)
```

Runs a morphological operation on the input.

Args:
    mode: name of the operation in ['dilation', 'erosion', 'opening', 'closing']
    op_size: size of the structuring element
    use_l1_distance: flag to use L1 (absolute) or L2 (squared) distance in the local computations
    device: Specifies whether this Operation should run on CPU or GPU.
    seed: Specifies seeding for any randomness that might be contained in this operation.
    error_on_unexpected_behaviour: Specifies whether to throw an exception instead of warning about unexpected behavior.
    apply_to: Specifies fields in a DataItem that this Operation should process (if empty, will select suitable fields based on the current `~imfusion.machinelearning.Operation.processing_policy`)
    record_identifier: Unused for this operation as it is not invertible

#### Methods

##### `__init__`

```python
def __init__(self, *, device: ComputingDevice | None = None, apply_to: list[str] | None = None, seed: int | None = None, error_on_unexpected_behaviour: bool | None = None) -> None
```

##### `__init__`

```python
def __init__(self, mode: str, op_size: int, use_l1_distance: bool, *, device: ComputingDevice | None = None, apply_to: list[str] | None = None, seed: int | None = None, error_on_unexpected_behaviour: bool | None = None) -> None
```

### `NormalizeMADOperation`

```python
class NormalizeMADOperation(Operation)
```

Normalize the input image based on robust statistics.
The image is shifted so that the median corresponds to 0 and normalized with the median absolute deviation (see https://en.wikipedia.org/wiki/Median_absolute_deviation).
The operation is performed channel-wise.

Args:
    selected_channels: channels selected for MAD normalization. If empty, all channels are normalized (default).
    device: Specifies whether this Operation should run on CPU or GPU.
    seed: Specifies seeding for any randomness that might be contained in this operation.
    error_on_unexpected_behaviour: Specifies whether to throw an exception instead of warning about unexpected behavior.
    apply_to: Specifies fields in a DataItem that this Operation should process (if empty, will select suitable fields based on the current `~imfusion.machinelearning.Operation.processing_policy`)
    record_identifier: Unused for this operation as it is not invertible

#### Methods

##### `__init__`

```python
def __init__(self, *, device: ComputingDevice | None = None, apply_to: list[str] | None = None, seed: int | None = None, error_on_unexpected_behaviour: bool | None = None) -> None
```

##### `__init__`

```python
def __init__(self, selected_channels: list[int] = [], *, device: ComputingDevice | None = None, apply_to: list[str] | None = None, seed: int | None = None, error_on_unexpected_behaviour: bool | None = None) -> None
```

### `NormalizeNormalOperation`

```python
class NormalizeNormalOperation(Operation)
```

Normalize the input image so that it has a zero-mean and a unit-standard deviation.
A particular intensity value can be set to be ignored during the computations.

Args:
    keep_background: Should ignore all intensities with `background_value`. Default: False
    background_value: Intensity value to be potentially ignored. Default: 0.0
    device: Specifies whether this Operation should run on CPU or GPU.
    seed: Specifies seeding for any randomness that might be contained in this operation.
    error_on_unexpected_behaviour: Specifies whether to throw an exception instead of warning about unexpected behavior.
    apply_to: Specifies fields in a DataItem that this Operation should process (if empty, will select suitable fields based on the current `~imfusion.machinelearning.Operation.processing_policy`)
    record_identifier: Unused for this operation as it is not invertible

#### Methods

##### `__init__`

```python
def __init__(self, *, device: ComputingDevice | None = None, apply_to: list[str] | None = None, seed: int | None = None, error_on_unexpected_behaviour: bool | None = None) -> None
```

##### `__init__`

```python
def __init__(self, keep_background: bool = False, background_value: float = 0, *, device: ComputingDevice | None = None, apply_to: list[str] | None = None, seed: int | None = None, error_on_unexpected_behaviour: bool | None = None) -> None
```

### `NormalizePercentileOperation`

```python
class NormalizePercentileOperation(Operation)
```

Normalize the input image based on its intensity distribution, in particular on a lower and upper percentile.
The output image is not guaranteed to be in [0;1] but the lower percentile will be mapped to 0 and the upper one to 1.

Args:
    min_percentile: Lower percentile in [0;1]. Default: 0.0
    max_percentile: Lower percentile in [0;1], Default: 1.0
    clamp_values: Intensities are clipped to the new range. Default: False
    ignore_zeros: Whether to ignore zeros when computing the percentiles. Default: False
    device: Specifies whether this Operation should run on CPU or GPU.
    seed: Specifies seeding for any randomness that might be contained in this operation.
    error_on_unexpected_behaviour: Specifies whether to throw an exception instead of warning about unexpected behavior.
    apply_to: Specifies fields in a DataItem that this Operation should process (if empty, will select suitable fields based on the current `~imfusion.machinelearning.Operation.processing_policy`)
    record_identifier: Unused for this operation as it is not invertible

#### Methods

##### `__init__`

```python
def __init__(self, *, device: ComputingDevice | None = None, apply_to: list[str] | None = None, seed: int | None = None, error_on_unexpected_behaviour: bool | None = None) -> None
```

##### `__init__`

```python
def __init__(self, min_percentile: float = 0.0, max_percentile: float = 1.0, clamp_values: bool = False, ignore_zeros: bool = False, *, device: ComputingDevice | None = None, apply_to: list[str] | None = None, seed: int | None = None, error_on_unexpected_behaviour: bool | None = None) -> None
```

### `NormalizeUniformOperation`

```python
class NormalizeUniformOperation(Operation)
```

Normalize the input image based so their minimum/maximum intensity so that the output image has a [min; max] range.
The operation is performed channel-wise.

Args:
    min: New minimum value of the image after normalization. Default: 0.0
    max: New maximum value of the image after normalization. Default: 1.0
    device: Specifies whether this Operation should run on CPU or GPU.
    seed: Specifies seeding for any randomness that might be contained in this operation.
    error_on_unexpected_behaviour: Specifies whether to throw an exception instead of warning about unexpected behavior.
    apply_to: Specifies fields in a DataItem that this Operation should process (if empty, will select suitable fields based on the current `~imfusion.machinelearning.Operation.processing_policy`)
    record_identifier: Unused for this operation as it is not invertible

#### Methods

##### `__init__`

```python
def __init__(self, *, device: ComputingDevice | None = None, apply_to: list[str] | None = None, seed: int | None = None, error_on_unexpected_behaviour: bool | None = None) -> None
```

##### `__init__`

```python
def __init__(self, min: float = 0.0, max: float = 1.0, *, device: ComputingDevice | None = None, apply_to: list[str] | None = None, seed: int | None = None, error_on_unexpected_behaviour: bool | None = None) -> None
```

### `OneHotOperation`

```python
class OneHotOperation(Operation)
```

Encode a single channel label image, to a one-hot representation of 'channels' channels.
If encode_background is off, label '0' will denote the background and doesn't encode to anything, Label '1' will set the value '1'
in the first channel, Label '2' will set the value '1' in the second channels, etc.
If encode_background is on, label '0' will be the background and set the value '1' in the first channel, Label '1' will set the value '1'
in the second channel, etc.
The number of channels must be large enough to contain this encoding.

Args:
    num_channels: Number of channels in the output. Must be equal or larger to the highest possible label value. Default: 0
    encode_background: whether to encode background in first channel. Default: True
    to_ubyte: return label as ubyte (=int8) instead of float. Default: False
    device: Specifies whether this Operation should run on CPU or GPU.
    seed: Specifies seeding for any randomness that might be contained in this operation.
    error_on_unexpected_behaviour: Specifies whether to throw an exception instead of warning about unexpected behavior.
    apply_to: Specifies fields in a DataItem that this Operation should process (if empty, will select suitable fields based on the current `~imfusion.machinelearning.Operation.processing_policy`)
    record_identifier: Unused for this operation as it is not invertible

#### Methods

##### `__init__`

```python
def __init__(self, *, device: ComputingDevice | None = None, apply_to: list[str] | None = None, seed: int | None = None, error_on_unexpected_behaviour: bool | None = None) -> None
```

##### `__init__`

```python
def __init__(self, num_channels: int = 0, encode_background: bool = True, to_ubyte: bool = False, *, device: ComputingDevice | None = None, apply_to: list[str] | None = None, seed: int | None = None, error_on_unexpected_behaviour: bool | None = None) -> None
```

### `OperationError`

```python
class OperationError(Exception)
```

### `OperationsSequence`

```python
class OperationsSequence
```

Helper class that executes a list of operations sequentially. This class tries to minimize the number of intermediate copies and should be used for performance reasons.

#### Methods

##### `__call__`

```python
def __call__(self, input: imfusion.SharedImageSet, exec_phase: Phase = Phase.ALWAYS, in_place: bool = True) -> imfusion.SharedImageSet
```

Execute the preprocessing pipeline on the given input images. This function never works in-place.

Args:
        input: input image
        exec_phase: specifies the execution phase of the preprocessing pipeline. The execution will run only those operations whose phase (specified in the specs) corresponds to the current exec_phase, with the following exceptions:

                1) Operations marked with phase == Phase.Always are always run regardless of the exec_phase.
                2) If exec_phase == Phase.Always, all operations in the preprocessing pipeline are run regardless of their individual phase.

in_place (bool): If False, the input is guaranteed to be unchanged and the function will return a new object. If True, the input will be changed and the function will return it. (Default: False).

##### `__call__`

```python
def __call__(self, input: DataItem, exec_phase: Phase = Phase.ALWAYS) -> bool
```

Execute the preprocessing pipeline on the given input. This function always works in-place, i.e. the input DataItem will be modified.

Args:
        input: DataItem to be processed
        exec_phase: specifies the execution phase of the preprocessing pipeline. The execution will run only those operations whose phase (specified in the specs) corresponds to the current exec_phase, with the following exceptions:

                1) Operations marked with phase == Phase.Always are always run regardless of the exec_phase.
                2) If exec_phase == Phase.Always, all operations in the preprocessing pipeline are run regardless of their individual phase.

##### `__init__`

```python
def __init__(self) -> None
```

Default constructor that initializes the class with an empty list of operations.

##### `__init__`

```python
def __init__(self, pipeline_config: list[tuple[str, imfusion.Properties, Phase]]) -> None
```

Init the sequential processing with a pipeline of Operations and their relative specs.
The operations are executed according to their pipeline order.

Args:
        pipeline_config: List of specs for the operations to add to the sequence.

##### `add_operation`

```python
def add_operation(self, operation: Operation, phase: Phase = Phase.ALWAYS) -> bool
```

Add an operation to the sequential processing.
The operations are executed according to the addition order.

Args:
        operation: operation instance to add to the sequence.
        phase: when to execute the added operation. Default: Phase.Always

##### `add_operation`

```python
def add_operation(self, name: str, properties: imfusion.Properties, phase: Phase = Phase.ALWAYS) -> None
```

Add an operation to the sequential processing.
The operations are executed according to the addition order.

Args:
        name: name of the operation to add to the sequence. You must use the name used for registering the op in the operation factory. A list of the available ops can be retrieved by `~imfusion.machinelearning.OperationsSequence.available_operations())`.
        properties: properties to configure the operation.
        phase: specifies at which execution phase should the operation be run.

##### `available_cpp_operations`

*staticmethod*

```python
def available_cpp_operations() -> list[str]
```

Returns the list of registered C++ operations available for usage in OperationsSequence.

##### `available_operations`

*staticmethod*

```python
def available_operations() -> list[str]
```

Returns the list of all registered operations available for usage in OperationsSequence.

##### `available_py_operations`

*staticmethod*

```python
def available_py_operations() -> list[str]
```

Returns the list of registered Python operations available for usage in OperationsSequence.

##### `ok`

```python
def ok(self) -> bool
```

Returns whether operation setup was successful.

##### `operation_names`

```python
def operation_names(self) -> list[str]
```

Returns the operation names added to the sequence.

##### `operations`

*property*

```python
def operations(self) -> list[Operation]
```

List of all operations in this sequence with their execution phases

##### `process`

```python
def process(self, input: imfusion.SharedImageSet, exec_phase: Phase = Phase.ALWAYS, in_place: bool = True) -> imfusion.SharedImageSet
```

Execute the preprocessing pipeline on the given input images. This function never works in-place.

Args:
        input: input image
        exec_phase: specifies the execution phase of the preprocessing pipeline. The execution will run only those operations whose phase (specified in the specs) corresponds to the current exec_phase, with the following exceptions:

                1) Operations marked with phase == Phase.Always are always run regardless of the exec_phase.
                2) If exec_phase == Phase.Always, all operations in the preprocessing pipeline are run regardless of their individual phase.

in_place (bool): If False, the input is guaranteed to be unchanged and the function will return a new object. If True, the input will be changed and the function will return it. (Default: False).

##### `process`

```python
def process(self, input: DataItem, exec_phase: Phase = Phase.ALWAYS) -> bool
```

Execute the preprocessing pipeline on the given input. This function always works in-place, i.e. the input DataItem will be modified.

Args:
        input: DataItem to be processed
        exec_phase: specifies the execution phase of the preprocessing pipeline. The execution will run only those operations whose phase (specified in the specs) corresponds to the current exec_phase, with the following exceptions:

                1) Operations marked with phase == Phase.Always are always run regardless of the exec_phase.
                2) If exec_phase == Phase.Always, all operations in the preprocessing pipeline are run regardless of their individual phase.

##### `set_error_on_unexpected_behaviour`

```python
def set_error_on_unexpected_behaviour(self, value: bool) -> None
```

Set flag on all operations to control error behavior for unexpected situations.

When enabled, operations will throw exceptions instead of issuing warnings
when encountering unexpected behavior (e.g., applying augmentation to labels).

Args:
        value: If True, throw exceptions on unexpected behavior; if False, issue warnings

### `OrientedROISampler`

```python
class OrientedROISampler(ImageROISampler)
```

The OrientedROISampler draws `num_samples` randomly ROIs of size `roi_size` with spacing `roi_spacing` per dataset.
The sampler takes `n_guided = floor(sample_from_labels_proportion * num_samples)` label guided samples, and uniformly random samples for the rest of the samples.
Labelmaps and Keypoints are supported for label guided sampling; for labelmap sampling, the labelmap is interpreted as a probabilistic output and sampled accordingly (thus negative values break the sampling, and labelmaps need to be one-hot encoded in case of multiple label values).
Random augmentations can applied, including rotation, flipping, shearing, scaling and jitter.
These augmentations are directly changing the matrix of the sample, thus the samples are not guaranteed to be affine or even in a right-handed coordinate system.
The samples retain their matrices, so they can be viewed in their original position.
May throw an ImageSamplerError

Args:
    roi_size: Target size of the ROIs to be extracted as [Width, Height, Slices]
    roi_spacing: Target spacing of the ROIs to be extracted in mm
    num_samples: Number of samples to draw from one image. Default: 1
    random_rotation_range: Vector defining deviation in quaternion rotation over the corresponding axis. Default: [0, 0, 0]
    random_flipping_probability: Vector defining the change that corresponding dimension gets flipped. Default: [0, 0, 0]
    random_shearing_range: Vector defining the range of proportional shearing in each dimension. Default: [0, 0, 0]
    random_scaling_range: Vector defining the range of scaling in each dimension. Default: [0, 0, 0]
    random_jitter_range: Vector defining the range of jitter applied on top of the crop location, defined as the standard deviation in mm in each dimension. Default: [0, 0, 0]
    sample_from_labels_proportion: If num_samples is greater than one, guaranteed per-batch proportion of `num_samples` that should contain non-zero labels in the ROI if possible. If num_samples is one, then this parameter is treated as a probability that the sample is forced to contain non-zero labels. Default: 0.0.
    avoid_borders: When taking random samples, the samples avoid to see the border if this is turned on. Default: false
    align_crop: Align crop to image grid system, before applying augmentations. Default: false
    centers: Optional list of centers to sample from. Default: []
    random_scaling_logarithmic: Sample the scaling from a distribution that yields uniform scaling factors in 1/x ... x with
                    :math:`x = 1 +  extnormal{random_scaling_range}` instead of using scalings from :math:`\max(|1.0 + \mathcal{N}(0,       ext{randomScalingRange})|, 0.001)`. Default: False
    random_rotation_probability: Probability to apply a random rotation (parametrized by `random_rotation_range`). Default: 1.f
    random_shearing_probability: Probability to apply a random shearing (parametrized by `random_shearing_range`). Default: 1.f
    random_scaling_probability: Probability to apply a random scaling (parametrized by `random_scaling_range`). Default: 1.f
    y_axis_down: Force y-axis direction to point down in world-coordinates, should only be used for certain legacy pipelines. Default: False
    squeeze: Squeeze crops to a 2D representation which has their unary dimension in the 'slices' dimension; requires that one of the dimensions in roi_size is 1.
    
    device: Specifies whether this Operation should run on CPU or GPU.
    seed: Specifies seeding for any randomness that might be contained in this operation.
    error_on_unexpected_behaviour: Specifies whether to throw an exception instead of warning about unexpected behavior.
    apply_to: Specifies fields in a DataItem that this Operation should process (if empty, will select suitable fields based on the current `~imfusion.machinelearning.Operation.processing_policy`)
    record_identifier: Unused for this operation as it is not invertible

#### Methods

##### `__init__`

```python
def __init__(self, *, device: ComputingDevice | None = None, apply_to: list[str] | None = None, seed: int | None = None, error_on_unexpected_behaviour: bool | None = None) -> None
```

##### `__init__`

```python
def __init__(self, roi_size: numpy.ndarray[numpy.int32[3, 1]], roi_spacing: numpy.ndarray[numpy.float64[3, 1]], num_samples: int = 1, random_rotation_range: numpy.ndarray[numpy.float64[3, 1]] = ..., random_flipping_probability: numpy.ndarray[numpy.float64[3, 1]] = ..., random_shearing_range: numpy.ndarray[numpy.float64[3, 1]] = ..., random_scaling_range: numpy.ndarray[numpy.float64[3, 1]] = ..., random_jitter_range: numpy.ndarray[numpy.float64[3, 1]] = ..., sample_from_labels_proportion: float = 0.0, avoid_borders: bool = False, align_crop: bool = False, centers: list[numpy.ndarray[numpy.float64[3, 1]]] | None = None, random_scaling_logarithmic: bool = False, random_rotation_probability: float = 1.0, random_shearing_probability: float = 1.0, random_scaling_probability: float = 1.0, y_axis_down: bool = False, squeeze: bool = False, *, device: ComputingDevice | None = None, apply_to: list[str] | None = None, seed: int | None = None, error_on_unexpected_behaviour: bool | None = None) -> None
```

### `PadDimsOperation`

```python
class PadDimsOperation(InvertibleOperation)
```

This operation expands an image by adding padding pixels to any or all sides.
The value of the border can be specified by the padding mode:

    - Clamp: The border pixels are the same as the closest image pixel.
    - Mirror: The border pixels are the same as the closest image pixel.
    - Zero: Constant padding with zeros or, if provided, with paddingValue.

For label maps (i.e. modality == Modality.LABEL), a separate padding mode and value can be specified:

    - If both label padding mode and label padding value are specified, those values are used to pad the label map.
    - If only the label padding mode is specified, the ``paddingValue`` is used to fill the label map (only for zero padding).
    - If only the label padding value is specified, the ``paddingMode`` is used as the label padding mode.
    - If neither label padding mode nor label padding value are specified, ``paddingMode`` and ``paddingValue`` are used for label maps as well.

Note: the padding widths are evenly distributed to the left and right of the input image.
            If the difference ``delta`` between the target dimensions and the input dimensions is odd,
            the padding is distributed as delta / 2 to the left and delta / 2 + 1 to the right.

Note: `~imfusion.PaddingMode` can be automatically converted from a string. This means you can directly pass a string like "zero", "clamp", or "mirror" to the padding_mode parameters instead of using the enum values.

Args:
    target_dims: Target dimensions [width, height, depth] for the padded image. Default: [1, 1, 1]
    padding_mode: Mode for padding in ["zero", "clamp", "mirror"]. Default: `~imfusion.PaddingMode.MIRROR`
    padding_value: Value to use for padding when using Zero mode (optional). Default: None
    label_padding_mode: Mode for padding label maps in ["zero", "clamp", "mirror"], optional. Default: None
    label_padding_value: Value to use for padding label maps when using Zero mode (optional). Default: None
    allow_dimension_change: Allow padding dimensions equal to 1, which can change image dimension (e.g. 2D to 3D). Default: True
    
    device: Specifies whether this Operation should run on CPU or GPU.
    seed: Specifies seeding for any randomness that might be contained in this operation.
    error_on_unexpected_behaviour: Specifies whether to throw an exception instead of warning about unexpected behavior.
    apply_to: Specifies fields in a DataItem that this Operation should process (if empty, will select suitable fields based on the current `~imfusion.machinelearning.Operation.processing_policy`)
    record_identifier: Identifier of this operation to retrieve inversion parameters from the record

#### Methods

##### `__init__`

```python
def __init__(self, *, device: ComputingDevice | None = None, apply_to: list[str] | None = None, seed: int | None = None, error_on_unexpected_behaviour: bool | None = None) -> None
```

##### `__init__`

```python
def __init__(self, target_dims: numpy.ndarray[numpy.int32[3, 1]] = ..., padding_mode: imfusion.PaddingMode = imfusion.PaddingMode.MIRROR, padding_value: float | None = None, label_padding_mode: imfusion.PaddingMode | None = None, label_padding_value: int | None = None, allow_dimension_change: bool = True, *, device: ComputingDevice | None = None, apply_to: list[str] | None = None, seed: int | None = None, error_on_unexpected_behaviour: bool | None = None) -> None
```

### `PadDimsToNextMultipleOperation`

```python
class PadDimsToNextMultipleOperation(InvertibleOperation)
```

Pads each dimension of the input image to the next multiple of the specified divisor.
For example, if an image has dimensions (100, 150, 200) and dimension_divisor is (32, 16, 64), the output will have dimensions (128, 160, 256).
This operation is useful for ensuring that the input dimensions are compatible with a ML model (e.g. a CNN or UNet) that expects specific dimensions.
The value of the border can be specified by the padding mode:

    - Clamp: The border pixels are the same as the closest image pixel.
    - Mirror: The border pixels are the same as the closest image pixel.
    - Zero: Constant padding with zeros or, if provided, with paddingValue.

For label maps (i.e. modality == Modality.LABEL), a separate padding mode and value can be specified:

    - If both label padding mode and label padding value are specified, those values are used to pad the label map.
    - If only the label padding mode is specified, the ``paddingValue`` is used to fill the label map (only for zero padding).
    - If only the label padding value is specified, the ``paddingMode`` is used as the label padding mode.
    - If neither label padding mode nor label padding value are specified, ``paddingMode`` and ``paddingValue`` are used for label maps as well.

Note: the padding widths are evenly distributed to the left and right of the input image.
      If the difference ``delta`` between the target dimensions and the input dimensions is odd,
      the padding is distributed as delta / 2 to the left and delta / 2 + 1 to the right.
    
Note: `~imfusion.PaddingMode` can be automatically converted from a string. This means you can directly pass a string like "zero", "clamp", or "mirror" to the padding_mode parameters instead of using the enum values.
      
Args:
    dimension_divisor: The divisor for each dimension of the input image. Default: [1, 1, 1]
    padding_mode: Mode for padding in ["zero", "clamp", "mirror"]. Default: `~imfusion.PaddingMode.MIRROR`
    padding_value: Value to use for padding when using Zero mode (optional). Default: None
    label_padding_mode: Mode for padding label maps in ["zero", "clamp", "mirror"], optional. Default: None
    label_padding_value: Value to use for padding label maps when using "zero" mode (optional). Default: None
    allow_dimension_change: Allow padding dimensions equal to 1, which can change image dimension (e.g. 2D to 3D). Default: True
    
    device: Specifies whether this Operation should run on CPU or GPU.
    seed: Specifies seeding for any randomness that might be contained in this operation.
    error_on_unexpected_behaviour: Specifies whether to throw an exception instead of warning about unexpected behavior.
    apply_to: Specifies fields in a DataItem that this Operation should process (if empty, will select suitable fields based on the current `~imfusion.machinelearning.Operation.processing_policy`)
    record_identifier: Identifier of this operation to retrieve inversion parameters from the record

#### Methods

##### `__init__`

```python
def __init__(self, *, device: ComputingDevice | None = None, apply_to: list[str] | None = None, seed: int | None = None, error_on_unexpected_behaviour: bool | None = None) -> None
```

##### `__init__`

```python
def __init__(self, dimension_divisor: numpy.ndarray[numpy.int32[3, 1]] = ..., padding_mode: imfusion.PaddingMode = imfusion.PaddingMode.MIRROR, padding_value: float | None = None, label_padding_mode: imfusion.PaddingMode | None = None, label_padding_value: int | None = None, allow_dimension_change: bool = True, *, device: ComputingDevice | None = None, apply_to: list[str] | None = None, seed: int | None = None, error_on_unexpected_behaviour: bool | None = None) -> None
```

### `PadOperation`

```python
class PadOperation(InvertibleOperation)
```

Pad an image to a specific padding size in each dimension.
This operation expands an image by adding padding pixels to any or all sides.
The value of the border can be specified by the padding mode:

    - Clamp: The border pixels are the same as the closest image pixel.
    - Mirror: The border pixels are the same as the closest image pixel.
    - Zero: Constant padding with zeros or, if provided, with paddingValue.
         
For label maps (i.e. modality == Modality.LABEL), a separate padding mode and value can be specified:

    - If both label padding mode and label padding value are specified, those values are used to pad the label map.
    - If only the label padding mode is specified, the ``paddingValue`` is used to fill the label map (only for zero padding).
    - If only the label padding value is specified, the ``paddingMode`` is used as the label padding mode.
    - If neither label padding mode nor label padding value are specified, ``paddingMode`` and ``paddingValue`` are used for label maps as well.

Note: Padding sizes are specified in pixels, and can be positive, negative or mixed. Negative padding means cropping.

Note: Both GPU and CPU implementations are provided.

Note: `~imfusion.PaddingMode` can be automatically converted from a string. This means you can directly pass a string like "zero", "clamp", or "mirror" to the padding_mode parameters instead of using the enum values.

Args:
    pad_size_x: Padding width in pixels for X dimension [left, right]. Default: [0, 0]
    pad_size_y: Padding width in pixels for Y dimension [top, bottom]. Default: [0, 0]
    pad_size_z: Padding width in pixels for Z dimension [front, back]. Default: [0, 0]
    padding_mode: Mode for padding in ["zero", "clamp", "mirror"]. Default: `~imfusion.PaddingMode.MIRROR`
    padding_value: Optional value to use for padding when using Zero mode. Default: None
    label_padding_mode: Optional mode for padding label maps in ["zero", "clamp", "mirror"]. Default: None
    label_padding_value: Optional value to use for padding label maps when using "zero" mode. Default: None
    allow_dimension_change: Allow padding dimensions equal to 1, which can change image dimension (e.g. 2D to 3D). Default: True
    
    device: Specifies whether this Operation should run on CPU or GPU.
    seed: Specifies seeding for any randomness that might be contained in this operation.
    error_on_unexpected_behaviour: Specifies whether to throw an exception instead of warning about unexpected behavior.
    apply_to: Specifies fields in a DataItem that this Operation should process (if empty, will select suitable fields based on the current `~imfusion.machinelearning.Operation.processing_policy`)
    record_identifier: Identifier of this operation to retrieve inversion parameters from the record

#### Methods

##### `__init__`

```python
def __init__(self, *, device: ComputingDevice | None = None, apply_to: list[str] | None = None, seed: int | None = None, error_on_unexpected_behaviour: bool | None = None) -> None
```

##### `__init__`

```python
def __init__(self, pad_size_x: numpy.ndarray[numpy.int32[2, 1]] = ..., pad_size_y: numpy.ndarray[numpy.int32[2, 1]] = ..., pad_size_z: numpy.ndarray[numpy.int32[2, 1]] = ..., padding_mode: imfusion.PaddingMode = imfusion.PaddingMode.MIRROR, padding_value: float | None = None, label_padding_mode: imfusion.PaddingMode | None = None, label_padding_value: int | None = None, allow_dimension_change: bool = True, *, device: ComputingDevice | None = None, apply_to: list[str] | None = None, seed: int | None = None, error_on_unexpected_behaviour: bool | None = None) -> None
```

### `PixelwiseClassificationMetrics`

```python
class PixelwiseClassificationMetrics(Metric)
```

Computes pixelwise classification metrics for segmentation tasks.

This metric computes precision, recall, F1-score, and accuracy for each class in a segmentation task.

#### Methods

##### `__init__`

```python
def __init__(self) -> None
```

Constructs a PixelwiseClassificationMetrics object.

##### `compute_per_label`

```python
def compute_per_label(self, prediction: imfusion.SharedImageSet, target: imfusion.SharedImageSet) -> list[dict[str, dict[int, float]]]
```

Computes classification metrics separately for each label/class.

This method calculates precision, recall, F1-score, and accuracy for each class
in the segmentation task, providing detailed per-class performance metrics.

Args:
        prediction: The predicted segmentation mask
        target: The target/ground truth segmentation mask
Returns:
        dict: Dictionary with metrics (precision, recall, F1, accuracy) per label

### `PolyCropOperation`

```python
class PolyCropOperation(Operation)
```

Masks the image with a convex polygon as described in Markova et al. 2022. (https://arxiv.org/abs/2205.03439)

Args:
    points: Each point (texture coordinates) in `points` defines a plane (perpendicular to the direction from the center to the
                                             point), this plane splits the volume in two parts, the part of the image that doesn't contain the image center is discarded.
    device: Specifies whether this Operation should run on CPU or GPU.
    seed: Specifies seeding for any randomness that might be contained in this operation.
    error_on_unexpected_behaviour: Specifies whether to throw an exception instead of warning about unexpected behavior.
    apply_to: Specifies fields in a DataItem that this Operation should process (if empty, will select suitable fields based on the current `~imfusion.machinelearning.Operation.processing_policy`)
    record_identifier: Unused for this operation as it is not invertible

#### Methods

##### `__init__`

```python
def __init__(self, *, device: ComputingDevice | None = None, apply_to: list[str] | None = None, seed: int | None = None, error_on_unexpected_behaviour: bool | None = None) -> None
```

##### `__init__`

```python
def __init__(self, points: list[numpy.ndarray[numpy.float64[3, 1]]] = [], *, device: ComputingDevice | None = None, apply_to: list[str] | None = None, seed: int | None = None, error_on_unexpected_behaviour: bool | None = None) -> None
```

### `ProcessingRecordComponent`

```python
class ProcessingRecordComponent(imfusion.DataComponentBase)
```

Component that stores a record of processing operations applied to data.

This component tracks the sequence of invertible operations that have been applied
to a DataElement, enabling inversion of transformations when needed.

#### Methods

##### `__init__`

```python
def __init__(self) -> None
```

Initialize a ProcessingRecordComponent.

Creates a component that tracks processing operations applied to a DataElement.

### `RandomAddDegradedLabelAsChannelOperation`

```python
class RandomAddDegradedLabelAsChannelOperation(Operation)
```

Append a channel to the image that contains a randomly degraded version of the label.

Args:
    blob_radius: Radius of each blob, in pixel coordinates. Default: 5.0.
    probability_no_blobs: Probability that zero blobs are chosen. Default: 0.1
    probability_invert: Probability of inverting the blobs, in this case the extra channel is positive/negative based on the label except at blobs, where it is zero. Default: 0.0
    mean_num_blobs : Mean of (Poisson-distributed) number of blobs to draw, conditional on `probability_no_blobs`. Default: 100.0
    only_positive: If true, output channel is clamped to zero from below. Default: False
    label_dilation_range: The label_dilation parameter of the underlying AddDegradedLabelAsChannelOperation is uniformly drawn from this range. Default: [0.0, 0.0]
    device: Specifies whether this Operation should run on CPU or GPU.
    seed: Specifies seeding for any randomness that might be contained in this operation.
    error_on_unexpected_behaviour: Specifies whether to throw an exception instead of warning about unexpected behavior.
    apply_to: Specifies fields in a DataItem that this Operation should process (if empty, will select suitable fields based on the current `~imfusion.machinelearning.Operation.processing_policy`)
    record_identifier: Unused for this operation as it is not invertible

#### Methods

##### `__init__`

```python
def __init__(self, *, device: ComputingDevice | None = None, apply_to: list[str] | None = None, seed: int | None = None, error_on_unexpected_behaviour: bool | None = None) -> None
```

##### `__init__`

```python
def __init__(self, blob_radius: float = 5.0, probability_no_blobs: float = 0.1, probability_invert: float = 0.0, mean_num_blobs: float = 100.0, only_positive: bool = False, dilation_range: numpy.ndarray[numpy.float64[2, 1]] = ..., *, device: ComputingDevice | None = None, apply_to: list[str] | None = None, seed: int | None = None, error_on_unexpected_behaviour: bool | None = None) -> None
```

### `RandomAddRandomNoiseOperation`

```python
class RandomAddRandomNoiseOperation(Operation)
```

Apply `~imfusion.machinelearning.AddRandomNoiseOperation` to images with randomized intensity parameter.

Args:
    type: Distribution of the noise ('uniform', 'gaussian', 'gamma','shot'). Default: 'uniform'. See `~imfusion.machinelearning.AddRandomNoiseOperation`.
             intensity_range: Range of the interval used to draw the intensity parameter. Default: [0.0, 0.0]. Absolute values of drawn values are taken.
    device: Specifies whether this Operation should run on CPU or GPU.
    seed: Specifies seeding for any randomness that might be contained in this operation.
    error_on_unexpected_behaviour: Specifies whether to throw an exception instead of warning about unexpected behavior.
    apply_to: Specifies fields in a DataItem that this Operation should process (if empty, will select suitable fields based on the current `~imfusion.machinelearning.Operation.processing_policy`)
    record_identifier: Unused for this operation as it is not invertible
    probability: Float in [0, 1] defining the probability for the operation to be executed. Default: 1.0

#### Methods

##### `__init__`

```python
def __init__(self, *, device: ComputingDevice | None = None, apply_to: list[str] | None = None, seed: int | None = None, error_on_unexpected_behaviour: bool | None = None) -> None
```

##### `__init__`

```python
def __init__(self, type: str = 'uniform', intensity_range: numpy.ndarray[numpy.float64[2, 1]] = ..., probability: float = 1.0, *, device: ComputingDevice | None = None, apply_to: list[str] | None = None, seed: int | None = None, error_on_unexpected_behaviour: bool | None = None) -> None
```

### `RandomAxisFlipOperation`

```python
class RandomAxisFlipOperation(Operation)
```

Flip image content along specified set of axes, with independent sampling for each axis.

Args:
    axes: List of strings from {'x','y','z'} specifying the axes to flip. For 2D images, only 'x' and 'y' are valid.
    probability: Float in [0;1] defining the probability for the operation to be executed. Default: 1.0
    device: Specifies whether this Operation should run on CPU or GPU.
    seed: Specifies seeding for any randomness that might be contained in this operation.
    error_on_unexpected_behaviour: Specifies whether to throw an exception instead of warning about unexpected behavior.
    apply_to: Specifies fields in a DataItem that this Operation should process (if empty, will select suitable fields based on the current `~imfusion.machinelearning.Operation.processing_policy`)
    record_identifier: Unused for this operation as it is not invertible

#### Methods

##### `__init__`

```python
def __init__(self, *, device: ComputingDevice | None = None, apply_to: list[str] | None = None, seed: int | None = None, error_on_unexpected_behaviour: bool | None = None) -> None
```

##### `__init__`

```python
def __init__(self, axes: list[str] = [], probability: float = 1.0, *, device: ComputingDevice | None = None, apply_to: list[str] | None = None, seed: int | None = None, error_on_unexpected_behaviour: bool | None = None) -> None
```

### `RandomAxisRotationOperation`

```python
class RandomAxisRotationOperation(Operation)
```

Rotate image around image axis with independently drawn axis-specific random rotation angle of +-{90, 180, 270} degrees.

Args:
    axes: List of strings from {'x','y','z'} specifying the axes to rotate around. For 2D images, only ['z'] is valid.
    probability: Float in [0;1] defining the probability for the operation to be executed. Default: 1.0
    device: Specifies whether this Operation should run on CPU or GPU.
    seed: Specifies seeding for any randomness that might be contained in this operation.
    error_on_unexpected_behaviour: Specifies whether to throw an exception instead of warning about unexpected behavior.
    apply_to: Specifies fields in a DataItem that this Operation should process (if empty, will select suitable fields based on the current `~imfusion.machinelearning.Operation.processing_policy`)
    record_identifier: Unused for this operation as it is not invertible

#### Methods

##### `__init__`

```python
def __init__(self, *, device: ComputingDevice | None = None, apply_to: list[str] | None = None, seed: int | None = None, error_on_unexpected_behaviour: bool | None = None) -> None
```

##### `__init__`

```python
def __init__(self, axes: list[str] = [], probability: float = 1.0, *, device: ComputingDevice | None = None, apply_to: list[str] | None = None, seed: int | None = None, error_on_unexpected_behaviour: bool | None = None) -> None
```

### `RandomChoiceOperation`

```python
class RandomChoiceOperation(Operation)
```

Meta-operation that picks one operation from its configuration randomly and executes it.
This is particularly useful for image samplers, where we might want to alternate between different ways of sampling the input images.

Args:
    operation_specs: List of operation Specs to configure the operations to be added.
    operation_weights: Weights associated to the each operation during the sampling process.
                                                                     A higher relative weight given to an operation means that this operation will be sampled more often.
    device: Specifies whether this Operation should run on CPU or GPU.
    seed: Specifies seeding for any randomness that might be contained in this operation.
    error_on_unexpected_behaviour: Specifies whether to throw an exception instead of warning about unexpected behavior.
    apply_to: Specifies fields in a DataItem that this Operation should process (if empty, will select suitable fields based on the current `~imfusion.machinelearning.Operation.processing_policy`)
    record_identifier: Unused for this operation as it is not invertible

#### Methods

##### `__init__`

```python
def __init__(self, *, device: ComputingDevice | None = None, apply_to: list[str] | None = None, seed: int | None = None, error_on_unexpected_behaviour: bool | None = None) -> None
```

##### `__init__`

```python
def __init__(self, operation_specs: list[Operation.Specs] = [], operation_weights: list[float] = [], *, device: ComputingDevice | None = None, apply_to: list[str] | None = None, seed: int | None = None, error_on_unexpected_behaviour: bool | None = None) -> None
```

##### `__init__`

```python
def __init__(self, operation_specs: list[tuple[str, imfusion.Properties, Phase]], operation_weights: list[float] = []) -> None
```

Meta-operation that picks one operation from its configuration randomly and executes it.
This is particularly useful for image samplers, where we might want to alternate between different ways of sampling the input images.

Args:
        operation_specs: List of operation (name, Properties, Phase) casted into Specs to configure the operations to be added.
        operation_weights: Weights associated to the each operation during the sampling process.
                                                                         A higher relative weight given to an operation means that this operation will be sampled more often.

##### `__init__`

```python
def __init__(self, operations: list[Operation], operation_weights: list[float]) -> None
```

Meta-operation that picks one operation from its configuration randomly and executes it.
This is particularly useful for image samplers, where we might want to alternate between different ways of sampling the input images.

Args:
        operations: List of operations to be added.
        operation_weights: Weights associated to the each operation during the sampling process.
                A higher relative weight given to an operation means that this operation will be sampled more often.

### `RandomCropAroundLabelMapOperation`

```python
class RandomCropAroundLabelMapOperation(Operation)
```

Crops the input image and label to the bounds of a random label value, and sets the label value to 1 and all other values to zero in the resulting label.

Args:
    margin: Margin, in pixels. Default: 1
    reorder: Whether label value in result should be mapped to 1. Default: False
    device: Specifies whether this Operation should run on CPU or GPU.
    seed: Specifies seeding for any randomness that might be contained in this operation.
    error_on_unexpected_behaviour: Specifies whether to throw an exception instead of warning about unexpected behavior.
    apply_to: Specifies fields in a DataItem that this Operation should process (if empty, will select suitable fields based on the current `~imfusion.machinelearning.Operation.processing_policy`)
    record_identifier: Unused for this operation as it is not invertible

#### Methods

##### `__init__`

```python
def __init__(self, *, device: ComputingDevice | None = None, apply_to: list[str] | None = None, seed: int | None = None, error_on_unexpected_behaviour: bool | None = None) -> None
```

##### `__init__`

```python
def __init__(self, margin: int = 1, reorder: bool = False, *, device: ComputingDevice | None = None, apply_to: list[str] | None = None, seed: int | None = None, error_on_unexpected_behaviour: bool | None = None) -> None
```

### `RandomCropOperation`

```python
class RandomCropOperation(Operation)
```

Crop input images and label maps with a matching random size and offset.

Args:
    crop_range: List of floats from [0;1] specifying the maximum percentage of the dimension to crop. Default: [0.0, 0.0, 0.0]
    probability: Float in [0;1] defining the probability for the operation to be executed. Default: 1.0
    device: Specifies whether this Operation should run on CPU or GPU.
    seed: Specifies seeding for any randomness that might be contained in this operation.
    error_on_unexpected_behaviour: Specifies whether to throw an exception instead of warning about unexpected behavior.
    apply_to: Specifies fields in a DataItem that this Operation should process (if empty, will select suitable fields based on the current `~imfusion.machinelearning.Operation.processing_policy`)
    record_identifier: Unused for this operation as it is not invertible

#### Methods

##### `__init__`

```python
def __init__(self, *, device: ComputingDevice | None = None, apply_to: list[str] | None = None, seed: int | None = None, error_on_unexpected_behaviour: bool | None = None) -> None
```

##### `__init__`

```python
def __init__(self, crop_range: numpy.ndarray[numpy.float64[3, 1]] = ..., probability: float = 1.0, *, device: ComputingDevice | None = None, apply_to: list[str] | None = None, seed: int | None = None, error_on_unexpected_behaviour: bool | None = None) -> None
```

### `RandomCutOutOperation`

```python
class RandomCutOutOperation(Operation)
```

Apply a random cutout to the image.

Args:
    cutout_size_lower: List of doubles specifying the lower bound of the cutout region size for each dimension in mm. Default: [0, 0, 0]
    cutout_size_upper: List of doubles specifying the upper bound of the cutout region size for each dimension in mm. Default: [0, 0, 0]
    cutout_value_range: List of floats specifying the minimum and maximum fill value for cutout regions. Default: [0, 0]
    cutout_number_range: List of integers specifying the minimum and maximum number of cutout regions. Default: [0, 0]
    cutout_size_units: Units of the cutout size. Default: `~imfusion.machinelearning.ParamUnit.MM`
    probability: Float in [0;1] defining the probability for the operation to be executed.
    device: Specifies whether this Operation should run on CPU or GPU.
    seed: Specifies seeding for any randomness that might be contained in this operation.
    error_on_unexpected_behaviour: Specifies whether to throw an exception instead of warning about unexpected behavior.
    apply_to: Specifies fields in a DataItem that this Operation should process (if empty, will select suitable fields based on the current `~imfusion.machinelearning.Operation.processing_policy`)
    record_identifier: Unused for this operation as it is not invertible

#### Methods

##### `__init__`

```python
def __init__(self, *, device: ComputingDevice | None = None, apply_to: list[str] | None = None, seed: int | None = None, error_on_unexpected_behaviour: bool | None = None) -> None
```

##### `__init__`

```python
def __init__(self, cutout_size_lower: numpy.ndarray[numpy.float64[3, 1]] = ..., cutout_size_upper: numpy.ndarray[numpy.float64[3, 1]] = ..., cutout_value_range: numpy.ndarray[numpy.float32[2, 1]] = ..., cutout_number_range: numpy.ndarray[numpy.int32[2, 1]] = ..., cutout_size_units: ParamUnit = ..., probability: float = 1.0, *, device: ComputingDevice | None = None, apply_to: list[str] | None = None, seed: int | None = None, error_on_unexpected_behaviour: bool | None = None) -> None
```

### `RandomDeformationOperation`

```python
class RandomDeformationOperation(Operation)
```

Apply a deformation to the image using a specified control point grid and random displacements

Args:
    num_subdivisions: list specifying the number of subdivisions for each dimension (the number of control points is subdivisions+1). For 2D images, the last component will be ignored. Default: [1, 1, 1]
    max_abs_displacement: absolute value of the maximum possible displacement (mm). Default: 1
    padding_mode: defines which type of padding is used in ["zero", "clamp", "mirror"]. Default: `~imfusion.PaddingMode.ZERO`
    probability: probability of applying this Operation. Default: 1.0
    adjust_size: configures whether the resulting image should adjust its size to encompass the deformation. Default: False
    device: Specifies whether this Operation should run on CPU or GPU.
    seed: Specifies seeding for any randomness that might be contained in this operation.
    error_on_unexpected_behaviour: Specifies whether to throw an exception instead of warning about unexpected behavior.
    apply_to: Specifies fields in a DataItem that this Operation should process (if empty, will select suitable fields based on the current `~imfusion.machinelearning.Operation.processing_policy`)
    record_identifier: Unused for this operation as it is not invertible

#### Methods

##### `__init__`

```python
def __init__(self, *, device: ComputingDevice | None = None, apply_to: list[str] | None = None, seed: int | None = None, error_on_unexpected_behaviour: bool | None = None) -> None
```

##### `__init__`

```python
def __init__(self, num_subdivisions: numpy.ndarray[numpy.int32[3, 1]] = ..., max_abs_displacement: float = 1, padding_mode: imfusion.PaddingMode = imfusion.PaddingMode.ZERO, probability: float = 1.0, adjust_size: bool = False, *, device: ComputingDevice | None = None, apply_to: list[str] | None = None, seed: int | None = None, error_on_unexpected_behaviour: bool | None = None) -> None
```

### `RandomGammaCorrectionOperation`

```python
class RandomGammaCorrectionOperation(Operation)
```

Apply a random gamma correction to the image intensities.
Output = Unnormalize(pow(Normalize(Input), gamma))
where `gamma` is drawn uniformly in [1-random_range; 1+random_range].

Args:
    random_range: Range of the interval used to draw the gamma correction, typically in [0; 0.5]. Default: 0.2
    probability: Float in [0;1] defining the probability for the operation to be executed. Default: 1.0
    device: Specifies whether this Operation should run on CPU or GPU.
    seed: Specifies seeding for any randomness that might be contained in this operation.
    error_on_unexpected_behaviour: Specifies whether to throw an exception instead of warning about unexpected behavior.
    apply_to: Specifies fields in a DataItem that this Operation should process (if empty, will select suitable fields based on the current `~imfusion.machinelearning.Operation.processing_policy`)
    record_identifier: Unused for this operation as it is not invertible

#### Methods

##### `__init__`

```python
def __init__(self, *, device: ComputingDevice | None = None, apply_to: list[str] | None = None, seed: int | None = None, error_on_unexpected_behaviour: bool | None = None) -> None
```

##### `__init__`

```python
def __init__(self, random_range: float = 0.2, probability: float = 1.0, *, device: ComputingDevice | None = None, apply_to: list[str] | None = None, seed: int | None = None, error_on_unexpected_behaviour: bool | None = None) -> None
```

### `RandomImageFromLabelOperation`

```python
class RandomImageFromLabelOperation(Operation)
```

Creates a random image from a label map, each label is sampled from a Gaussian distribution.
Each Gaussian distribution parameters (mean and standard deviation) are uniformly sampled withing the provided intervals
(respectively mean_range and standard_dev_range).
 
Args:
    mean_range: Range of means for the intensities' Gaussian distributions.
    standard_dev_range: Range of standard deviations for the intensities' Gaussian distributions.
    output_field: Output field for the generated image.
    device: Specifies whether this Operation should run on CPU or GPU.
    seed: Specifies seeding for any randomness that might be contained in this operation.
    error_on_unexpected_behaviour: Specifies whether to throw an exception instead of warning about unexpected behavior.
    apply_to: Specifies fields in a DataItem that this Operation should process (if empty, will select suitable fields based on the current `~imfusion.machinelearning.Operation.processing_policy`)
    record_identifier: Unused for this operation as it is not invertible

#### Methods

##### `__init__`

```python
def __init__(self, *, device: ComputingDevice | None = None, apply_to: list[str] | None = None, seed: int | None = None, error_on_unexpected_behaviour: bool | None = None) -> None
```

##### `__init__`

```python
def __init__(self, mean_range: numpy.ndarray[numpy.float64[2, 1]], standard_dev_range: numpy.ndarray[numpy.float64[2, 1]], output_field: str = 'ImageFromLabel', *, device: ComputingDevice | None = None, apply_to: list[str] | None = None, seed: int | None = None, error_on_unexpected_behaviour: bool | None = None) -> None
```

### `RandomInvertOperation`

```python
class RandomInvertOperation(Operation)
```

Invert the intensities of the image: :math:`        extnormal{output} = -   extnormal{input}`.

Args:
    probability: Float in [0;1] defining the probability for the operation to be executed. Default: 0.5
    device: Specifies whether this Operation should run on CPU or GPU.
    seed: Specifies seeding for any randomness that might be contained in this operation.
    error_on_unexpected_behaviour: Specifies whether to throw an exception instead of warning about unexpected behavior.
    apply_to: Specifies fields in a DataItem that this Operation should process (if empty, will select suitable fields based on the current `~imfusion.machinelearning.Operation.processing_policy`)
    record_identifier: Unused for this operation as it is not invertible

#### Methods

##### `__init__`

```python
def __init__(self, *, device: ComputingDevice | None = None, apply_to: list[str] | None = None, seed: int | None = None, error_on_unexpected_behaviour: bool | None = None) -> None
```

##### `__init__`

```python
def __init__(self, probability: float = 0.5, *, device: ComputingDevice | None = None, apply_to: list[str] | None = None, seed: int | None = None, error_on_unexpected_behaviour: bool | None = None) -> None
```

### `RandomKeypointJitterOperation`

```python
class RandomKeypointJitterOperation(Operation)
```

Adds an individually and randomly sampled offset to each keypoint of each KeypointElement.

Args:
    offset_std_dev: standard deviation of the normal distribution used to sample the jitter in mm
    device: Specifies whether this Operation should run on CPU or GPU.
    seed: Specifies seeding for any randomness that might be contained in this operation.
    error_on_unexpected_behaviour: Specifies whether to throw an exception instead of warning about unexpected behavior.
    apply_to: Specifies fields in a DataItem that this Operation should process (if empty, will select suitable fields based on the current `~imfusion.machinelearning.Operation.processing_policy`)
    record_identifier: Unused for this operation as it is not invertible

#### Methods

##### `__init__`

```python
def __init__(self, *, device: ComputingDevice | None = None, apply_to: list[str] | None = None, seed: int | None = None, error_on_unexpected_behaviour: bool | None = None) -> None
```

##### `__init__`

```python
def __init__(self, offset_std_dev: float, *, device: ComputingDevice | None = None, apply_to: list[str] | None = None, seed: int | None = None, error_on_unexpected_behaviour: bool | None = None) -> None
```

### `RandomLinearIntensityMappingOperation`

```python
class RandomLinearIntensityMappingOperation(Operation)
```

Apply a random linear shift and scale to the image intensities.
:math:`     extnormal{output} =     extnormal{factor}_      extnormal{random} *     extnormal{input} +      extnormal{bias}_        extnormal{random}` ,

| where :math:`     extnormal{factor}_      extnormal{random}` is drawn uniformly in :math:`[1-     extnormal{random_range}, 1+     extnormal{random_range}]`
| and :math:`       extnormal{bias}_        extnormal{random}` is drawn uniformly in :math:`[-      extnormal{random_range}*(\max(  extnormal{input})-\min( extnormal{input})),     extnormal{random_range}*(\max(  extnormal{input})-\min( extnormal{input}))]`.

Args:
    random_range: Perturbation amplitude, typically in [0.0, 1.0]. Default: 0.2
    probability: Float in [0, 1] defining the probability for the operation to be executed. Default: 1.0
    device: Specifies whether this Operation should run on CPU or GPU.
    seed: Specifies seeding for any randomness that might be contained in this operation.
    error_on_unexpected_behaviour: Specifies whether to throw an exception instead of warning about unexpected behavior.
    apply_to: Specifies fields in a DataItem that this Operation should process (if empty, will select suitable fields based on the current `~imfusion.machinelearning.Operation.processing_policy`)
    record_identifier: Unused for this operation as it is not invertible

#### Methods

##### `__init__`

```python
def __init__(self, *, device: ComputingDevice | None = None, apply_to: list[str] | None = None, seed: int | None = None, error_on_unexpected_behaviour: bool | None = None) -> None
```

##### `__init__`

```python
def __init__(self, random_range: float = 0.2, probability: float = 1.0, *, device: ComputingDevice | None = None, apply_to: list[str] | None = None, seed: int | None = None, error_on_unexpected_behaviour: bool | None = None) -> None
```

### `RandomMRIBiasFieldGenerationOperation`

```python
class RandomMRIBiasFieldGenerationOperation(Operation)
```

Apply or generate a random multiplicative intensity modulation field.
If the output is a field, it is shifted as close to mean 1 as possible while remaining positive everywhere.
If the output is not a field, the image intensity is shifted so that the mean intensity of the input image is preserved.

Args:
    center_beta_dist_params: Beta distribution parameters for sampling the relative center coordinate locations. Default: [0.0, 1.0]
    field_amplitude_random_range: Amplitude of the field. Default: [0.2, 0.5]
    length_scale_mm_random_range: Range of length scale of the distance kernel in mm. Default: [50.0, 400.0]
    distance_scaling_random_range: Range of relative scaling of scanner space coordinates for anisotropic fields. Default: [0.5, 1.0]
    invert_probability: Probability to invert the field (before normalization): field &lt;- 2.0 - field. Default: 0.0
    output_is_field: Produce field instead of corrupted image. Default: False
    device: Specifies whether this Operation should run on CPU or GPU.
    seed: Specifies seeding for any randomness that might be contained in this operation.
    error_on_unexpected_behaviour: Specifies whether to throw an exception instead of warning about unexpected behavior.
    apply_to: Specifies fields in a DataItem that this Operation should process (if empty, will select suitable fields based on the current `~imfusion.machinelearning.Operation.processing_policy`)
    record_identifier: Unused for this operation as it is not invertible

#### Methods

##### `__init__`

```python
def __init__(self, *, device: ComputingDevice | None = None, apply_to: list[str] | None = None, seed: int | None = None, error_on_unexpected_behaviour: bool | None = None) -> None
```

##### `__init__`

```python
def __init__(self, center_beta_dist_params: numpy.ndarray[numpy.float64[2, 1]] = ..., field_amplitude_random_range: numpy.ndarray[numpy.float64[2, 1]] = ..., length_scale_mm_random_range: numpy.ndarray[numpy.float64[2, 1]] = ..., distance_scaling_random_range: numpy.ndarray[numpy.float64[2, 1]] = ..., invert_probability: float = 0.0, output_is_field: bool = False, *, device: ComputingDevice | None = None, apply_to: list[str] | None = None, seed: int | None = None, error_on_unexpected_behaviour: bool | None = None) -> None
```

### `RandomPolyCropOperation`

```python
class RandomPolyCropOperation(Operation)
```

Masks the image with a random convex polygon as described in Markova et al. 2022 (https://arxiv.org/abs/2205.03439).
The convex polygon mask is constructed by sampling random planes, each plane splits the volume in two parts, the part of the image that
doesn't contain the image center is discarded.

Args:
    number_range: Range of integers specifying the minimum and maximum number of cutting planes. Default: [5, 10]
    min_radius: The minimum distance a cutting plane must have from the center (image coordinates are normalized to [-1, 1]). Default: 1.0
    device: Specifies whether this Operation should run on CPU or GPU.
    seed: Specifies seeding for any randomness that might be contained in this operation.
    error_on_unexpected_behaviour: Specifies whether to throw an exception instead of warning about unexpected behavior.
    apply_to: Specifies fields in a DataItem that this Operation should process (if empty, will select suitable fields based on the current `~imfusion.machinelearning.Operation.processing_policy`)
    record_identifier: Unused for this operation as it is not invertible

#### Methods

##### `__init__`

```python
def __init__(self, *, device: ComputingDevice | None = None, apply_to: list[str] | None = None, seed: int | None = None, error_on_unexpected_behaviour: bool | None = None) -> None
```

##### `__init__`

```python
def __init__(self, number_range: numpy.ndarray[numpy.int32[2, 1]] = ..., min_radius: float = 1.0, *, device: ComputingDevice | None = None, apply_to: list[str] | None = None, seed: int | None = None, error_on_unexpected_behaviour: bool | None = None) -> None
```

### `RandomROISampler`

```python
class RandomROISampler(ImageROISampler)
```

Sampler which randomly samples ROIs from the input image and label map with a target
The images will be padded if the target size is larger than the input image.

Args:
    roi_size: Target size of the ROIs to be extracted as [Width, Height, Slices]
    device: Specifies whether this Operation should run on CPU or GPU.
    seed: Specifies seeding for any randomness that might be contained in this operation.
    error_on_unexpected_behaviour: Specifies whether to throw an exception instead of warning about unexpected behavior.
    apply_to: Specifies fields in a DataItem that this Operation should process (if empty, will select suitable fields based on the current `~imfusion.machinelearning.Operation.processing_policy`)
    record_identifier: Unused for this operation as it is not invertible

#### Methods

##### `__init__`

```python
def __init__(self, *, device: ComputingDevice | None = None, apply_to: list[str] | None = None, seed: int | None = None, error_on_unexpected_behaviour: bool | None = None) -> None
```

##### `__init__`

```python
def __init__(self, roi_size: numpy.ndarray[numpy.int32[3, 1]], *, device: ComputingDevice | None = None, apply_to: list[str] | None = None, seed: int | None = None, error_on_unexpected_behaviour: bool | None = None) -> None
```

### `RandomResolutionReductionOperation`

```python
class RandomResolutionReductionOperation(Operation)
```

Downsamples the image to a target_spacing and upsamples again to the original spacing to reduce image information.
The `target_spacing` is sampled uniformly and independently in each dimension between the corresponding image spacing and `max_spacing`.

Args:
    max_spacing: maximum spacing per dimension which the target spacing is randomly sampled from. Minimum sampling spacing is the maximum (over all frames of the image set) spacing
                                            per dimension of the input SharedImageSet. Default: [0.0, 0.0, 0.0]
    probability: probability of applying this Operation. Default: 1.0
    device: Specifies whether this Operation should run on CPU or GPU.
    seed: Specifies seeding for any randomness that might be contained in this operation.
    error_on_unexpected_behaviour: Specifies whether to throw an exception instead of warning about unexpected behavior.
    apply_to: Specifies fields in a DataItem that this Operation should process (if empty, will select suitable fields based on the current `~imfusion.machinelearning.Operation.processing_policy`)
    record_identifier: Unused for this operation as it is not invertible

#### Methods

##### `__init__`

```python
def __init__(self, *, device: ComputingDevice | None = None, apply_to: list[str] | None = None, seed: int | None = None, error_on_unexpected_behaviour: bool | None = None) -> None
```

##### `__init__`

```python
def __init__(self, max_spacing: numpy.ndarray[numpy.float64[3, 1]] = ..., probability: float = 1.0, *, device: ComputingDevice | None = None, apply_to: list[str] | None = None, seed: int | None = None, error_on_unexpected_behaviour: bool | None = None) -> None
```

### `RandomRotationOperation`

```python
class RandomRotationOperation(Operation)
```

Rotate input images and label maps with random angles.

Args:
    angles_range: List of floats specifying the upper bound (in degrees) of the range from with the rotation angles will be drawn uniformly. Only the third component should be non-zero for 2D images. Default: [0, 0, 0]
    adjust_size: Increase image size to include the whole rotated image or keep current dimensions. Default: False
    apply_now: Bake transformation right way (otherwise, just changes the matrix). Default: False
    probability: Float in [0;1] defining the probability for the operation to be executed. Default: 1.0
    device: Specifies whether this Operation should run on CPU or GPU.
    seed: Specifies seeding for any randomness that might be contained in this operation.
    error_on_unexpected_behaviour: Specifies whether to throw an exception instead of warning about unexpected behavior.
    apply_to: Specifies fields in a DataItem that this Operation should process (if empty, will select suitable fields based on the current `~imfusion.machinelearning.Operation.processing_policy`)
    record_identifier: Unused for this operation as it is not invertible

#### Methods

##### `__init__`

```python
def __init__(self, *, device: ComputingDevice | None = None, apply_to: list[str] | None = None, seed: int | None = None, error_on_unexpected_behaviour: bool | None = None) -> None
```

##### `__init__`

```python
def __init__(self, angles_range: numpy.ndarray[numpy.float64[3, 1]] = ..., adjust_size: bool = False, apply_now: bool = False, probability: float = 1.0, *, device: ComputingDevice | None = None, apply_to: list[str] | None = None, seed: int | None = None, error_on_unexpected_behaviour: bool | None = None) -> None
```

### `RandomScalingOperation`

```python
class RandomScalingOperation(Operation)
```

Scale input images and label maps with random factors.

Args:
    scales_range (vec3): List of floats specifying the upper bound of the range from which the scaling ofset will be sampled.
                                             The scaling factor will be drawn uniformly within [1-scale, 1+scale]. Scale should be between 0 and 1. Default: [0.5, 0.5, 0.5]
    log_scales_range (vec3): List of floats specifying the upper bound of the range from which the scaling factor will be drawn uniformly in log scale.
                             The scaling will then be distributed within [1/log_scale, log_scale]. Default: [2., 2., 2.]
    device: Specifies whether this Operation should run on CPU or GPU.
    seed: Specifies seeding for any randomness that might be contained in this operation.
    error_on_unexpected_behaviour: Specifies whether to throw an exception instead of warning about unexpected behavior.
    apply_to: Specifies fields in a DataItem that this Operation should process (if empty, will select suitable fields based on the current `~imfusion.machinelearning.Operation.processing_policy`)
    record_identifier: Unused for this operation as it is not invertible
    log_parameterization (bool): If true, uses the log scales range parameterization, otherwise uses the scales range parameterization. Default: False
    apply_now (bool): Bake transformation right way (otherwise, just changes the matrix). Default: False
    probability (float): Float in [0;1] defining the probability for the operation to be executed. Default: 1.0

#### Methods

##### `__init__`

```python
def __init__(self, *, device: ComputingDevice | None = None, apply_to: list[str] | None = None, seed: int | None = None, error_on_unexpected_behaviour: bool | None = None) -> None
```

##### `__init__`

```python
def __init__(self, scales_range: numpy.ndarray[numpy.float64[3, 1]] = ..., log_scales_range: numpy.ndarray[numpy.float64[3, 1]] = ..., log_parameterization: bool = False, apply_now: bool = False, probability: float = 1.0, *, device: ComputingDevice | None = None, apply_to: list[str] | None = None, seed: int | None = None, error_on_unexpected_behaviour: bool | None = None) -> None
```

### `RandomSmoothOperation`

```python
class RandomSmoothOperation(Operation)
```

Apply a random smoothing on the image (Gaussian kernel).
The kernel can be parameterized either in pixel or in mm, and can be anisotropic.
The half kernel size is distributed uniformly between half_kernel_bounds[0] and half_kernel_bounds[1].
:math:`     extnormal{image_output} =       extnormal{image} *      extnormal{gaussian_kernel}(\sigma)` , with
:math:`\sigma \sim U(       extnormal{half_kernel_bounds}[0],       extnormal{half_kernel_bounds}[1])`

Args:
    half_kernel_bounds: Bounds for the half kernel size. The final kernel size is 2 times the sampled half kernel size plus one. Default: [1, 1]
    kernel_size_in_mm: Interpret kernel size as mm. Otherwise uses pixels. Default: False
    isotropic: Forces the randomly drawn kernel size to be isotropic. Default: True
    probability: Value in [0.0; 1.0] indicating the probability of this operation to be performed. Default: 1.0
    device: Specifies whether this Operation should run on CPU or GPU.
    seed: Specifies seeding for any randomness that might be contained in this operation.
    error_on_unexpected_behaviour: Specifies whether to throw an exception instead of warning about unexpected behavior.
    apply_to: Specifies fields in a DataItem that this Operation should process (if empty, will select suitable fields based on the current `~imfusion.machinelearning.Operation.processing_policy`)
    record_identifier: Unused for this operation as it is not invertible

#### Methods

##### `__init__`

```python
def __init__(self, *, device: ComputingDevice | None = None, apply_to: list[str] | None = None, seed: int | None = None, error_on_unexpected_behaviour: bool | None = None) -> None
```

##### `__init__`

```python
def __init__(self, half_kernel_bounds: numpy.ndarray[numpy.float64[2, 1]] = ..., kernel_size_in_mm: bool = False, isotropic: bool = True, probability: float = 1.0, *, device: ComputingDevice | None = None, apply_to: list[str] | None = None, seed: int | None = None, error_on_unexpected_behaviour: bool | None = None) -> None
```

### `RandomTemplateInpaintingOperation`

```python
class RandomTemplateInpaintingOperation(Operation)
```

Inpaints a template into an image with randomly selected spatial and intensity transformation in a given range.

Args:
    template_paths: paths from which a template .imf file is randomly loaded.
    rotation_range: rotation of template in degrees per axis randomly sampled from [-rotation_range, rotation_range]. Default: [0, 0, 0]
    translation_range: translation of template in degrees per axis randomly sampled from [-translation_range, translation_range]. Default: [0, 0, 0]
    template_mult_factor_range: Multiply template intensities with a factor randomly sampled from this range. Default: [0.0, 0.0]
    add_values_to_existing: Adding values to input image rather than replacing them. Default: False
    probability: Float in [0;1] defining the probability for the operation to be executed. Default: 1.0
    device: Specifies whether this Operation should run on CPU or GPU.
    seed: Specifies seeding for any randomness that might be contained in this operation.
    error_on_unexpected_behaviour: Specifies whether to throw an exception instead of warning about unexpected behavior.
    apply_to: Specifies fields in a DataItem that this Operation should process (if empty, will select suitable fields based on the current `~imfusion.machinelearning.Operation.processing_policy`)
    record_identifier: Unused for this operation as it is not invertible

#### Methods

##### `__init__`

```python
def __init__(self, *, device: ComputingDevice | None = None, apply_to: list[str] | None = None, seed: int | None = None, error_on_unexpected_behaviour: bool | None = None) -> None
```

##### `__init__`

```python
def __init__(self, template_paths: list[str] = [], rotation_range: numpy.ndarray[numpy.float64[3, 1]] = ..., translation_range: numpy.ndarray[numpy.float64[3, 1]] = ..., template_mult_factor_range: numpy.ndarray[numpy.float64[2, 1]] = ..., add_values_to_existing: bool = False, probability: float = 1.0, *, device: ComputingDevice | None = None, apply_to: list[str] | None = None, seed: int | None = None, error_on_unexpected_behaviour: bool | None = None) -> None
```

### `RecombinePatchesOperation`

```python
class RecombinePatchesOperation(Operation)
```

Operation to recombine image patches back into a full image.
        This operation is typically used in conjunction with `~imfusion.machinelearning.SplitIntoPatchesOperation` to reconstruct
        a full image from its patches after processing (e.g., after neural network inference).

        The operation handles overlapping patches by averaging the overlapping regions.
        For each output pixel, the final value is computed as the weighted average of all patches that
        contain that pixel. The weighting mode is specified by the `~imfusion.RecombineMode` parameter.

        It requires input images to have a `~imfusion.machinelearning.PatchesFromImageDataComponent` that
        stores the location of each patch in the original image. This component is automatically added
        by the `~imfusion.machinelearning.SplitIntoPatchesOperation` or by the `~imfusion.machinelearning.SplitROISampler`.

        Two recombination modes are supported:

        - `~imfusion.RecombineMode.DEFAULT`: Simple averaging of overlapping regions
        - `~imfusion.RecombineMode.WEIGHTED`: Weighted averaging of overlapping regions

        Note: Both GPU and CPU computing devices are supported, via the ComputingDevice parameter in Operation.

        Note: `~imfusion.RecombineMode` can be automatically converted from a string. This means you can directly pass a string like "default" or "weighted" to the mode parameter instead of using the enum values.

        Args:
                mode: The mode for recombining the patches. Default: `~imfusion.RecombineMode.DEFAULT`
        
device: Specifies whether this Operation should run on CPU or GPU.
seed: Specifies seeding for any randomness that might be contained in this operation.
error_on_unexpected_behaviour: Specifies whether to throw an exception instead of warning about unexpected behavior.
apply_to: Specifies fields in a DataItem that this Operation should process (if empty, will select suitable fields based on the current `~imfusion.machinelearning.Operation.processing_policy`)
record_identifier: Unused for this operation as it is not invertible

#### Methods

##### `__init__`

```python
def __init__(self, *, device: ComputingDevice | None = None, apply_to: list[str] | None = None, seed: int | None = None, error_on_unexpected_behaviour: bool | None = None) -> None
```

##### `__init__`

```python
def __init__(self, mode: RecombineMode = RecombineMode.DEFAULT, *, device: ComputingDevice | None = None, apply_to: list[str] | None = None, seed: int | None = None, error_on_unexpected_behaviour: bool | None = None) -> None
```

### `RectifyRotationOperation`

```python
class RectifyRotationOperation(Operation)
```

Sets the image matrix to the closest xyz-axis aligned rotation, effectively making every rotation angle a multiple of 90 degrees.
This is useful when the values of the rotation are unimportant but the axis flips need to be preserved.
If used before `~imfusion.machinelearning.BakeTransformationOperation`, this operation will avoid oblique angles and a lot of zero padding.

Args:

    device: Specifies whether this Operation should run on CPU or GPU.
    seed: Specifies seeding for any randomness that might be contained in this operation.
    error_on_unexpected_behaviour: Specifies whether to throw an exception instead of warning about unexpected behavior.
    apply_to: Specifies fields in a DataItem that this Operation should process (if empty, will select suitable fields based on the current `~imfusion.machinelearning.Operation.processing_policy`)
    record_identifier: Unused for this operation as it is not invertible

#### Methods

##### `__init__`

```python
def __init__(self, *, device: ComputingDevice | None = None, apply_to: list[str] | None = None, seed: int | None = None, error_on_unexpected_behaviour: bool | None = None) -> None
```

##### `__init__`

```python
def __init__(self, *, device: ComputingDevice | None = None, apply_to: list[str] | None = None, seed: int | None = None, error_on_unexpected_behaviour: bool | None = None) -> None
```

### `RemoveMaskOperation`

```python
class RemoveMaskOperation(Operation)
```

Removes the mask of all input images.

Args:

    device: Specifies whether this Operation should run on CPU or GPU.
    seed: Specifies seeding for any randomness that might be contained in this operation.
    error_on_unexpected_behaviour: Specifies whether to throw an exception instead of warning about unexpected behavior.
    apply_to: Specifies fields in a DataItem that this Operation should process (if empty, will select suitable fields based on the current `~imfusion.machinelearning.Operation.processing_policy`)
    record_identifier: Unused for this operation as it is not invertible

#### Methods

##### `__init__`

```python
def __init__(self, *, device: ComputingDevice | None = None, apply_to: list[str] | None = None, seed: int | None = None, error_on_unexpected_behaviour: bool | None = None) -> None
```

##### `__init__`

```python
def __init__(self, *, device: ComputingDevice | None = None, apply_to: list[str] | None = None, seed: int | None = None, error_on_unexpected_behaviour: bool | None = None) -> None
```

### `RemoveOperation`

```python
class RemoveOperation(Operation)
```

Removes a set of fields from a data item.

Args:
    apply_to: fields to mark as targets (will initialize the underlying `apply_to` parameter)
    device: Specifies whether this Operation should run on CPU or GPU.
    seed: Specifies seeding for any randomness that might be contained in this operation.
    error_on_unexpected_behaviour: Specifies whether to throw an exception instead of warning about unexpected behavior.
    record_identifier: Unused for this operation as it is not invertible

#### Methods

##### `__init__`

```python
def __init__(self, *, device: ComputingDevice | None = None, seed: int | None = None, error_on_unexpected_behaviour: bool | None = None) -> None
```

##### `__init__`

```python
def __init__(self, source: set[str], *, device: ComputingDevice | None = None, seed: int | None = None, error_on_unexpected_behaviour: bool | None = None) -> None
```

### `RenameOperation`

```python
class RenameOperation(Operation)
```

Renames a set of fields of a data item.

Args:
    source: list of the elements to be replaced
    target: list of names of the new elements (must match the size of source)
    throw_error_on_missing_source: if source field is missing, then throw an error (otherwise warn about unexpected behavior and do nothing). Default: True
    throw_error_on_existing_target: if target field already exists, then throw an error (otherwise warn about unexpected behavior and overwrite it). Default: True
    device: Specifies whether this Operation should run on CPU or GPU.
    seed: Specifies seeding for any randomness that might be contained in this operation.
    error_on_unexpected_behaviour: Specifies whether to throw an exception instead of warning about unexpected behavior.
    apply_to: Specifies fields in a DataItem that this Operation should process (if empty, will select suitable fields based on the current `~imfusion.machinelearning.Operation.processing_policy`)
    record_identifier: Unused for this operation as it is not invertible

#### Methods

##### `__init__`

```python
def __init__(self, *, device: ComputingDevice | None = None, apply_to: list[str] | None = None, seed: int | None = None, error_on_unexpected_behaviour: bool | None = None) -> None
```

##### `__init__`

```python
def __init__(self, source: list[str], target: list[str], throw_error_on_missing_source: bool = True, throw_error_on_existing_target: bool = True, *, device: ComputingDevice | None = None, apply_to: list[str] | None = None, seed: int | None = None, error_on_unexpected_behaviour: bool | None = None) -> None
```

### `ReplaceLabelsValuesOperation`

```python
class ReplaceLabelsValuesOperation(Operation)
```

Replace some label values with other values (only works for integer-typed labels).

For convenience purposes, a default value can be set, in which case all not explicitly defined non-zero input values will be assigned this value.

Args:
    old_values: List of integer values to be replaced. All values that are not in this list will remain unchanged.
    new_values: List of integer values to replace `old_values`. It must have the same size as `old_values`, since there should be a one-to-one mapping.
    update_labelsdatacomponent: Replaces the old-values in the LabelsDataComponent with the mapped ones. Default: True
    default_value: If set, this value will be assigned to all non-zero labels that have not been explicitly assigned. Default: None

    device: Specifies whether this Operation should run on CPU or GPU.
    seed: Specifies seeding for any randomness that might be contained in this operation.
    error_on_unexpected_behaviour: Specifies whether to throw an exception instead of warning about unexpected behavior.
    apply_to: Specifies fields in a DataItem that this Operation should process (if empty, will select suitable fields based on the current `~imfusion.machinelearning.Operation.processing_policy`)
    record_identifier: Unused for this operation as it is not invertible

#### Methods

##### `__init__`

```python
def __init__(self, *, device: ComputingDevice | None = None, apply_to: list[str] | None = None, seed: int | None = None, error_on_unexpected_behaviour: bool | None = None) -> None
```

##### `__init__`

```python
def __init__(self, old_values: list[int], new_values: list[int], update_labelsdatacomponent: bool = True, default_value: int | None = None, *, device: ComputingDevice | None = None, apply_to: list[str] | None = None, seed: int | None = None, error_on_unexpected_behaviour: bool | None = None) -> None
```

### `ResampleDimsOperation`

```python
class ResampleDimsOperation(Operation)
```

Resample the input to fixed target dimensions.

Args:
    target_dims: Target dimensions in pixels as [Width, Height, Slices].
    device: Specifies whether this Operation should run on CPU or GPU.
    seed: Specifies seeding for any randomness that might be contained in this operation.
    error_on_unexpected_behaviour: Specifies whether to throw an exception instead of warning about unexpected behavior.
    apply_to: Specifies fields in a DataItem that this Operation should process (if empty, will select suitable fields based on the current `~imfusion.machinelearning.Operation.processing_policy`)
    record_identifier: Unused for this operation as it is not invertible

#### Methods

##### `__init__`

```python
def __init__(self, *, device: ComputingDevice | None = None, apply_to: list[str] | None = None, seed: int | None = None, error_on_unexpected_behaviour: bool | None = None) -> None
```

##### `__init__`

```python
def __init__(self, target_dims: numpy.ndarray[numpy.int32[3, 1]], *, device: ComputingDevice | None = None, apply_to: list[str] | None = None, seed: int | None = None, error_on_unexpected_behaviour: bool | None = None) -> None
```

### `ResampleKeepingAspectRatioOperation`

```python
class ResampleKeepingAspectRatioOperation(Operation)
```

Resample input to target dimensions while keeping aspect ratio of original images.
The target dimensions are specified by either:

1. one target dimension, i.e.: `target_dim_x: 128`. In such case the resampling will keep the aspect ratio of dimension y and z wrt x.
2. two target dimensions, i.e.: `target_dim_x: 128`, i.e.: `target_dim_y: 128` and which dimension to consider for preserving the aspect ratio of the leftover dimension, i.e. `keep_aspect_ratio_wrt: x`.

Args:
    keep_aspect_ratio_wrt: specifies the dimension to which lock the aspect ratio, please assign either of "", "x", "y" or "z".
                                                    If only one target_dim is specified, then this can be empty (or must match the given target_dim).
                                                    If all the target_dim args are specified, then this argument must be empty, however, in this case ResampleDims should then be preferred.
    target_dim_x: either the target width or None if this dimension will be computed automatically by preserving the aspect ratio.
    target_dim_y: either the target height or None if this dimension will be computed automatically by preserving the aspect ratio.
    target_dim_z: for 3D images, either the target slices or None if this dimension will be computed automatically by preserving the aspect ratio.
    device: Specifies whether this Operation should run on CPU or GPU.
    seed: Specifies seeding for any randomness that might be contained in this operation.
    error_on_unexpected_behaviour: Specifies whether to throw an exception instead of warning about unexpected behavior.
    apply_to: Specifies fields in a DataItem that this Operation should process (if empty, will select suitable fields based on the current `~imfusion.machinelearning.Operation.processing_policy`)
    record_identifier: Unused for this operation as it is not invertible

#### Methods

##### `__init__`

```python
def __init__(self, *, device: ComputingDevice | None = None, apply_to: list[str] | None = None, seed: int | None = None, error_on_unexpected_behaviour: bool | None = None) -> None
```

##### `__init__`

```python
def __init__(self, keep_aspect_ratio_wrt: str = '', target_dim_x: int | None = 1, target_dim_y: int | None = 1, target_dim_z: int | None = 1, *, device: ComputingDevice | None = None, apply_to: list[str] | None = None, seed: int | None = None, error_on_unexpected_behaviour: bool | None = None) -> None
```

### `ResampleOperation`

```python
class ResampleOperation(Operation)
```

Resample the input to a fixed target resolution.

Args:
    resolution: Target spacing in mm.
    preserve_extent: Preserve the exact spatial extent of the image, adjusting the output spacing `resolution` accordingly (since the extent is not always a multiple of `resolution`). Default: True
    device: Specifies whether this Operation should run on CPU or GPU.
    seed: Specifies seeding for any randomness that might be contained in this operation.
    error_on_unexpected_behaviour: Specifies whether to throw an exception instead of warning about unexpected behavior.
    apply_to: Specifies fields in a DataItem that this Operation should process (if empty, will select suitable fields based on the current `~imfusion.machinelearning.Operation.processing_policy`)
    record_identifier: Unused for this operation as it is not invertible

#### Methods

##### `__init__`

```python
def __init__(self, *, device: ComputingDevice | None = None, apply_to: list[str] | None = None, seed: int | None = None, error_on_unexpected_behaviour: bool | None = None) -> None
```

##### `__init__`

```python
def __init__(self, resolution: numpy.ndarray[numpy.float64[3, 1]], preserve_extent: bool = True, *, device: ComputingDevice | None = None, apply_to: list[str] | None = None, seed: int | None = None, error_on_unexpected_behaviour: bool | None = None) -> None
```

### `ResampleToInputOperation`

```python
class ResampleToInputOperation(Operation)
```

Resample the input image with respect to the image in `~imfusion.ReferenceImageDataComponent`

Args:

    device: Specifies whether this Operation should run on CPU or GPU.
    seed: Specifies seeding for any randomness that might be contained in this operation.
    error_on_unexpected_behaviour: Specifies whether to throw an exception instead of warning about unexpected behavior.
    apply_to: Specifies fields in a DataItem that this Operation should process (if empty, will select suitable fields based on the current `~imfusion.machinelearning.Operation.processing_policy`)
    record_identifier: Unused for this operation as it is not invertible

#### Methods

##### `__init__`

```python
def __init__(self, *, device: ComputingDevice | None = None, apply_to: list[str] | None = None, seed: int | None = None, error_on_unexpected_behaviour: bool | None = None) -> None
```

##### `__init__`

```python
def __init__(self, *, device: ComputingDevice | None = None, apply_to: list[str] | None = None, seed: int | None = None, error_on_unexpected_behaviour: bool | None = None) -> None
```

### `ResolutionReductionOperation`

```python
class ResolutionReductionOperation(Operation)
```

Downsamples the image to the target_spacing and upsamples again to the original spacing to reduce image information.

Args:
    target_spacing: spacing per dimension to which the image is resampled before it is resampled back
    device: Specifies whether this Operation should run on CPU or GPU.
    seed: Specifies seeding for any randomness that might be contained in this operation.
    error_on_unexpected_behaviour: Specifies whether to throw an exception instead of warning about unexpected behavior.
    apply_to: Specifies fields in a DataItem that this Operation should process (if empty, will select suitable fields based on the current `~imfusion.machinelearning.Operation.processing_policy`)
    record_identifier: Unused for this operation as it is not invertible

#### Methods

##### `__init__`

```python
def __init__(self, *, device: ComputingDevice | None = None, apply_to: list[str] | None = None, seed: int | None = None, error_on_unexpected_behaviour: bool | None = None) -> None
```

##### `__init__`

```python
def __init__(self, target_spacing: numpy.ndarray[numpy.float64[3, 1]], *, device: ComputingDevice | None = None, apply_to: list[str] | None = None, seed: int | None = None, error_on_unexpected_behaviour: bool | None = None) -> None
```

### `RotationOperation`

```python
class RotationOperation(Operation)
```

Rotate input images and label maps with fixed angles.

Args:
    angles: Rotation angles in degrees. Only the third component should be non-zero for 2D images. Default: [0, 0, 0]
    adjust_size: Increase image size to include the whole rotated image or keep current dimensions. Default: False
    apply_now: Bake transformation right way (otherwise, just changes the matrix). Default: False
    device: Specifies whether this Operation should run on CPU or GPU.
    seed: Specifies seeding for any randomness that might be contained in this operation.
    error_on_unexpected_behaviour: Specifies whether to throw an exception instead of warning about unexpected behavior.
    apply_to: Specifies fields in a DataItem that this Operation should process (if empty, will select suitable fields based on the current `~imfusion.machinelearning.Operation.processing_policy`)
    record_identifier: Unused for this operation as it is not invertible

#### Methods

##### `__init__`

```python
def __init__(self, *, device: ComputingDevice | None = None, apply_to: list[str] | None = None, seed: int | None = None, error_on_unexpected_behaviour: bool | None = None) -> None
```

##### `__init__`

```python
def __init__(self, angles: numpy.ndarray[numpy.float64[3, 1]] = ..., adjust_size: bool = False, apply_now: bool = False, *, device: ComputingDevice | None = None, apply_to: list[str] | None = None, seed: int | None = None, error_on_unexpected_behaviour: bool | None = None) -> None
```

### `RunModelOperation`

```python
class RunModelOperation(Operation)
```

Run a machine learning model on the input item and merge the prediction to the input item.
The input field names specified in the model config yaml will be use to determine which fields
in the input data item the model is run.
If the model doesn't specify any input field, i.e. is a single input model, the
user can either provide an input data item with a single image element,
or use the `apply_to` to specify on which field the model should be run.
The input item will be populated with the model prediction. The field names are those specified
in the model configuration. If no output name is specified (i.e. single output case), the
prediction will be associated to the field "Prediction"

Args:
    config_path: path to the YAML configuration file of the pixelwise model
    device: Specifies whether this Operation should run on CPU or GPU.
    seed: Specifies seeding for any randomness that might be contained in this operation.
    error_on_unexpected_behaviour: Specifies whether to throw an exception instead of warning about unexpected behavior.
    apply_to: Specifies fields in a DataItem that this Operation should process (if empty, will select suitable fields based on the current `~imfusion.machinelearning.Operation.processing_policy`)
    record_identifier: Unused for this operation as it is not invertible

#### Methods

##### `__init__`

```python
def __init__(self, *, device: ComputingDevice | None = None, apply_to: list[str] | None = None, seed: int | None = None, error_on_unexpected_behaviour: bool | None = None) -> None
```

##### `__init__`

```python
def __init__(self, config_path: str, *, device: ComputingDevice | None = None, apply_to: list[str] | None = None, seed: int | None = None, error_on_unexpected_behaviour: bool | None = None) -> None
```

### `SISBasedElement`

```python
class SISBasedElement(DataElement)
```

Base class for DataElements that wrap SharedImageSet data.

This class provides a common interface for elements that store their content
as SharedImageSet objects, including images and vectors. It enables uniform
access to the underlying image data structure.

#### Methods

##### `sis`

*property*

```python
def sis(self) -> imfusion.SharedImageSet
```

Access to the underlying SharedImageSet.

##### `sis`

```python
def sis(self, arg1: imfusion.SharedImageSet) -> None
```

##### `to_sis`

```python
def to_sis(self) -> imfusion.SharedImageSet
```

Get the underlying SharedImageSet.

.. deprecated::
        Use the `sis` property instead.

Returns:
        The underlying SharedImageSet

### `ScalingOperation`

```python
class ScalingOperation(Operation)
```

Scale input images and label maps with fixed factors.

Args:
    scales: Scaling factor applied to each dimension. Default: [1, 1, 1]
    apply_now: Bake transformation right way (otherwise, just changes the matrix). Default: True
    device: Specifies whether this Operation should run on CPU or GPU.
    seed: Specifies seeding for any randomness that might be contained in this operation.
    error_on_unexpected_behaviour: Specifies whether to throw an exception instead of warning about unexpected behavior.
    apply_to: Specifies fields in a DataItem that this Operation should process (if empty, will select suitable fields based on the current `~imfusion.machinelearning.Operation.processing_policy`)
    record_identifier: Unused for this operation as it is not invertible

#### Methods

##### `__init__`

```python
def __init__(self, *, device: ComputingDevice | None = None, apply_to: list[str] | None = None, seed: int | None = None, error_on_unexpected_behaviour: bool | None = None) -> None
```

##### `__init__`

```python
def __init__(self, scales: numpy.ndarray[numpy.float64[3, 1]] = ..., apply_now: bool = True, *, device: ComputingDevice | None = None, apply_to: list[str] | None = None, seed: int | None = None, error_on_unexpected_behaviour: bool | None = None) -> None
```

### `SelectChannelsOperation`

```python
class SelectChannelsOperation(Operation)
```

Keeps a subset of the input channels specified by the selected channel indices (0-based indexing).

Args:
    selected_channels: List of channels to be selected in input. If empty, use all channels.
    device: Specifies whether this Operation should run on CPU or GPU.
    seed: Specifies seeding for any randomness that might be contained in this operation.
    error_on_unexpected_behaviour: Specifies whether to throw an exception instead of warning about unexpected behavior.
    apply_to: Specifies fields in a DataItem that this Operation should process (if empty, will select suitable fields based on the current `~imfusion.machinelearning.Operation.processing_policy`)
    record_identifier: Unused for this operation as it is not invertible

#### Methods

##### `__init__`

```python
def __init__(self, *, device: ComputingDevice | None = None, apply_to: list[str] | None = None, seed: int | None = None, error_on_unexpected_behaviour: bool | None = None) -> None
```

##### `__init__`

```python
def __init__(self, selected_channels: list[int], *, device: ComputingDevice | None = None, apply_to: list[str] | None = None, seed: int | None = None, error_on_unexpected_behaviour: bool | None = None) -> None
```

### `SetLabelModalityOperation`

```python
class SetLabelModalityOperation(Operation)
```

Sets the input modality. If the target modality is `~imfusion.Data.Modality.LABEL`, warns and skips fields that are not unsigned 8-bit integer. The default processing policy is to apply to targets only.

Args:
    label_names: List of non-background label names. The label with index zero is assigned the name 'Background'.
    device: Specifies whether this Operation should run on CPU or GPU.
    seed: Specifies seeding for any randomness that might be contained in this operation.
    error_on_unexpected_behaviour: Specifies whether to throw an exception instead of warning about unexpected behavior.
    apply_to: Specifies fields in a DataItem that this Operation should process (if empty, will select suitable fields based on the current `~imfusion.machinelearning.Operation.processing_policy`)
    record_identifier: Unused for this operation as it is not invertible

#### Methods

##### `__init__`

```python
def __init__(self, *, device: ComputingDevice | None = None, apply_to: list[str] | None = None, seed: int | None = None, error_on_unexpected_behaviour: bool | None = None) -> None
```

##### `__init__`

```python
def __init__(self, label_names: list[str], *, device: ComputingDevice | None = None, apply_to: list[str] | None = None, seed: int | None = None, error_on_unexpected_behaviour: bool | None = None) -> None
```

### `SetMatrixToIdentityOperation`

```python
class SetMatrixToIdentityOperation(Operation)
```

Set the matrices of all images to identity (associated landmarks and boxes will be moved accordingly).

Args:

    device: Specifies whether this Operation should run on CPU or GPU.
    seed: Specifies seeding for any randomness that might be contained in this operation.
    error_on_unexpected_behaviour: Specifies whether to throw an exception instead of warning about unexpected behavior.
    apply_to: Specifies fields in a DataItem that this Operation should process (if empty, will select suitable fields based on the current `~imfusion.machinelearning.Operation.processing_policy`)
    record_identifier: Unused for this operation as it is not invertible

#### Methods

##### `__init__`

```python
def __init__(self, *, device: ComputingDevice | None = None, apply_to: list[str] | None = None, seed: int | None = None, error_on_unexpected_behaviour: bool | None = None) -> None
```

##### `__init__`

```python
def __init__(self, *, device: ComputingDevice | None = None, apply_to: list[str] | None = None, seed: int | None = None, error_on_unexpected_behaviour: bool | None = None) -> None
```

### `SetModalityOperation`

```python
class SetModalityOperation(Operation)
```

Sets the input modality. If the target modality is `~imfusion.Data.Modality.LABEL`, warns and skips fields that are not unsigned 8-bit integer. The default processing policy is to apply to all fields.

Args:
    modality: Modality to set the input to.
    label_names: List of non-background label names. The label with index zero is assigned the name 'Background'. Default: [].
    device: Specifies whether this Operation should run on CPU or GPU.
    seed: Specifies seeding for any randomness that might be contained in this operation.
    error_on_unexpected_behaviour: Specifies whether to throw an exception instead of warning about unexpected behavior.
    apply_to: Specifies fields in a DataItem that this Operation should process (if empty, will select suitable fields based on the current `~imfusion.machinelearning.Operation.processing_policy`)
    record_identifier: Unused for this operation as it is not invertible

#### Methods

##### `__init__`

```python
def __init__(self, *, device: ComputingDevice | None = None, apply_to: list[str] | None = None, seed: int | None = None, error_on_unexpected_behaviour: bool | None = None) -> None
```

##### `__init__`

```python
def __init__(self, modality: imfusion.Data.Modality, label_names: list[str] = [], *, device: ComputingDevice | None = None, apply_to: list[str] | None = None, seed: int | None = None, error_on_unexpected_behaviour: bool | None = None) -> None
```

### `SetSpacingOperation`

```python
class SetSpacingOperation(Operation)
```

Modify images so that image elements have specified spacing (associated landmarks and boxes will be moved accordingly).
Args:
    spacing : Target spacing. 
    device: Specifies whether this Operation should run on CPU or GPU.
    seed: Specifies seeding for any randomness that might be contained in this operation.
    error_on_unexpected_behaviour: Specifies whether to throw an exception instead of warning about unexpected behavior.
    apply_to: Specifies fields in a DataItem that this Operation should process (if empty, will select suitable fields based on the current `~imfusion.machinelearning.Operation.processing_policy`)
    record_identifier: Unused for this operation as it is not invertible

#### Methods

##### `__init__`

```python
def __init__(self, *, device: ComputingDevice | None = None, apply_to: list[str] | None = None, seed: int | None = None, error_on_unexpected_behaviour: bool | None = None) -> None
```

##### `__init__`

```python
def __init__(self, spacing: numpy.ndarray[numpy.float64[3, 1]] = ..., *, device: ComputingDevice | None = None, apply_to: list[str] | None = None, seed: int | None = None, error_on_unexpected_behaviour: bool | None = None) -> None
```

### `SigmoidOperation`

```python
class SigmoidOperation(Operation)
```

Apply a sigmoid function on the input image.
:math:`     extnormal{output} = 1.0/(1.0 + \exp(-   extnormal{scale} *      extnormal{input}))`

Args:
    scale: Scale parameter within the sigmoid function. Default: 1.0
    device: Specifies whether this Operation should run on CPU or GPU.
    seed: Specifies seeding for any randomness that might be contained in this operation.
    error_on_unexpected_behaviour: Specifies whether to throw an exception instead of warning about unexpected behavior.
    apply_to: Specifies fields in a DataItem that this Operation should process (if empty, will select suitable fields based on the current `~imfusion.machinelearning.Operation.processing_policy`)
    record_identifier: Unused for this operation as it is not invertible

#### Methods

##### `__init__`

```python
def __init__(self, *, device: ComputingDevice | None = None, apply_to: list[str] | None = None, seed: int | None = None, error_on_unexpected_behaviour: bool | None = None) -> None
```

##### `__init__`

```python
def __init__(self, scale: float = 1.0, *, device: ComputingDevice | None = None, apply_to: list[str] | None = None, seed: int | None = None, error_on_unexpected_behaviour: bool | None = None) -> None
```

### `SmoothOperation`

```python
class SmoothOperation(Operation)
```

Run a convolution with a Gaussian kernel on the input image.
The kernel can be parameterized either in pixel or in mm, and can be anisotropic.

Args:
    half_kernel_size: Half size of the convolution kernel in pixels or mm.
    kernel_size_in_mm: Interpret kernel size as mm. Otherwise uses pixels. Default: False
    device: Specifies whether this Operation should run on CPU or GPU.
    seed: Specifies seeding for any randomness that might be contained in this operation.
    error_on_unexpected_behaviour: Specifies whether to throw an exception instead of warning about unexpected behavior.
    apply_to: Specifies fields in a DataItem that this Operation should process (if empty, will select suitable fields based on the current `~imfusion.machinelearning.Operation.processing_policy`)
    record_identifier: Unused for this operation as it is not invertible

#### Methods

##### `__init__`

```python
def __init__(self, *, device: ComputingDevice | None = None, apply_to: list[str] | None = None, seed: int | None = None, error_on_unexpected_behaviour: bool | None = None) -> None
```

##### `__init__`

```python
def __init__(self, half_kernel_size: numpy.ndarray[numpy.float64[3, 1]], kernel_size_in_mm: bool = False, *, device: ComputingDevice | None = None, apply_to: list[str] | None = None, seed: int | None = None, error_on_unexpected_behaviour: bool | None = None) -> None
```

### `SoftmaxOperation`

```python
class SoftmaxOperation(Operation)
```

Computes channel-wise softmax on input.

Args:

    device: Specifies whether this Operation should run on CPU or GPU.
    seed: Specifies seeding for any randomness that might be contained in this operation.
    error_on_unexpected_behaviour: Specifies whether to throw an exception instead of warning about unexpected behavior.
    apply_to: Specifies fields in a DataItem that this Operation should process (if empty, will select suitable fields based on the current `~imfusion.machinelearning.Operation.processing_policy`)
    record_identifier: Unused for this operation as it is not invertible

#### Methods

##### `__init__`

```python
def __init__(self, *, device: ComputingDevice | None = None, apply_to: list[str] | None = None, seed: int | None = None, error_on_unexpected_behaviour: bool | None = None) -> None
```

##### `__init__`

```python
def __init__(self, *, device: ComputingDevice | None = None, apply_to: list[str] | None = None, seed: int | None = None, error_on_unexpected_behaviour: bool | None = None) -> None
```

### `SplitIntoPatchesOperation`

```python
class SplitIntoPatchesOperation(Operation)
```

Operation which splits the input image into overlapping patches for sliding window inference.

The step size is used to compute valid patch positions that cover the full input image. The sampling behavior is controlled by
the patch_step_size parameter, which is used to compute the patch offsets in the input image as a fraction of the specified patch size.

Args:
    patch_size: Target size of the patches to be extracted as [Width, Height, Slices].
    patch_step_size: Controls the step size between patches as a fraction of the patch size. Range [0, 1].
            In cases where the input image is a multiple of the patch size, a step size of 1.0 means no overlapping patches, 
            while a lower step size means a higher number of overlapping patches.
            
            Example: If the input image is 100x100 and the patch size is 50x50, a patch_step_size of 1.0 will result in a 2x2 grid of non-overlapping patches,
            while a patch_step_size of 0.5 will result in a 3x3 grid of patches, with a 25 pixel overlap between adjacent patches.
            
            In cases where the input image is not a multiple of the ROI size, a step size of 1.0 indicates the optimal way of splitting the image in the least possible number of patches.
            Example: If the input image is 100x100 and the ROI size is 40x40, a patch_step_size of 1.0 will result in a 3x3 grid of patches,
            with a 10 pixel overlap between adjacent patches. Conversely, a patch_step_size of 0.5 will result in a 4x4 grid of patches,
            with a 20 pixel overlap between adjacent patches.
    padding_mode: Specifies the padding mode used when the input image is smaller than the specified patch size.
            In this case, the image is padded to the patch size with the specified padding mode.

    device: Specifies whether this Operation should run on CPU or GPU.
    seed: Specifies seeding for any randomness that might be contained in this operation.
    error_on_unexpected_behaviour: Specifies whether to throw an exception instead of warning about unexpected behavior.
    apply_to: Specifies fields in a DataItem that this Operation should process (if empty, will select suitable fields based on the current `~imfusion.machinelearning.Operation.processing_policy`)
    record_identifier: Unused for this operation as it is not invertible
Note: This operation uses the `~imfusion.machinelearning.SplitROISampler` internally.

#### Methods

##### `__init__`

```python
def __init__(self, *, device: ComputingDevice | None = None, apply_to: list[str] | None = None, seed: int | None = None, error_on_unexpected_behaviour: bool | None = None) -> None
```

##### `__init__`

```python
def __init__(self, patch_size: numpy.ndarray[numpy.int32[3, 1]] = ..., patch_step_size: float = 0.8, padding_mode: imfusion.PaddingMode = imfusion.PaddingMode.MIRROR, *, device: ComputingDevice | None = None, apply_to: list[str] | None = None, seed: int | None = None, error_on_unexpected_behaviour: bool | None = None) -> None
```

### `SplitROISampler`

```python
class SplitROISampler(ImageROISampler)
```

Sampler which splits the input image into overlapping ROIs for sliding window inference. This sampler mimics the situation at test-time, when one image needs to be processed in regularly spaced patches.
   
   The step size is used to compute valid ROI positions that cover the full input image. The sampling behavior is controlled by 
   the patch_step_size parameter, which is used to compute the ROIs offsets in the input image as a fraction of the specified ROI size.

Args:
    roi_size: Target size of the ROIs to be extracted as [Width, Height, Slices].
    patch_step_size [0, 1]: Controls the step size between ROIs as a fraction of the ROI size.
            In cases where the input image is a multiple of the ROI size, a step size of 1.0 means no overlapping patches, while a lower step size means a higher number of overlapping patches.
            Example: If the input image is 100x100 and the ROI size is 50x50, a patch_step_size of 1.0 will result in a 2x2 grid of non-overlapping patches, 
            while a patch_step_size of 0.5 will result in a 3x3 grid of patches, with a 25 pixel overlap between adjacent patches.
            In cases where the input image is not a multiple of the ROI size, a step size of 1.0 indicates the optimal way of splitting the image in the least possible number of patches.
            Example: If the input image is 100x100 and the ROI size is 40x40, a patch_step_size of 1.0 will result in a 3x3 grid of patches, 
            with a 10 pixel overlap between adjacent patches. Conversely, a patch_step_size of 0.5 will result in a 4x4 grid of patches, 
            with a 20 pixel overlap between adjacent patches.
    extract_all_patches (bool): When true, returns all overlapping patches according to the step size.
            When false, returns a single randomly selected patch from all possible positions.
    allow_dimension_change (bool): If True, allow padding dimensions equal to 1. This results in changing image dimension e.g. from 2D to 3D. Default: True
    device: Specifies whether this Operation should run on CPU or GPU.
    seed: Specifies seeding for any randomness that might be contained in this operation.
    error_on_unexpected_behaviour: Specifies whether to throw an exception instead of warning about unexpected behavior.
    apply_to: Specifies fields in a DataItem that this Operation should process (if empty, will select suitable fields based on the current `~imfusion.machinelearning.Operation.processing_policy`)
    record_identifier: Unused for this operation as it is not invertible

#### Methods

##### `__init__`

```python
def __init__(self, *, device: ComputingDevice | None = None, apply_to: list[str] | None = None, seed: int | None = None, error_on_unexpected_behaviour: bool | None = None) -> None
```

##### `__init__`

```python
def __init__(self, roi_size: numpy.ndarray[numpy.int32[3, 1]], patch_step_size: float = 0.8, extract_all_patches: bool = False, allow_dimension_change: bool = True, *, device: ComputingDevice | None = None, apply_to: list[str] | None = None, seed: int | None = None, error_on_unexpected_behaviour: bool | None = None) -> None
```

### `StandardizeImageAxesOperation`

```python
class StandardizeImageAxesOperation(Operation)
```

Reorganize the memory buffer of a medical image to ensure anatomical consistency.
This operation rearranges the axes and orientation of the input image to align with right-handed anatomical coordinate systems.

The coordinate system is specified as a 3-character string where:
- 1st character: L (Left, +x) or R (Right, -x)
- 2nd character: P (Posterior, +y) or A (Anterior, -y)
- 3rd character: S (Superior, +z) or I (Inferior, -z)

Supported right-handed coordinate systems:
- LPS: Left-Posterior-Superior (DICOM standard) - {+1, +1, +1}
- RAS: Right-Anterior-Superior (neuroimaging) - {-1, -1, +1}
- LAI: Left-Anterior-Inferior - {+1, -1, -1}
- RPI: Right-Posterior-Inferior - {-1, +1, -1}

The operation uses the rotation matrix of the image and modifies it so that only a non-axis aligned rotation remains.

Note that this operation only re-arranges internal representations but does not modify the actual spatial position and orientation of the image (as opposed to RectifyRotationOperation).
This operation differs from BakeTransformationOperation because it only applies axis-based rotations or flips and therefore does not do any kind of interpolation. Unlike BakeTransformationOperation, a residual rotation might remain in the matrix of the output image.

Args:
    coordinate_system: Target coordinate system (3-character string). Default: "LPS"

    device: Specifies whether this Operation should run on CPU or GPU.
    seed: Specifies seeding for any randomness that might be contained in this operation.
    error_on_unexpected_behaviour: Specifies whether to throw an exception instead of warning about unexpected behavior.
    apply_to: Specifies fields in a DataItem that this Operation should process (if empty, will select suitable fields based on the current `~imfusion.machinelearning.Operation.processing_policy`)
    record_identifier: Unused for this operation as it is not invertible

#### Methods

##### `__init__`

```python
def __init__(self, *, device: ComputingDevice | None = None, apply_to: list[str] | None = None, seed: int | None = None, error_on_unexpected_behaviour: bool | None = None) -> None
```

##### `__init__`

```python
def __init__(self, coordinate_system: str = 'LPS', *, device: ComputingDevice | None = None, apply_to: list[str] | None = None, seed: int | None = None, error_on_unexpected_behaviour: bool | None = None) -> None
```

### `SurfaceDistancesMetric`

```python
class SurfaceDistancesMetric(Metric)
```

Computes surface distance metrics between predicted and target segmentation surfaces.

This metric computes various distance measures between the surfaces of segmented objects,
including mean signed distance, mean absolute distance, and maximum distance.

#### Methods

##### `__init__`

```python
def __init__(self, symmetric: bool = True, crop_margin: int = -1) -> None
```

Constructs a SurfaceDistancesMetric.

Args:
        symmetric: If True, computes bidirectional distances (prediction to target and target to prediction). Default: True
        crop_margin: Margin in voxels to crop before computing distances. If -1, no cropping is performed. Default: -1

##### `compute_distances`

```python
def compute_distances(self, prediction: imfusion.SharedImageSet, target: imfusion.SharedImageSet) -> list[dict[int, SurfaceDistancesMetric.Results]]
```

Computes surface distances between prediction and target.

Args:
        prediction: The predicted segmentation
        target: The target/ground truth segmentation
Returns:
        Results: Object containing computed distance metrics

#### Nested Classes

##### `Results`

```python
class Results
```

Results container for surface distance metric computations.

###### Methods

####### `__init__`

```python
def __init__(self) -> None
```

Initialize an empty Results object.

Creates a results container that will be populated by the SurfaceDistancesMetric.

####### `all_distances`

*property*

```python
def all_distances(self) -> list[float]
```

Vector containing all computed distances in millimeters

####### `all_distances`

```python
def all_distances(self, arg0: list[float]) -> None
```

####### `max_absolute_distance`

*property*

```python
def max_absolute_distance(self) -> float
```

Maximum absolute distance between surfaces in millimeters

####### `max_absolute_distance`

```python
def max_absolute_distance(self, arg0: float) -> None
```

####### `mean_absolute_distance`

*property*

```python
def mean_absolute_distance(self) -> float
```

Mean absolute distance between surfaces in millimeters

####### `mean_absolute_distance`

```python
def mean_absolute_distance(self, arg0: float) -> None
```

####### `mean_signed_distance`

*property*

```python
def mean_signed_distance(self) -> float
```

Mean signed distance between surfaces in millimeters

####### `mean_signed_distance`

```python
def mean_signed_distance(self, arg0: float) -> None
```

### `SwapImageAndLabelsOperation`

```python
class SwapImageAndLabelsOperation(Operation)
```

Swaps image and label map.

Args:

    device: Specifies whether this Operation should run on CPU or GPU.
    seed: Specifies seeding for any randomness that might be contained in this operation.
    error_on_unexpected_behaviour: Specifies whether to throw an exception instead of warning about unexpected behavior.
    apply_to: Specifies fields in a DataItem that this Operation should process (if empty, will select suitable fields based on the current `~imfusion.machinelearning.Operation.processing_policy`)
    record_identifier: Unused for this operation as it is not invertible

#### Methods

##### `__init__`

```python
def __init__(self, *, device: ComputingDevice | None = None, apply_to: list[str] | None = None, seed: int | None = None, error_on_unexpected_behaviour: bool | None = None) -> None
```

##### `__init__`

```python
def __init__(self, *, device: ComputingDevice | None = None, apply_to: list[str] | None = None, seed: int | None = None, error_on_unexpected_behaviour: bool | None = None) -> None
```

### `SyncOperation`

```python
class SyncOperation(Operation)
```

Synchronizes shared memory (CPU &lt;-&gt; OpenGL) of images.

Args:

    device: Specifies whether this Operation should run on CPU or GPU.
    seed: Specifies seeding for any randomness that might be contained in this operation.
    error_on_unexpected_behaviour: Specifies whether to throw an exception instead of warning about unexpected behavior.
    apply_to: Specifies fields in a DataItem that this Operation should process (if empty, will select suitable fields based on the current `~imfusion.machinelearning.Operation.processing_policy`)
    record_identifier: Unused for this operation as it is not invertible

#### Methods

##### `__init__`

```python
def __init__(self, *, device: ComputingDevice | None = None, apply_to: list[str] | None = None, seed: int | None = None, error_on_unexpected_behaviour: bool | None = None) -> None
```

##### `__init__`

```python
def __init__(self, *, device: ComputingDevice | None = None, apply_to: list[str] | None = None, seed: int | None = None, error_on_unexpected_behaviour: bool | None = None) -> None
```

### `TanhOperation`

```python
class TanhOperation(Operation)
```

Apply a tanh function on the input image.
:math:`     extnormal{output} =     anh(    extnormal{scale} *      extnormal{input})`

Args:
    scale: Scale parameter within the tanh function. Default: 1.0
    device: Specifies whether this Operation should run on CPU or GPU.
    seed: Specifies seeding for any randomness that might be contained in this operation.
    error_on_unexpected_behaviour: Specifies whether to throw an exception instead of warning about unexpected behavior.
    apply_to: Specifies fields in a DataItem that this Operation should process (if empty, will select suitable fields based on the current `~imfusion.machinelearning.Operation.processing_policy`)
    record_identifier: Unused for this operation as it is not invertible

#### Methods

##### `__init__`

```python
def __init__(self, *, device: ComputingDevice | None = None, apply_to: list[str] | None = None, seed: int | None = None, error_on_unexpected_behaviour: bool | None = None) -> None
```

##### `__init__`

```python
def __init__(self, scale: float = 1.0, *, device: ComputingDevice | None = None, apply_to: list[str] | None = None, seed: int | None = None, error_on_unexpected_behaviour: bool | None = None) -> None
```

### `TargetTag`

```python
class TargetTag(imfusion.DataComponentBase)
```

Component tag to mark a DataElement as a target/label for training.

This tag is attached to DataElements to indicate they should be used as targets
during training rather than as inputs. It enables automatic handling of target
data in ML pipelines.

#### Methods

##### `__init__`

```python
def __init__(self) -> None
```

Initialize a TargetTag component.

Creates a tag that can be attached to a DataElement to mark it as a target/label.

### `TemplateInpaintingOperation`

```python
class TemplateInpaintingOperation(Operation)
```

Inpaints a template into an image with specified spatial and intensity transformation.

Args:
    template_path: path to load template .imf file.
    template_rotation: rotation of template in degrees per axis. Default: [0, 0, 0]
    template_translation: translation of template in degrees per axis. Default: [0, 0, 0]
    add_values_to_existing: Adding values to input image rather than replacing them. Default: False
    template_mult_factor: Multiply template intensities with this factor. Default: 1.0
    device: Specifies whether this Operation should run on CPU or GPU.
    seed: Specifies seeding for any randomness that might be contained in this operation.
    error_on_unexpected_behaviour: Specifies whether to throw an exception instead of warning about unexpected behavior.
    apply_to: Specifies fields in a DataItem that this Operation should process (if empty, will select suitable fields based on the current `~imfusion.machinelearning.Operation.processing_policy`)
    record_identifier: Unused for this operation as it is not invertible

#### Methods

##### `__init__`

```python
def __init__(self, *, device: ComputingDevice | None = None, apply_to: list[str] | None = None, seed: int | None = None, error_on_unexpected_behaviour: bool | None = None) -> None
```

##### `__init__`

```python
def __init__(self, template_path: str = '', template_rotation: numpy.ndarray[numpy.float64[3, 1]] = ..., template_translation: numpy.ndarray[numpy.float64[3, 1]] = ..., add_values_to_existing: bool = False, template_mult_factor: float = 1.0, *, device: ComputingDevice | None = None, apply_to: list[str] | None = None, seed: int | None = None, error_on_unexpected_behaviour: bool | None = None) -> None
```

### `Tensor`

```python
class Tensor
```

Class for managing raw Tensors

This class is meant to have direct control over tensors either passed to, or received from a MachineLearningModel.
Unlike the SISBasedElements, there is no inherent stacking/permuting of tensors, and there are no constraints on the order of the Tensor.

Note:
    The API for this class is experimental and may change soon.

#### Methods

##### `__init__`

```python
def __init__(self, tensor: typing_extensions.Buffer) -> None
```

Initialize a Tensor from a numpy array or buffer.

The tensor takes ownership of the data from the provided buffer. Supported types
are float32 and int64. The buffer must be contiguous.

Args:
        tensor: Numpy array or buffer object with float32 or int64 data

##### `shape`

*property*

```python
def shape(self) -> list[int]
```

Return shape of tensor.

### `TensorSet`

```python
class TensorSet(imfusion.Data)
```

Class for managing TensorSets

This class is meant to have direct control over tensors either passed to, or received from a MachineLearningModel.
Unlike the SISBasedElements, there is no inherent stacking/permuting of tensors, and there are no constraints on the order of the Tensor.

Note:
    The API for this class is experimental and may change soon.

#### Methods

##### `__init__`

```python
def __init__(self, tensors: list[Tensor] = []) -> None
```

Initialize a TensorSet

Args:
        tensors: Set of tensors to initialize the TensorSet with.

##### `add`

```python
def add(self, tensor: Tensor) -> None
```

Add a Tensor to this TensorSet.

Args:
        tensor: The Tensor to add to the set

##### `tensor`

```python
def tensor(self, index: int = 0) -> Tensor
```

Retrieve a Tensor at the specified index.

Args:
        index: The index of the tensor to retrieve. Default: 0
Returns:
        The Tensor at the specified index

##### `tensors`

```python
def tensors(self) -> list[Tensor]
```

Retrieve Tensors

### `TensorSetElement`

```python
class TensorSetElement(DataElement)
```

Class for managing raw Tensorsets

This class is meant to have direct control over tensors either passed to, or received from a MachineLearningModel.
Unlike the SISBasedElements, there is no inherent stacking/permuting of tensors, and there are no constraints on the order of the Tensor.

Note:
    The API for this class is experimental and may change soon.

#### Methods

##### `__init__`

```python
def __init__(self, tensorset: TensorSet) -> None
```

Initialize a TensorSetElement from a TensorSet.

Args:
        tensorset: TensorSet containing one or more tensors

##### `__init__`

```python
def __init__(self, tensorset: TensorSet) -> None
```

Initialize a TensorSetElement from a TensorSet.

Args:
        tensorset: TensorSet containing one or more tensors

##### `__init__`

```python
def __init__(self, tensor: Tensor) -> None
```

Initialize a TensorSetElement from a single Tensor.

The tensor will be wrapped in a TensorSet automatically.

Args:
        tensor: Single Tensor to be wrapped in a TensorSetElement

##### `tensor`

```python
def tensor(self, index: int = 0) -> Tensor
```

Access tensor as certain index.

Args:
        index (int)

##### `tensorset`

*property*

```python
def tensorset(self) -> TensorSet
```

Access to the underlying TensorSet.

##### `tensorset`

```python
def tensorset(self, arg1: TensorSet) -> None
```

### `ThresholdOperation`

```python
class ThresholdOperation(Operation)
```

Threshold the input image to a binary map with only 0 or 1 values.

Args:
    value: Threshold value (strictly) above which the pixel will set to 1. Default: 0.0
    to_ubyte: Output image must be unsigned byte instead of float. Default: False
    device: Specifies whether this Operation should run on CPU or GPU.
    seed: Specifies seeding for any randomness that might be contained in this operation.
    error_on_unexpected_behaviour: Specifies whether to throw an exception instead of warning about unexpected behavior.
    apply_to: Specifies fields in a DataItem that this Operation should process (if empty, will select suitable fields based on the current `~imfusion.machinelearning.Operation.processing_policy`)
    record_identifier: Unused for this operation as it is not invertible

#### Methods

##### `__init__`

```python
def __init__(self, *, device: ComputingDevice | None = None, apply_to: list[str] | None = None, seed: int | None = None, error_on_unexpected_behaviour: bool | None = None) -> None
```

##### `__init__`

```python
def __init__(self, value: float = 0.0, to_ubyte: bool = False, *, device: ComputingDevice | None = None, apply_to: list[str] | None = None, seed: int | None = None, error_on_unexpected_behaviour: bool | None = None) -> None
```

### `UndoPaddingOperation`

```python
class UndoPaddingOperation(Operation)
```

Apply the inverse of a previously applied padding operation.
This operation requires the input to have an InversionComponent containing padding information.
The padding information must have been previously stored with a matching record identifier
during the padding operation.

Note: Both GPU and CPU implementations are provided.

Note: If no InversionComponent is present, or no matching record identifier is found,
the operation will return the input unchanged and warn about unexpected behavior.
The operations throws an error if the number of images has changed since the padding was applied.

Args:
    target_identifier: The identifier of the operation to undo. Default: ""

    device: Specifies whether this Operation should run on CPU or GPU.
    seed: Specifies seeding for any randomness that might be contained in this operation.
    error_on_unexpected_behaviour: Specifies whether to throw an exception instead of warning about unexpected behavior.
    apply_to: Specifies fields in a DataItem that this Operation should process (if empty, will select suitable fields based on the current `~imfusion.machinelearning.Operation.processing_policy`)
    record_identifier: Unused for this operation as it is not invertible

#### Methods

##### `__init__`

```python
def __init__(self, *, device: ComputingDevice | None = None, apply_to: list[str] | None = None, seed: int | None = None, error_on_unexpected_behaviour: bool | None = None) -> None
```

##### `__init__`

```python
def __init__(self, target_identifier: str = '', *, device: ComputingDevice | None = None, apply_to: list[str] | None = None, seed: int | None = None, error_on_unexpected_behaviour: bool | None = None) -> None
```

### `UnmarkAsTargetOperation`

```python
class UnmarkAsTargetOperation(Operation)
```

Unmark elements from the input data item as learning "target". This operation is the opposite of `~imfusion.machinelearning.MarkAsTargetOperation`.

Args:
    apply_to: fields to unmark as targets (will initialize the underlying `apply_to` parameter)
    device: Specifies whether this Operation should run on CPU or GPU.
    seed: Specifies seeding for any randomness that might be contained in this operation.
    error_on_unexpected_behaviour: Specifies whether to throw an exception instead of warning about unexpected behavior.
    record_identifier: Unused for this operation as it is not invertible

#### Methods

##### `__init__`

```python
def __init__(self, *, device: ComputingDevice | None = None, seed: int | None = None, error_on_unexpected_behaviour: bool | None = None) -> None
```

##### `__init__`

```python
def __init__(self, apply_to: list[str], *, device: ComputingDevice | None = None, seed: int | None = None, error_on_unexpected_behaviour: bool | None = None) -> None
```

### `VectorElement`

```python
class VectorElement(SISBasedElement)
```

DataElement for storing and processing vector/feature data.

VectorElement wraps a SharedImageSet to represent vector features or embeddings
in ML pipelines. Despite using SharedImageSet internally, it represents
1D feature vectors rather than spatial images.

#### Methods

##### `__init__`

```python
def __init__(self, vectors: imfusion.SharedImageSet) -> None
```

Initialize a VectorElement from a SharedImageSet.

Args:
        vectors: SharedImageSet containing vector/feature data to be wrapped in a VectorElement

##### `from_torch`

*staticmethod*

```python
def from_torch(tensor: torch.Tensor) -> VectorElement
```

Create a VectorElement from a torch Tensor.

This is a convenience wrapper around SharedImageSet.from_torch() that automatically
wraps the result in a VectorElement for 1D vector data.

:param tensor: Instance of torch.Tensor to convert
:return: New VectorElement containing the converted data

## Enums

### `ComputingDevice`

```python
class ComputingDevice
```

Enum specifying which computing device (CPU or GPU) should be used for operations.

Values:
    FORCE_CPU: Always use CPU for processing
    GPU_IF_GL_IMAGE: Use GPU only if the input is a GlImage
    GPU_IF_OPENGL: Use GPU if OpenGL is available
    FORCE_GPU: Always use GPU for processing

Members:

  FORCE_CPU : Always execute on CPU

  GPU_IF_GL_IMAGE : Execute on GPU only if input is a GlImage

  GPU_IF_OPENGL : Execute on GPU if OpenGL is available

  FORCE_GPU : Always execute on GPU

#### Attributes

##### `FORCE_CPU`

```python
FORCE_CPU: typing.ClassVar[ComputingDevice]
```

##### `FORCE_GPU`

```python
FORCE_GPU: typing.ClassVar[ComputingDevice]
```

##### `GPU_IF_GL_IMAGE`

```python
GPU_IF_GL_IMAGE: typing.ClassVar[ComputingDevice]
```

##### `GPU_IF_OPENGL`

```python
GPU_IF_OPENGL: typing.ClassVar[ComputingDevice]
```

#### Methods

##### `__init__`

```python
def __init__(self, value: int) -> None
```

##### `__init__`

```python
def __init__(self, arg0: str) -> None
```

##### `name`

*property*

```python
def name(self) -> str
```

##### `value`

*property*

```python
def value(self) -> int
```

### `ElementType`

```python
class ElementType
```

Enum specifying the type of data element in a DataItem.

Values:
    IMAGE: Image data (stored as SharedImageSet)
    KEYPOINT: Keypoint data (stored as KeypointSet)
    BOUNDING_BOX: Bounding box data (stored as BoundingBoxSet)
    VECTOR: Vector data (stored as SharedImageSet with 1D data)
    TENSOR: Tensor data (stored as TensorSet)

Members:

  IMAGE : Image data element (2D or 3D images)

  KEYPOINT : Keypoint data element (sets of point coordinates)

  BOUNDING_BOX : Bounding box data element (rectangular regions)

  VECTOR : Vector data element (1D arrays of values)

  TENSOR : Tensor data element (arbitrary-dimensional arrays)

#### Attributes

##### `BOUNDING_BOX`

```python
BOUNDING_BOX: typing.ClassVar[ElementType]
```

##### `IMAGE`

```python
IMAGE: typing.ClassVar[ElementType]
```

##### `KEYPOINT`

```python
KEYPOINT: typing.ClassVar[ElementType]
```

##### `TENSOR`

```python
TENSOR: typing.ClassVar[ElementType]
```

##### `VECTOR`

```python
VECTOR: typing.ClassVar[ElementType]
```

#### Methods

##### `__init__`

```python
def __init__(self, value: int) -> None
```

##### `__init__`

```python
def __init__(self, arg0: str) -> None
```

##### `name`

*property*

```python
def name(self) -> str
```

##### `value`

*property*

```python
def value(self) -> int
```

### `ExecutionProvider`

```python
class ExecutionProvider
```

Enum specifying the execution provider (backend) for running machine learning models.

Different execution providers offer varying levels of performance and hardware acceleration.

Values:
    CPU: CPU execution (available on all platforms)
    CUDA: NVIDIA CUDA GPU execution
    CUSTOM: Custom execution provider
    DIRECTML: DirectML GPU execution (Windows)
    MPS: Apple Metal Performance Shaders (macOS)
    OPENVINO: Intel OpenVINO execution

Members:

  CPU : CPU execution provider - available on all platforms

  CUDA : NVIDIA CUDA GPU execution provider

  CUSTOM : Custom execution provider

  DIRECTML : DirectML GPU execution provider (Windows)

  MPS : Apple Metal Performance Shaders execution provider (macOS)

  OPENVINO : Intel OpenVINO execution provider

#### Attributes

##### `CPU`

```python
CPU: typing.ClassVar[ExecutionProvider]
```

##### `CUDA`

```python
CUDA: typing.ClassVar[ExecutionProvider]
```

##### `CUSTOM`

```python
CUSTOM: typing.ClassVar[ExecutionProvider]
```

##### `DIRECTML`

```python
DIRECTML: typing.ClassVar[ExecutionProvider]
```

##### `MPS`

```python
MPS: typing.ClassVar[ExecutionProvider]
```

##### `OPENVINO`

```python
OPENVINO: typing.ClassVar[ExecutionProvider]
```

#### Methods

##### `__init__`

```python
def __init__(self, value: int) -> None
```

##### `name`

*property*

```python
def name(self) -> str
```

##### `value`

*property*

```python
def value(self) -> int
```

### `InterleaveMode`

```python
class InterleaveMode
```

Enum specifying how to interleave data from multiple datasets.

Values:
    ALTERNATE: Alternate between datasets in round-robin fashion
    PROPORTIONAL: Sample from datasets proportionally to their sizes

Members:

  ALTERNATE : Alternate between datasets in round-robin fashion

  PROPORTIONAL : Sample from datasets proportionally to their relative sizes

#### Attributes

##### `ALTERNATE`

```python
ALTERNATE: typing.ClassVar[InterleaveMode]
```

##### `PROPORTIONAL`

```python
PROPORTIONAL: typing.ClassVar[InterleaveMode]
```

#### Methods

##### `__init__`

```python
def __init__(self, value: int) -> None
```

##### `__init__`

```python
def __init__(self, arg0: str) -> None
```

##### `name`

*property*

```python
def name(self) -> str
```

##### `value`

*property*

```python
def value(self) -> int
```

### `ModelType`

```python
class ModelType
```

Enum specifying the type of machine learning model.

Values:
    RANDOM_FOREST: Random forest model
    NEURAL_NETWORK: Neural network model

Members:

  RANDOM_FOREST : Random forest-based model

  NEURAL_NETWORK : Neural network-based model

#### Attributes

##### `NEURAL_NETWORK`

```python
NEURAL_NETWORK: typing.ClassVar[ModelType]
```

##### `RANDOM_FOREST`

```python
RANDOM_FOREST: typing.ClassVar[ModelType]
```

#### Methods

##### `__init__`

```python
def __init__(self, value: int) -> None
```

##### `__init__`

```python
def __init__(self, arg0: str) -> None
```

##### `name`

*property*

```python
def name(self) -> str
```

##### `value`

*property*

```python
def value(self) -> int
```

### `Operation`

```python
class Operation
```

Base class for data preprocessing and augmentation operations in machine learning pipelines.

Operations are used to transform DataItems in a Dataset pipeline. They can process images,
keypoints, bounding boxes, vectors, and tensors. Operations can be chained together to create
complex preprocessing pipelines.

To create a custom operation in Python, inherit from this class and implement:
- `configure`: Configure the operation from Properties
- `configuration`: Return current configuration as Properties
- `process`: Process a DataItem (or override type-specific methods like process_images)

See Also:
    `InvertibleOperation`: For operations that support inversion
    `Dataset`: For using operations in data pipelines

#### Attributes

##### `EVERYTHING`

```python
EVERYTHING: typing.ClassVar[Operation.ProcessingPolicy]
```

##### `EVERYTHING_BUT_LABELS`

```python
EVERYTHING_BUT_LABELS: typing.ClassVar[Operation.ProcessingPolicy]
```

##### `ONLY_LABELS`

```python
ONLY_LABELS: typing.ClassVar[Operation.ProcessingPolicy]
```

#### Methods

##### `__call__`

```python
def __call__(self, item: DataItem) -> None
```

Execute the operation on the input DataItem in-place, i.e. the input item will be modified.

##### `__call__`

```python
def __call__(self, images: imfusion.SharedImageSet, in_place: bool = False) -> imfusion.SharedImageSet
```

Execute the operation on the input images and returns its output.
Args:
        images (SharedImageSet): the input images.
        in_place (bool): If False, the input is guaranteed to be unchanged and the function will return a new object. If True, the input will be changed and the function will return it. (Default: False).

##### `__call__`

```python
def __call__(self, points: KeypointSet, in_place: bool = False) -> KeypointSet
```

Execute the operation on the input keypoints. The output will always be a different set of keypoints, i.e. this function never works in-place.
Args:
        points (SharedImageSet): the input points.
        in_place (bool): if True, the input will be changed and the function will return it. If False, the input is guaranteed to be unchanged and the function will return a new object (Default: False).

##### `__call__`

```python
def __call__(self, boxes: BoundingBoxSet, in_place: bool = False) -> BoundingBoxSet
```

Execute the operation on the input bounding boxes. The output will always be a different set of bounding boxes, i.e. this function never works in-place.
Args:
        boxes (SharedImageSet): the input boxes.
        in_place (bool): If False, the input is guaranteed to be unchanged and the function will return a new object. If True, the input will be changed and the function will return it. (Default: False).

##### `__init__`

```python
def __init__(self, name: str, processing_policy: Operation.ProcessingPolicy = ProcessingPolicy.EVERYTHING_BUT_LABELS, *, device: ComputingDevice | None = None, apply_to: list[str] | None = None, seed: int | None = None, error_on_unexpected_behaviour: bool | None = None) -> None
```

Initialize a custom Operation subclass.

This constructor is used when creating custom operation classes in Python.

Args:
        name: Name identifier for the operation
        processing_policy: Policy determining which fields to process (default: EverythingExceptLabels)
        device: Computing device (CPU/GPU) for the operation
        apply_to: List of field names to process (if empty, uses processing_policy)
        seed: Random seed for reproducible behavior
        error_on_unexpected_behaviour: If True, raise exceptions instead of warnings

##### `active_fields`

*property*

```python
def active_fields(self) -> set[str] | None
```

Fields in the data item that this operation will process.

##### `active_fields`

```python
def active_fields(self, arg1: set[str] | None) -> None
```

##### `computing_device`

*property*

```python
def computing_device(self) -> ComputingDevice
```

The computing device property.

##### `computing_device`

```python
def computing_device(self, arg1: ComputingDevice) -> None
```

##### `configuration`

```python
def configuration(self) -> imfusion.Properties
```

Returns the current configuration as a Properties object

##### `configure`

```python
def configure(self, properties: imfusion.Properties) -> bool
```

Configures the operation with the given properties

##### `does_not_modify_input`

*property*

```python
def does_not_modify_input(self) -> bool
```

Returns True if the operation guarantees not to modify the input data

##### `error_on_unexpected_behaviour`

*property*

```python
def error_on_unexpected_behaviour(self) -> bool
```

Treat unexpected behaviour warnings as errors.

##### `error_on_unexpected_behaviour`

```python
def error_on_unexpected_behaviour(self, arg1: bool) -> None
```

##### `name`

*property*

```python
def name(self) -> str
```

The name of the operation

##### `process`

```python
def process(self, item: DataItem) -> None
```

Execute the operation on the input DataItem in-place, i.e. the input item will be modified.

##### `process`

```python
def process(self, images: imfusion.SharedImageSet, in_place: bool = False) -> imfusion.SharedImageSet
```

Execute the operation on the input images and returns its output.
Args:
        images (SharedImageSet): the input images.
        in_place (bool): If False, the input is guaranteed to be unchanged and the function will return a new object. If True, the input will be changed and the function will return it. (Default: False).

##### `process`

```python
def process(self, points: KeypointSet, in_place: bool = False) -> KeypointSet
```

Execute the operation on the input keypoints. The output will always be a different set of keypoints, i.e. this function never works in-place.
Args:
        points (SharedImageSet): the input points.
        in_place (bool): if True, the input will be changed and the function will return it. If False, the input is guaranteed to be unchanged and the function will return a new object (Default: False).

##### `process`

```python
def process(self, boxes: BoundingBoxSet, in_place: bool = False) -> BoundingBoxSet
```

Execute the operation on the input bounding boxes. The output will always be a different set of bounding boxes, i.e. this function never works in-place.
Args:
        boxes (SharedImageSet): the input boxes.
        in_place (bool): If False, the input is guaranteed to be unchanged and the function will return a new object. If True, the input will be changed and the function will return it. (Default: False).

##### `processing_policy`

*property*

```python
def processing_policy(self) -> Operation.ProcessingPolicy
```

The processing_policy property. Resetting it overrides the default operation behaviour on label.

##### `processing_policy`

```python
def processing_policy(self, arg1: Operation.ProcessingPolicy) -> None
```

##### `record_identifier`

*property*

```python
def record_identifier(self) -> str
```

Identifier used to record this operation for inversion. Setting this enables inversion if the operation supports it.

##### `record_identifier`

```python
def record_identifier(self, arg1: str) -> None
```

##### `seed`

*property*

```python
def seed(self) -> int | None
```

Random seed for operations with randomness

##### `seed`

```python
def seed(self, arg1: int) -> None
```

##### `seed_random_engine`

```python
def seed_random_engine(self, seed: int) -> None
```

Seeds the random number generator for this operation

##### `supports_inversion`

*property*

```python
def supports_inversion(self) -> bool
```

Returns whether this operation supports inversion.

#### Nested Classes

##### `Specs`

```python
class Specs
```

Specification for constructing and configuring an Operation.

This class contains the necessary information to create an operation instance,
including its name, configuration properties, and execution phase.

###### Methods

####### `__init__`

```python
def __init__(self) -> None
```

Initialize an empty Operation::Specs object.

Creates a specification with default values that can be filled in later.

####### `__init__`

```python
def __init__(self, name: str, configuration: imfusion.Properties, when_to_apply: Phase) -> None
```

Initialize an Operation::Specs with full configuration.

Args:
    name: Name of the operation to instantiate
    configuration: Properties object containing operation configuration
    when_to_apply: Phase during which the operation should be executed

####### `name`

*property*

```python
def name(self) -> str
```

Name of the operation to instantiate

####### `name`

```python
def name(self, arg0: str) -> None
```

####### `prop`

*property*

```python
def prop(self) -> imfusion.Properties
```

Configuration properties for the operation

####### `prop`

```python
def prop(self, arg0: imfusion.Properties) -> None
```

####### `when_to_apply`

*property*

```python
def when_to_apply(self) -> Phase
```

Phase during which this operation should be executed (Training/Validation/Inference/Always)

####### `when_to_apply`

```python
def when_to_apply(self, arg0: Phase) -> None
```

#### Nested Enums

##### `ProcessingPolicy`

```python
class ProcessingPolicy
```

Enum specifying which types of data an operation should process.

This controls whether operations process regular data, label/segmentation data, or both.

Values:
        EVERYTHING_BUT_LABELS: Process all data except labels/segmentation masks
        EVERYTHING: Process all data including labels
        ONLY_LABELS: Process only labels/segmentation masks

Members:

  EVERYTHING_BUT_LABELS : Process all data except labels and segmentation masks

  EVERYTHING : Process all data including both regular data and labels

  ONLY_LABELS : Process only labels and segmentation masks, skip regular data

###### Attributes

####### `EVERYTHING`

```python
EVERYTHING: typing.ClassVar[Operation.ProcessingPolicy]
```

####### `EVERYTHING_BUT_LABELS`

```python
EVERYTHING_BUT_LABELS: typing.ClassVar[Operation.ProcessingPolicy]
```

####### `ONLY_LABELS`

```python
ONLY_LABELS: typing.ClassVar[Operation.ProcessingPolicy]
```

###### Methods

####### `__init__`

```python
def __init__(self, value: int) -> None
```

####### `__init__`

```python
def __init__(self, arg0: str) -> None
```

####### `name`

*property*

```python
def name(self) -> str
```

####### `value`

*property*

```python
def value(self) -> int
```

### `ParamUnit`

```python
class ParamUnit
```

Enum specifying the unit of measurement for operation parameters.

Values:
    MM: Millimeters (physical world coordinates)
    FRACTION: Fraction of image size (0.0 to 1.0)
    VOXEL: Voxel units (image pixels/voxels)

Members:

  MM : Millimeters - physical world coordinates

  FRACTION : Fraction of image size (values between 0.0 and 1.0)

  VOXEL : Voxel/pixel units in image space

#### Attributes

##### `FRACTION`

```python
FRACTION: typing.ClassVar[ParamUnit]
```

##### `MM`

```python
MM: typing.ClassVar[ParamUnit]
```

##### `VOXEL`

```python
VOXEL: typing.ClassVar[ParamUnit]
```

#### Methods

##### `__init__`

```python
def __init__(self, value: int) -> None
```

##### `__init__`

```python
def __init__(self, arg0: str) -> None
```

##### `name`

*property*

```python
def name(self) -> str
```

##### `value`

*property*

```python
def value(self) -> int
```

### `Phase`

```python
class Phase
```

Enum specifying the execution phase for machine learning operations.

This enum is used to control when operations in a data processing pipeline should be executed.
It supports bitwise arithmetic to combine multiple phases.

Values:
    TRAINING: Execute only during training phase
    VALIDATION: Execute only during validation phase
    INFERENCE: Execute only during inference/prediction phase
    ALWAYS: Execute in all phases

Example:
    &gt;&gt;&gt; phase = ml.Phase.TRAINING | ml.Phase.VALIDATION  # Combine phases
    &gt;&gt;&gt; ml.Phase.TRAINING in phase  # Check if phase contains TRAINING
    True

Members:

  TRAINING : Execute only during the training phase

  VALIDATION : Execute only during the validation phase

  INFERENCE : Execute only during inference/prediction

  ALWAYS : Execute in all phases (training, validation, and inference)

#### Attributes

##### `ALWAYS`

```python
ALWAYS: typing.ClassVar[Phase]
```

##### `INFERENCE`

```python
INFERENCE: typing.ClassVar[Phase]
```

##### `TRAINING`

```python
TRAINING: typing.ClassVar[Phase]
```

##### `VALIDATION`

```python
VALIDATION: typing.ClassVar[Phase]
```

#### Methods

##### `__init__`

```python
def __init__(self, value: int) -> None
```

##### `__init__`

```python
def __init__(self, arg0: str) -> None
```

##### `__init__`

```python
def __init__(self, arg0: list[str]) -> None
```

##### `name`

*property*

```python
def name(self) -> str
```

##### `value`

*property*

```python
def value(self) -> int
```

### `PredictionOutput`

```python
class PredictionOutput
```

Enum specifying the output format of a machine learning model prediction.

Values:
    UNKNOWN: Unknown output format
    VECTOR: Output is a vector (1D array of values)
    IMAGE: Output is an image (e.g., segmentation mask, reconstructed image)
    KEYPOINTS: Output is a set of keypoints (e.g., landmark coordinates)
    BOUNDING_BOXES: Output is a set of bounding boxes (e.g., object detection results)

Members:

  UNKNOWN : Unknown or unspecified output format

  VECTOR : Output is a 1D vector of values

  IMAGE : Output is an image (e.g., segmentation mask)

  KEYPOINTS : Output is a set of keypoint coordinates

  BOUNDING_BOXES : Output is a set of bounding boxes with coordinates

#### Attributes

##### `BOUNDING_BOXES`

```python
BOUNDING_BOXES: typing.ClassVar[PredictionOutput]
```

##### `IMAGE`

```python
IMAGE: typing.ClassVar[PredictionOutput]
```

##### `KEYPOINTS`

```python
KEYPOINTS: typing.ClassVar[PredictionOutput]
```

##### `UNKNOWN`

```python
UNKNOWN: typing.ClassVar[PredictionOutput]
```

##### `VECTOR`

```python
VECTOR: typing.ClassVar[PredictionOutput]
```

#### Methods

##### `__init__`

```python
def __init__(self, value: int) -> None
```

##### `__init__`

```python
def __init__(self, arg0: str) -> None
```

##### `name`

*property*

```python
def name(self) -> str
```

##### `value`

*property*

```python
def value(self) -> int
```

### `PredictionType`

```python
class PredictionType
```

Enum specifying the type of prediction task.

Values:
    UNKNOWN: Unknown prediction type
    CLASSIFICATION: Classification task (assigning discrete class labels)
    REGRESSION: Regression task (predicting continuous values)
    OBJECT_DETECTION: Object detection task (locating and classifying objects)

Members:

  UNKNOWN : Unknown or unspecified prediction type

  CLASSIFICATION : Classification task - assigning discrete class labels

  REGRESSION : Regression task - predicting continuous values

  OBJECT_DETECTION : Object detection task - locating and classifying objects in images

#### Attributes

##### `CLASSIFICATION`

```python
CLASSIFICATION: typing.ClassVar[PredictionType]
```

##### `OBJECT_DETECTION`

```python
OBJECT_DETECTION: typing.ClassVar[PredictionType]
```

##### `REGRESSION`

```python
REGRESSION: typing.ClassVar[PredictionType]
```

##### `UNKNOWN`

```python
UNKNOWN: typing.ClassVar[PredictionType]
```

#### Methods

##### `__init__`

```python
def __init__(self, value: int) -> None
```

##### `__init__`

```python
def __init__(self, arg0: str) -> None
```

##### `name`

*property*

```python
def name(self) -> str
```

##### `value`

*property*

```python
def value(self) -> int
```

### `RecombineMode`

```python
class RecombineMode
```

Enum specifying the mode for recombining overlapping image patches.

This enum is used by `RecombinePatchesOperation` to determine how
overlapping regions from multiple patches should be merged back into the full image.

Values:
    DEFAULT: Simple averaging of overlapping regions. Each overlapping pixel is averaged equally.
    WEIGHTED: Weighted averaging of overlapping regions.

Members:

  DEFAULT : Simple averaging of overlapping regions

  WEIGHTED : Weighted averaging of overlapping regions

#### Attributes

##### `DEFAULT`

```python
DEFAULT: typing.ClassVar[RecombineMode]
```

##### `WEIGHTED`

```python
WEIGHTED: typing.ClassVar[RecombineMode]
```

#### Methods

##### `__init__`

```python
def __init__(self, value: int) -> None
```

##### `__init__`

```python
def __init__(self, arg0: str) -> None
```

##### `name`

*property*

```python
def name(self) -> str
```

##### `value`

*property*

```python
def value(self) -> int
```

### `ResetCriterion`

```python
class ResetCriterion
```

Enum specifying when to reset a dataset during iteration.

Values:
    FIXED: Reset after a fixed number of items
    SMALLEST_LOADER: Reset when the smallest data loader is exhausted
    LARGEST_LOADER: Reset when the largest data loader is exhausted
    UNKNOWN: Default, invalid value

Members:

  FIXED : Reset after processing a fixed number of items

  SMALLEST_LOADER : Reset when the smallest loader runs out of data

  LARGEST_LOADER : Reset when the largest loader runs out of data

  UNKNOWN : Default, invalid value

#### Attributes

##### `FIXED`

```python
FIXED: typing.ClassVar[ResetCriterion]
```

##### `LARGEST_LOADER`

```python
LARGEST_LOADER: typing.ClassVar[ResetCriterion]
```

##### `SMALLEST_LOADER`

```python
SMALLEST_LOADER: typing.ClassVar[ResetCriterion]
```

##### `UNKNOWN`

```python
UNKNOWN: typing.ClassVar[ResetCriterion]
```

#### Methods

##### `__init__`

```python
def __init__(self, value: int) -> None
```

##### `__init__`

```python
def __init__(self, arg0: str) -> None
```

##### `name`

*property*

```python
def name(self) -> str
```

##### `value`

*property*

```python
def value(self) -> int
```
