---
title: "Install the ImFusion Python SDK 0.12"
sidebarTitle: "Installation"
description: "Install imfusion-sdk 0.12 on Windows, Linux, or macOS via pip. Covers license activation, environment variables, and uninstalling."
---

Install the ImFusion Python SDK 0.12 on Windows, Linux, or macOS with pip:

```bash
pip install imfusion-sdk==0.12.*
```

Activate your license and verify the installation:

```python
import imfusion
print(imfusion.info())
```